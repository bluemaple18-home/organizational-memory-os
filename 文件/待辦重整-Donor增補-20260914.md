# 待辦重整 Donor 增補｜2026-09-14

`文件/待辦重整.md` 仍是現役排序與施工狀態主檔。本檔只把本輪研究對回既有能力列，不建立第二份 backlog。

| Donor | Pin | Existing seam | 最小吸收 |
|---|---|---|---|
| OpenViking | `9b5f43d8` | Context Budget / Bounded Retrieval / Knowledge Lifecycle | L0/L1/L2 context projection、hierarchical retrieval、mutation-diff pattern；不取代既有正式知識流程。 |
| Semantica | `251ecf75` | Provenance / Conflict / Reviewer Graph | conflict detect/analyze/source tracking、provenance、deterministic validation、impact traversal；resolution 只提供建議。 |
| ai-memory | `74d2d31e` | AIWR `SSP-307/308/309` | native-event spool、idempotency、replay、partial completion、handoff failure cases；不建第二 memory runtime。 |
| llmfit | `dc9a8cca` | executor capability evidence | 由 AI Core 產生 capability receipt，OMOS 只消費結果。 |
| oMLX | `ea974149` | local rerank / embedding executor | 真實需求才評估。 |
| Switchyard | `9b6efb94` | executor routing | 只作既有 routing seam 的候選演算法。 |

原施工順序不變：Permission / Retention / Deletion → Canonical Direct-Write Audit → Architecture Canvas。AIWR 仍走 `SSP-307 → SSP-308(optional parallel) → SSP-309 → SSP-310`。
