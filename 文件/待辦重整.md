# 待辦重整

## 一、狀態標籤

重要 Backlog item 以以下狀態標記：

- `ALREADY_EXISTS`：現有架構/元件已涵蓋，不重做
- `PARTIAL`：方向已有但 contract / implementation 不完整
- `MISSING`：真正缺口
- `DONOR_AVAILABLE`：已有成熟先例或可吸收實作
- `CUSTOM_CODE_REQUIRED`：仍需自研產品獨有語意
- `SHOULD_NOT_ADOPT`：明確不應搬進本產品

本輪 Minimal Core Rebaseline 再增加責任分類：

- `DOMAIN_CORE`：產品/domain responsibility，架構必須保留
- `EXECUTOR_ONLY`：Agent / Model / Worker 只是可替換 executor
- `DETERMINISTIC_FIRST`：優先 deterministic code / rule / parser / validator
- `MODEL_ASSISTED`：只有語意問題才使用模型
- `RUNTIME_OWNED`：交既有 runtime / worker / queue / provider，不成為 Knowledge subsystem
- `PROJECTION_ONLY`：可重建衍生物，不能取得 canonical authority
- `ENTERPRISE_GOVERNANCE`：企業安全/治理核心，不因 Agent 瘦身而降級
- `CROSS_CUTTING_PLATFORM_CONTROL`：平台級控制能力，可是 P0/P1，但不是第九顆 Knowledge Domain Core
- `DEFER`：現在不施工
- `REJECT`：目前不應引入

## 二、目前重點盤點

| 能力 | 狀態 | 責任分類 | Knowledge SaaS 優先級 | Rebaseline 裁決 |
|---|---|---|---:|---|
| Raw Evidence Contract | LOCKED_OWNER_ACCEPTED_20260904 | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | LOCKED；identity、version、hash、ACL snapshot、provenance、chronology、payload reference 已封 |
| Evidence → Candidate → Canonical | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE | P0 | KEEP；補 immutable evidence、idempotency、retry、single writer |
| Provenance | PARTIAL | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | KEEP |
| Permission-before-Retrieval | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | KEEP；模型不得成為 enforcement boundary |
| Canonical Single Writer | MISSING | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | KEEP；不必做 Writer Agent/Registry，只鎖唯一 mutation boundary |
| Review / Approval | PARTIAL | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | KEEP；依 Risk 分級，不預設每筆人工 review |
| Conflict / Supersession | PARTIAL | DOMAIN_CORE / MODEL_ASSISTED | P0 | KEEP；Agent chain 降級成 executor |
| Evaluation → Repair → Re-eval | ALREADY_EXISTS / PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE | P0 | KEEP；Failure 先定位 layer，再修正；EvolveMem 只吸 `Evaluate→Diagnose→Proposal→Guard` 的 offline config-eval pattern，禁止 LLM 直接改 production config |
| Managed Policy Floor | MISSING | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | KEEP |
| Permission / Retention / Deletion Contract | MISSING / DONOR_AVAILABLE | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | FIRST FRONTIER；封 ACL delta、source deletion、retention tier、legal hold、projection/cache cleanup |
| Canonical Direct-Write Audit | MISSING | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0 | FIRST FRONTIER；盤所有 writer path 是否繞過 Proposal/Acceptance/Single Writer |
| Minimum Telemetry / Cost | MISSING / DONOR_AVAILABLE | CROSS_CUTTING_PLATFORM_CONTROL；OTel export=PROJECTION_ONLY | P0-min | KEEP；usage/cost 為 operational records，OTel 不是 Knowledge Truth |
| Context Budget | MISSING / DONOR_AVAILABLE | DOMAIN_CORE | P1-A | KEEP；不是 prompt optimization；SimpleMem/EvolveMem 已 pin 為 query-aware / config-budget donor，不新增 Retrieval Planner Agent |
| Bounded Retrieval | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE | P1-A | KEEP；SimpleMem/EvolveMem 的 lexical+dense+structured multi-view、query decomposition、fusion/guard 只作 authorized bounded retrieval donor |
| Knowledge Lifecycle | PARTIAL | DOMAIN_CORE | P1-A | KEEP；不吸 memory eviction authority |
| Config Scope / Precedence | MISSING | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P1-A | KEEP |
| External Capability Admission | MISSING | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P1-A | KEEP |
| Documents / Jira / Outlook / Teams ingestion | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE / DETERMINISTIC_FIRST | P1-A | KEEP；文件+Jira 為第一交付批次，Outlook+Teams 為下一批；全部收斂同一 Evidence Contract；batch executor 可吸 per-item receipt schema，不建 WorkPool/Job Engine |
| Object-Centric Evidence / Work Record | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE / PROJECTION_ONLY(WorkRecord) | P1-A | KEEP；Work Record 是 projection，但 Object/Work semantics 是 domain |
| Repository Intelligence | PARTIAL / DONOR_AVAILABLE | DOMAIN_CORE + PROJECTION_ONLY indexes | P1-B | KEEP；Code index 不取得 knowledge authority |
| Multi-Agent Evaluation | PARTIAL | MODEL_ASSISTED / RUNTIME_OWNED | P1-B | SIMPLIFY；只作 escalation strategy，不造 runtime |
| Reviewer Graph / Impact UI | PARTIAL | PROJECTION_ONLY | P1-B | KEEP as reviewer workflow；Graph 不是真相 |
| Knowledge / Procedure Split | PARTIAL | DOMAIN_CORE | P1-B | KEEP |
| AnswerTrace / EvaluationReceipt Contract | PARTIAL | DOMAIN_CORE / ENTERPRISE_GOVERNANCE | P0-min | KEEP；封 evidence refs、permission decision、projection/model/policy version、chronology、failure layer |
| Outline Production Role Contract | PARTIAL | DOMAIN_CORE / PROJECTION_ONLY 待裁決 | P1-A | OPEN；封 Canonical Store、Publication Surface、Retrieval Projection 的正式分工 |
| Personal Mini Memory Runtime | PARTIAL | EXECUTOR_ONLY / RUNTIME_OWNED | NEXT | SIMPLIFY；不要求每人一套 AI Core |
| Resident Knowledge Agent | PARTIAL | EXECUTOR_ONLY | — | RECLASSIFY 為 Evidence Admission / Candidate Preparation operation |
| Conflict Agent | PARTIAL | EXECUTOR_ONLY / MODEL_ASSISTED | — | RECLASSIFY；保留 Conflict capability，移除 Agent-as-architecture |
| Review Agent | PARTIAL | EXECUTOR_ONLY | — | RECLASSIFY；Review responsibility 保留，executor 可換 |
| Ingestion Agent | PARTIAL | EXECUTOR_ONLY / DETERMINISTIC_FIRST | — | RECLASSIFY；Parser/validator/adapter 優先 deterministic |
| Evaluation Agent | PARTIAL | EXECUTOR_ONLY / MODEL_ASSISTED | — | RECLASSIFY；Evaluation capability 保留 |
| Repair Agent | PARTIAL | EXECUTOR_ONLY | — | SIMPLIFY；Failure routing 到真正 layer owner |
| Vector DB | ALREADY_EXISTS concept | PROJECTION_ONLY | P1-A | KEEP as retrieval engine；禁止 canonical authority |
| RAG Chunk | ALREADY_EXISTS concept | PROJECTION_ONLY | P1-A | KEEP as derived retrieval unit；Chunk != Knowledge |
| Knowledge Graph | PARTIAL | PROJECTION_ONLY | P1/P2 | KEEP；reviewer/impact use 早，高階視覺化晚 |
| Default Multi-Agent Query | SHOULD_NOT_ADOPT | REJECT | — | REJECT；只有 escalation 才 fan-out |
| Shared Blackboard | SHOULD_NOT_ADOPT NOW | DEFER / Trigger | Trigger | 有明確 shared-work demand 再做 |
| Multi-Agent Runtime | SHOULD_NOT_ADOPT NOW | RUNTIME_OWNED / REJECT | — | 不自建，透過 provider/runtime adapter |
| Agent Registry / Agent FSM / Agent Ledger per worker | SHOULD_NOT_ADOPT NOW | REJECT | — | 不因 Agent 名稱建立 permanent subsystem |

## 三、2026-08-26 Current Truth Reconciliation

### LOCKED

- 八個 Minimal Core 與六條 Truth Boundary
- Canonical Single Writer
- Permission-before-Retrieval / Managed Policy Floor
- Deterministic First
- Projection Boundary
- Multi-Agent escalation only
- AI Core 只作 donor / producer / runtime adapter
- STD-01 Raw Evidence field-level contract（`LOCKED_OWNER_ACCEPTED_20260904`）

### PARTIAL

- ObjectLink / WorkRecord confidence contract
- Mutation Proposal / Single Writer transaction contract
- Permission / Retention / Deletion contract
- AnswerTrace / EvaluationReceipt contract
- Document / Jira source adapter mapping

### OPEN

- Permission promotion authority
- Email / Teams work closeout precedence
- Knowledge taxonomy MVP
- ObjectLink assertion acceptance boundary
- Outline Production role
- MVP acceptance metrics

### PREVIOUSLY_RESEARCHED

文件解析、OpenFGA/OPA/Cerbos、Codex/GraphRAG context、OpenChronicle/DayTrail/OCEL、DeepEval、OTel、Microsoft Graph/Jira prior art 已足夠進 exact contract，不再重開概念研究。

2026-09-06 已另外完成並 pin：

`文件/研究包D-SimpleMem-EvolveMem-Projection-Retrieval-Evaluation-20260906.md`

其中：

- `aiming-lab/SimpleMem@db80b6a7c591e0ea730a058e9f5fc4eb06572299`：root MIT；Atomic extraction / query-aware retrieval / projection donor。
- 同 commit 的 `EvolveMem/`：Evaluate→Diagnose→Propose→Guard、multi-view retrieval、offline config evolution donor；未見 subdir separate license，未來 copy 前須再做 path-level license recheck。
- 同 commit 的 `OmniSimpleMem/`：**Apache-2.0**；只列 future multimodal source/projection donor，不插隊 Phase-1。
- `can1357/oh-my-pi@1c929b95a2c42384b7fedd52ed4f374f1be65f6c`：MIT；只吸 per-item incremental receipt / bounded executor pattern，不吸 WorkPool runtime。
- Stencil `The Harness Playbook`（2026-09-02）：工程文獻，只吸 bounded work / state ownership principle，不是 OSS dependency。

上述研究一律 `P1 PRIOR ART / NOT CURRENT FRONTIER`；後續進 Projection、Context Budget、Bounded Retrieval、Evaluation 時直接引用，不重新做概念研究。

### NEEDS_REFRESH

需補 exact repository、commit/tag、license、donor file/API/schema；授權未封者保持 `REFERENCE_ONLY`。Architecture Canvas 必須取得舊 `index.html` 後再改，不憑描述重造互動實作。

2026-09-06 補充：SimpleMem / EvolveMem / Omni-SimpleMem / oh-my-pi 本輪已完成上述 pin 與 license/path ledger；其他 prior art 的 `NEEDS_REFRESH` 狀態不因此自動關閉。

## 四、Agent / Worker Audit 問題

所有現有 Agent / Worker / Reviewer 卡必須回答：

1. Product/domain responsibility 是什麼？
2. 為什麼 Knowledge OS 必須擁有？
3. Agent 是否只是 executor？
4. 是否可以 deterministic？
5. 是否有成熟 OSS / API / SDK 可直接用？
6. 是否真的需要 canonical storage？
7. 是否只是 rebuildable projection？
8. 是否真的需要獨立 lifecycle / DB / registry / ledger / FSM？
9. 如果移除 Agent 名稱，產品 capability 是否仍成立？
10. Phase-1 內部導入是否必要？
11. Future SaaS 是否必要？
12. 現在是否應施工？

## 五、重要 implementation card 必填欄位

```text
問題 / 目標
Product / Domain Responsibility
責任分類：DOMAIN_CORE / EXECUTOR_ONLY / ...
既有接點 Existing Seam
Prior Art
Reuse Candidate
Absorb
Do Not Absorb
License
AI Core Priority
Knowledge SaaS Priority
Priority Rationale
Why Custom Code Is Still Needed
Deterministic First 判斷
Canonical / Projection 判斷
Human Review Requirement
驗收條件
```

如果 `Why Custom Code Is Still Needed` 無法回答，不准直接自己寫。

## 六、高 token 研究卡改寫原則

能找到成熟 donor 的研究卡，改成 teardown / adaptation card：

- Context Budget：拆 Codex / GraphRAG engineering，只自研 ContextPolicy；SimpleMem/EvolveMem 作 query-aware / multi-view / guarded config donor
- Telemetry：採 OpenTelemetry GenAI conventions，只做 mapping
- 文件解析：MarkItDown / Docling / Unstructured / Tika，不自造 parser
- Permission Engine：OpenFGA / OPA / Cerbos 做 semantic diff，不自造 graph evaluator
- Work Grouper：OpenChronicle / DayTrail / OCEL，不自造 sessionization
- Source Adapter：官方 API / SDK → configure → wrap → adapt → 最後才 custom
- Batch executor：可吸 oh-my-pi incremental per-item receipt schema，但不建 WorkPool Service / second Job Engine

## 七、本輪只做 Amendment / Merge / Reclassify

禁止因本輪 Rebaseline 直接大砍架構。

優先順序：

1. `Agent card → Domain Capability card`
2. `Runtime card → Implementation note / RUNTIME_OWNED`
3. `Projection card → PROJECTION_ONLY`
4. `Deterministic operation wrapped as Agent → DETERMINISTIC_FIRST`
5. `Default multi-agent topology → REJECT / escalation only`
6. 保留並強化 Evidence、Canonical Knowledge、ACL、Provenance、Conflict、Freshness、Single Writer、Permission-before-Retrieval、Context Budget、Verification / Acceptance

完整裁決見：`文件/最小核心重基準.md`。

## 八、下一個施工順序

已確認 2026-09-06 prior-art 登錄不改順序、不建立新 frontier。

已鎖定前置：`STD-01 RawEvidenceEnvelope`、`STD-02 SourceAnchor` 與
`STD-03 NormalizedDocument block subset`。

1. CC Schema／STD-03 限域 hardening（`CC-SF-004～008/010`、`STD03-RV-001/002`）—— `COMPLETE_OWNER_ACCEPTED_20260907`
2. Document Adapter Mapping
3. Jira Adapter Mapping
4. Permission / Retention / Deletion Contract
5. Canonical Direct-Write Audit
6. 最新繁體中文互動 Architecture Canvas
7. 再依本表派施工卡

### 施工卡派工（2026-09-09，CC）

Lane B（AIWR）DoD 達成後，Lane A 前置卡已依 `文件/CC開發派工-v0.1.md` 分 Tier 開卡：

| repo # | 卡 | Tier | 狀態 |
|---|---|---|---|
| #2 Document Adapter Mapping | `.work/CARD-DOC-ADAPTER-MAPPING-20260909.md` | T1 | **`ACCEPTED_GO` / MERGED** —— big-review NO_GO → repair-01..06 → F-03 觸發 hard stop → T2 Owner spec-freeze（FP-1…FP-5，`OWNER_SIGNED`）→ T1 收尾 + P2 classification 收斂 + authority 入 tree；最終 targeted authority/parity re-review `9dbae75..62d6c6c` = **GO（0 findings）**。merge commit `ebeaab2`，receipt `.work/evidence/DOC-ADAPTER-MAPPING-REREVIEW-FINAL-20260910.yaml`；branch + worktree 已刪 |
| #3 Jira Adapter Mapping | `.work/CARD-JIRA-ADAPTER-MAPPING-20260909.md` | T1 | **`ACCEPTED_GO` / MERGED** —— big-review NO_GO → repair-01..04（frozen chain `170da7f → 733a9e3 → fb939c1 → 3e18080 → e774603`）；repair-04 再 review GO；merge commit `0ac8c09`，receipt `.work/evidence/JIRA-ADAPTER-MAPPING-REREVIEW-04-20260910.yaml`；branch + worktree 已刪 |
| #4 Permission / Retention / Deletion Contract | `.work/CARD-PERMISSION-RETENTION-DELETION-CONTRACT-20260909.md` | T2 | `BLOCKED_AWAITING_OWNER_SPEC_FREEZE` —— ENTERPRISE_GOVERNANCE / P0 / FIRST FRONTIER；需 Owner 簽 5 項凍結點（retention state machine／deletion 語意／legal hold／ACL delta／projection cleanup），CC 不自行開工 |
| #5 Canonical Direct-Write Audit | `.work/CARD-CANONICAL-DIRECT-WRITE-AUDIT-20260909.md` | T3 | `BLOCKED_AWAITING_OWNER_SCOPE_DECISION` —— 直接稽核 Canonical Single Writer Truth Boundary；需 Owner 定稽核範圍／判準／findings 處置權，CC 停回 Owner |

- **repo #2 與 #3 皆已 `ACCEPTED_GO` 並 merge（`ebeaab2` / `0ac8c09`）。**
  → `EMEM-02`（`SSP-291`，Personal Evidence Profile / Source Mapping）已 **`ACCEPTED_GO` + merged**（`7cfe2fe`）。
    大 review `NO_GO`（1×P1 `SSP291-F-01` 准入值驗證不完整）→ repair-01（`cd30d22`）→ 定點 re-review `GO`（0 findings）→ Owner accept。
    交付 `規格/v0.1/personal-evidence-profile.yaml` + `scripts/validate_personal_evidence_profile_contract.rb`（351 行），
    掛進 `validate_personal_memory_contract.rb` aggregator。這是個人層第一次與兩份已驗收 adapter mapping 建立執行期綁定：
    准入來源必須指名一份 mapping，且投影出的 `source_system` 須等於該 mapping 執行期讀到的值。
    收斂範圍註記：本卡證明的是「准入來源與 adapter 的來源標籤一致」，**不是**完整 RawEvidence 已通過 adapter／STD instance 驗證。
    上游 `role_profiles` 列出 `source_profiles` 未宣告的 `CLIENT_FEEDBACK`／`CRM_ENTITY`／`CLIENT_DOCUMENT`，
    目前一律 fail-closed 拒絕，留 Owner 裁決（非 blocker）。branch 已刪。
  → `SSP302-CONTRACT-TIGHTEN` 已 **`ACCEPTED_GO` + merged**（`5201f1e`）；其 TIGHTEN-F-01 / F-02
    由 `SSP302-ERROR-CODE-COVERAGE` 收尾，狀態 `IMPLEMENTED_AWAITING_TARGETED_REREVIEW`（大 review NO_GO → repair-01/02 → 第三輪 hard stop → Owner spec-freeze `B A A A` → T1 closeout）
    （卡片 `.work/CARD-SSP302-ERROR-CODE-COVERAGE-20260911.md`，
    evidence `.work/evidence/SSP302-ERROR-CODE-COVERAGE-20260911.md`）。
    重點：`error_contract` 改由掃描 evaluator 原始碼綁定，不再是兩張手寫清單互比；
    `LOOP_MISSING_FIELD` 補齊（15 個 code，15/15 有負例覆蓋）；控制負例改為由正例複製、
    只換 `terminal_condition`。另主動補上兩條從未被負例踩到的 `LOOP_EXCEEDS_AUTHORITY` 分支。
  → 仍待 Owner：repo #4（T2 spec-freeze 5 點）、repo #5（T3 scope 決策）。
- #4 / #5 是 `SSP-294`（EMEM-05 Promotion）的前置；在 Owner 簽核 / 定 scope 前 `SSP-294` 維持 `BLOCKED`。

## 九、CC Schema Foundation 非阻塞 backlog

外部 CC targeted re-review 已 `GO`，`CC-SF-001/002/003` 關閉。下列項目不阻塞
`STD-03`，但須在 Document／Jira Adapter Mapping 前完成一次限域強化：

| ID | 後續項目 | 優先度 | 施工時機 |
|---|---|---:|---|
| `CC-SF-004` | JSON／YAML duplicate-key fail-closed detector | P3 | STD-03 後、Adapter Mapping 前 |
| `CC-SF-005` | coverage 輸出改為實際受測／通過／parity 統計 | P3 | 與 CC-SF-004 同卡 |
| `CC-SF-006` | STD-01／02 UUID／reference pattern 寬鬆度對齊 | P3 | Adapter Mapping 前 |
| `CC-SF-007` | JSON_SCHEMA 實際 error set 與宣告 error set 對稱驗證 | P3 | 與 CC-SF-004 同卡 |
| `CC-SF-008` | NON_I_JSON Ruby defensive mirror 正例覆蓋 | P3 | 與 CC-SF-004 同卡 |
| `CC-SF-009` | `source_aliases` 空陣列語意裁決 | `CLOSED_DECISION_ALLOW_EMPTY` | 已於 STD-03 kickoff 裁決；不單獨阻塞開工 |
| `CC-SF-010` | cross-layer negative case name／count exact registry | P3 | Adapter Mapping 前 |
| `STD03-RV-001` | table／image conditional 的專用 schema negatives 與 AST mutation guard | P2 | STD-03 鎖版後與 CC-SF hardening 同卡；Adapter Mapping 前 |
| `STD03-RV-002` | STD-03 JSON Schema actual／declared error set exact parity | P3 | 併入 `CC-SF-007` |

2026-09-07：`CC-SF-004/005/006/007/008/010` 與 `STD03-RV-001/002` 已完成限域 hardening 與 targeted re-review，`SH-RV-001 CLOSED`／verdict `GO`；Owner 已接受 STD-02 timestamp contract 精準化，狀態 `COMPLETE_OWNER_ACCEPTED_20260907`，Adapter Mapping gate 已解除。證據見 `.work/evidence/SCHEMA-HARDENING-PRE-ADAPTER-20260907.md`。

## 十、AIWR 平台落地 Lane（Owner 重排，2026-09-10）

`SSP-298～305` 已完成 AIWR 共用契約、Skill、Hook、Loop、Harness、Hermes 與端到端主管視圖。新缺口不是重做共用契約，而是把 Codex／Claude Code 的原生事件接到既有 AIWR seam，並以真實 runtime evidence 驗證。

| 順序 | Jira | 工作 | 狀態 | 依賴／阻塞 |
|---:|---|---|---|---|
| 1 | `SSP-307` | `AIWR-09` Codex Native Adapter／Runtime Probe | `FRONTIER` | `SSP-301/304` 已完成；blocks `SSP-309` |
| 2 | `SSP-308` | `AIWR-10` Claude Code Native Adapter／Runtime Probe | `PARALLEL_OPTIONAL` | 可與 `SSP-307` 平行；blocks `SSP-309` |
| 3 | `SSP-309` | `AIWR-11` Codex／Claude Code Conformance | `BLOCKED` | blocked by `SSP-307/308`；blocks `SSP-310` |
| 4 | `SSP-310` | `AIWR-12` PM 同事輕量 Pilot／Rollback | `BLOCKED` | blocked by `SSP-309`；relates `SSP-295` |

重排原則：

1. 單一施工者先做實際使用中的 Codex Desktop／App Server；Claude Code 接續。
2. 有第二個施工者才平行 `SSP-307/308`，避免為了形式平行增加協調成本。
3. 兩個 Adapter 都以 lifecycle event 作 Evidence，不取得 AIWR `complete`、Personal Memory acceptance 或 canonical writer authority。
4. `SSP-309` 是 checkpoint；未通過不得進 PM 真實任務。
5. `SSP-310` 只做一位 PM、一個任務、一個平台，先 dry-run，並驗證停用／回退。

Jira payload、建立結果與 link 回讀見 `.work/JIRA-AIWR-ADAPTERS-20260910/`。這是個人／AIWR rollout lane，不取代本文件第八節企業知識 SaaS 主線。

## 十一、CC lane 進度（2026-09-08，2026-09-09 更新）

Lane A（EPM／`SSP-286`）與 Lane B（AIWR／`SSP-287`）依 `文件/CC開發派工-v0.1.md` 並行推進。

| Jira | 契約產出 | 狀態 |
|---|---|---|
| `SSP-290` Personal Knowledge L1～L4 | `personal-harness-integration.yaml` 的 `capability_matrix`／`level_transitions`／`capability_safety_floor` + validator + fixtures | 大 review `NO_GO`（1×P1／2×P2）→ repair 01（`73313c8`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp290-personal-knowledge-levels` |
| `SSP-298` AIWR-01 邊界契約 | 新 `規格/v0.1/ai-work-record-boundary.yaml` + `scripts/validate_ai_work_record_boundary_contract.rb` + fixtures | 大 review `NO_GO`（2×P1／2×P2）→ repair 01（`c91c02d`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp298-aiwr-boundary` |
| `SSP-292` EMEM-03 Recall／MemoryContextPack | `personal-harness-integration.yaml` 的 `recall_context_pack.contract` + `MemoryContextPack` + validator `context_pack_failure` + fixtures | 大 review `NO_GO`（2×P0／2×P1）→ repair 01（`2b507a0`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp292-recall-context-pack` |
| `SSP-299` AIWR-02 任務卡格式 | 新 `規格/v0.1/ai-task-card-record.yaml` + `scripts/validate_ai_task_card_record_contract.rb` + fixtures；cross-reference 至 `ai-work-record-boundary.yaml`（pointer binding） | 大 review `NO_GO`（1×P1／2×P2）→ repair 01（`a49b11f`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp299-task-card-format` |
| `SSP-293` EMEM-04 Correction／Supersession | `correction_flow.contract` + `MemoryCorrectionProposal` / `MemorySupersessionReceipt` + validator `correction_proposal_failure` / `supersession_receipt_failure` + fixtures | 大 review `NO_GO`（1×P0／2×P1／1×P3）→ repair 01（`0b58535`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp293-correction-supersession` |
| `SSP-300` AIWR-03 工作紀錄 Skill | 新 `規格/v0.1/ai-work-record-skill.yaml` + `scripts/validate_ai_work_record_skill_contract.rb` + fixtures；cross-reference 至 boundary / task-card-record（pointer binding） | 大 review `NO_GO`（3×P1／3×P2）→ repair 01（`f630164`）→ targeted re-review `GO` → Owner accept。branch `cc/ssp300-work-record-skill` |
| `REFACTOR-VALIDATOR-STRUCTURE` repo-wide validator 結構 | `scripts/lib/omos_contract_helpers.rb` + `validate_personal_memory_contract.rb` 拆 5 slice + 舊入口改 aggregator | S01 `7b9c714`／S02 `f3bb423`；大 review `NO_GO`（1×P1）→ repair 01 `d4934a5`（aggregator）→ targeted re-review `GO` → Owner accept → **merge `main` @ `3e8589c`**。branch 已刪。 |
| `SSP-301` AIWR-04 Hook 事件擷取 | 新 `規格/v0.1/ai-work-record-hook.yaml` + `scripts/validate_ai_work_record_hook_contract.rb` + fixtures；cross-reference 至 boundary／task-card-record／skill（pointer binding） | 大 review `NO_GO`（2×P1 F-01 disabled 繞過 authority／F-02 failure isolation 只擋 BLOCKED + 1×P2 F-03 event_key 衝突）→ repair 01 `d04f137`（authority 前移 / host_task_impact fail-closed allowlist / `HOOK_DUPLICATE_CONFLICT`）→ targeted re-review `GO`，3 findings CLOSED → Owner accept → **merge `main` @ `b0e0fee`**。branch 已刪。 |
| `SSP-302` AIWR-05 Loop 收口與缺口補登 | 新 `規格/v0.1/ai-work-record-loop.yaml` + `scripts/validate_ai_work_record_loop_contract.rb` + fixtures；cross-reference 至 task-card-record／skill／boundary（pointer binding） | 大 review `NO_GO`（2×P1 F-01 timeout 未 enforce／F-02 mandatory-stop gap 只看最後一輪 + 1×P2 F-03 iteration 未 fail-closed + 1×P3 F-04）→ repair 01 `63673ae`（`elapsed_seconds` + `LOOP_OVER_TIMEOUT` + 逐輪 mandatory-stop + `LOOP_MALFORMED_ITERATION`）→ targeted re-review `GO`，F-01/02/03 CLOSED，F-04/F-05 留 backlog → Owner accept → merge `main`。branch `cc/ssp302-loop-closeout` |
| `SSP-303` AIWR-06 輕量 Harness 編排 | 新 `規格/v0.1/ai-work-record-harness.yaml` + `scripts/validate_ai_work_record_harness_contract.rb` + fixtures；cross-reference 至 hook／skill／loop／boundary（pointer binding） | 大 review `NO_GO`（2×P1 F-01 failed_step 未綁 error step／F-02 no-second-runtime 只掃 4 旗標 + 1×P2 F-03 step 編號未連續）→ repair 01 `a2d7c29`（`HARNESS_FAILED_STEP_MISMATCH` + fail-closed `builds_*` 掃描 + `HARNESS_STEP_INDEX_GAP`）→ targeted re-review `GO`，3 findings CLOSED → Owner accept → **merge `main` @ `df34064`**。branch 已刪。 |
| `SSP-304` AIWR-07 Hermes 薄 Adapter | 新 `規格/v0.1/ai-work-record-hermes-adapter.yaml` + `scripts/validate_ai_work_record_hermes_adapter_contract.rb` + fixtures；cross-reference 至 skill／task-card-record／harness／boundary（pointer binding） | 大 review `NO_GO`（2×P1 F-01 版本清單讀 run 自報／F-02 宣告 table 未約束實際翻譯 + 2×P2 F-03 假 fail-loud／F-04 event_map 未驗 event key）→ repair 01 `e3be0a4`（`supported_versions` 由 spec 傳入 + `HERMES_MAPPING_NOT_DECLARED` + `HERMES_FAKE_FAIL_LOUD` + `HERMES_EVENT_MAP_MALFORMED`）→ targeted re-review `GO`，4 findings CLOSED → Owner accept → **merge `main` @ `b07353a`**。branch 已刪。 |
| `SSP-305` AIWR-08 端到端驗收 + 主管進度視圖 | 新 `規格/v0.1/ai-work-record-e2e-acceptance.yaml` + `scripts/validate_ai_work_record_e2e_acceptance_contract.rb` + fixtures；cross-reference 至 task-card-record／boundary／harness（pointer binding） | 大 review `NO_GO`（4×P1 + 1×P2）→ repair 01 `3c8fcd1` → targeted re-review `GO` → Owner accept → **merge `main` @ `0d68a3f`**。舊 branch／worktree 已清理。 |

### Lane A / Lane B 下一步（2026-09-09 更新）

- **Lane A**：repo 前置卡已開（見上方「施工卡派工」）。可立即進 T1 迴圈的是 repo #2
  `.work/CARD-DOC-ADAPTER-MAPPING-20260909.md` 與 repo #3 `.work/CARD-JIRA-ADAPTER-MAPPING-20260909.md`
  （依賴已滿足，可並行）；完成後 `SSP-291`（EMEM-02）解除。`SSP-294`（EMEM-05 Promotion）仍
  `BLOCKED`，等 repo #4（T2，Owner 簽凍結點）+ #5（T3，Owner 定稽核 scope）。
- **Lane B（AIWR 自動化執行層）DoD 達成（2026-09-09）**：`SSP-298`～`SSP-305` 全 `ACCEPTED_GO` + merged —— 邊界／卡片格式／Skill／Hook／Loop／Harness／Hermes Adapter／端到端驗收 + 主管視圖 契約鏈完整。

### 規範債（2026-09-09 掃描）

對照 `.agentskills/docs/coding-standards.md`（本專案採用）第 2 節 hard limit `< 400 行`：

- `validate_personal_memory_contract.rb` = **1119 行**（EMEM-00/01 舊 518 + CC 加的 SSP-290/292/293）。
- 前人既有超標：`validate_std01`（557）、`validate_std02`（540）、`validate_std03`（429）、`validate_std_schema_engine.py`（436，schema-hardening 推超）。
- 每個 Ruby validator 各自帶一份 `StrictJsonObject` / dup-key guard / `read_json` / `read_yaml` / `present?` / URN helper ≈ 50–70 行 × 8 檔重複。
- CC 這輪從零寫的 AIWR validator（362／300／264）合規。

**處置**：一張 repo-wide validator Refactor 卡（Refactor Mode / strict / 自己的 review）——

1. `scripts/lib/omos_contract_helpers.rb` 抽共用 helper，各 validator `require_relative`。
2. `validate_personal_memory_contract.rb` 拆：EMEM-00/01 留原檔；`validate_personal_capability_contract.rb`（SSP-290）／`validate_recall_context_pack_contract.rb`（SSP-292）／`validate_correction_flow_contract.rb`（SSP-293）。
3. STD-01/02/03 + schema engine（`LOCKED_OWNER_ACCEPTED`）：只做 helper 抽取（行為不變），結構拆分另議。

#### 處置結果（`REFACTOR-VALIDATOR-STRUCTURE-20260909`，branch `cc/refactor-validator-structure`）

- **S01（`7b9c714`）**：`scripts/lib/omos_contract_helpers.rb` 已抽；8 個 Ruby validator 改 `require_relative`。`validate_std03` 429 → 397（僅移除重複 helper 即回到 `< 400`）。10/10 validator stdout+exit 對 golden 逐字相同。
- **S02**：`validate_personal_memory_contract.rb` 1119 → 拆 5 個 slice（全部 `< 400`）：`validate_personal_memory_scope_contract.rb`（EMEM-00）／`validate_personal_memory_resource_contract.rb`（347，EMEM-01）／`validate_personal_capability_contract.rb`（253）／`validate_recall_context_pack_contract.rb`（179）／`validate_correction_flow_contract.rb`（218）。
- **S02 repair 01（大 review `NO_GO` 1×P1）**：拆分把舊入口 `validate_personal_memory_contract.rb` 的 coverage 縮成只剩 EMEM-00 —— 仍只呼叫舊命令的 CI／流程會在 SSP-290/292/293 或 EMEM-01 壞掉時得到假綠。修法（`d4934a5`）：舊檔名改成 backward-compatible **aggregator**（`open3` fail-closed 依序跑 5 個 slice，全綠才印原字串），EMEM-00 本體搬到 `validate_personal_memory_scope_contract.rb`。舊命令對 5 個 slice 各補 1 條 mutation parity，全部同 base 一樣 RED。行為逐字不變（aggregator 對 golden byte-identical）。**定點複審 `GO`，F-01 CLOSED，regression PASS** → Owner accept → merge `main`。
- **Refactor 結案**：`REFACTOR-VALIDATOR-STRUCTURE-20260909` = `ACCEPTED_GO`。合規現況：14 個 Ruby validator 有 11 個 `< 400`；仍超標 `validate_std01`(525)／`validate_std02`(512)／`validate_std_schema_engine.py`(436) 皆 `LOCKED_OWNER_ACCEPTED`，留 backlog。
- **合規現況**：13 個 Ruby validator 有 11 個 `< 400`。仍超標：`validate_std01`（525）、`validate_std02`（512）、`validate_std_schema_engine.py`（436）—— 皆 `LOCKED_OWNER_ACCEPTED`，卡片 Scope 明訂只做 helper 抽取、結構拆分需重開已鎖契約，留 backlog。
- **S03（DEFER）**：`draft_card_contract_valid?`（SSP-300）與 `task_card_record_failure`（SSP-299）的第二份 shape 實作去重 —— AIWR 三檔已 `< 400`，S03 需動已鎖 evaluator，風險大於收益，維持「已標註第二份實作 + parity 測試」，留 backlog。

### 規範債 backlog（2026-09-09 掃描；2026-09-10 triage）

| 項目 | Tier / 處置 | 狀態 |
|---|---|---|
| STD-01/02 + `std_schema_engine.py` 結構拆分（> 400 行） | 需 Owner 決定是否重開 `LOCKED_OWNER_ACCEPTED` 契約檔 | `BLOCKED_OWNER`。3 個檔皆 LOCKED；卡片 Scope 明訂只抽 helper、結構拆分需 Owner 授權重開。 |
| SSP-299/300 shape 實作去重（S03） | Refactor Mode / strict（動兩張已鎖 evaluator，需行為不變驗證 + 大 review） | `DEFERRED`。維持「已標註第二份實作 + parity 測試」；待 Owner 排 Refactor 卡。 |
| SSP-302 F-04（P3，`CLOSED` 語意）+ F-05（P2，timeout 反向一致性） | T1 `.work/CARD-SSP302-CONTRACT-TIGHTEN-20260910.md` | **`CLEARED`** —— 大 review `f2e47ca..8d9c829` = GO（P0/P1 = 0）；merge `5201f1e`，receipt `.work/evidence/SSP302-CONTRACT-TIGHTEN-REVIEW-20260910.yaml`。F-04 依選項 (a)、F-05 依嚴格大於結案。 |
| SSP-302 TIGHTEN-F-01（P2，error-code 契約不完整：`LOOP_MISSING_FIELD` 未宣告，斷言只比對兩張手寫清單）+ TIGHTEN-F-02（P3，控制測資實際差四欄，與證據描述不符） | T1，已開卡 `.work/CARD-SSP302-ERROR-CODE-COVERAGE-20260911.md` | `DRAFT_READY`。TIGHTEN 大 review 的非阻塞 finding，reviewer disposition = `NON_BLOCKING_BACKLOG`；不是 allow/deny bypass。可直接進迴圈。 |
| SSP-302：`filled_fields` gap provenance matching；`timeout_seconds` 無上界 | reviewer 明確不列 finding | `WONT_FIX`（除非日後有 measured gap）。 |
| SSP-303 handoff 加壓面 #2（step 編號連續性已補）/ SSP-304 加壓面（rollback evaluator 三份重複） | 觀察項，非 finding | `NOTED`。rollback evaluator 抽共用 helper 併入 S03 一起評估。 |

下一步（2026-09-11）：Lane A 的 `SSP-291` 已 accepted + merged；`SSP-294` 仍被 repo #4/#5 擋住，Lane A 暫時走不動，實質施工面在 Lane B；Lane B 的 `SSP-298～305` 已完成，平台落地新 frontier 改為 `SSP-307`，`SSP-308` 可平行，之後依序 `SSP-309` → `SSP-310`。
