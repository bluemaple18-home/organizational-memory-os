---
id: SSP298-REPAIR-01-20260907
type: evidence
card: .work/CARD-SSP298-REPAIR-01-20260907.md
review_line: SSP298-REVIEW-20260907
status: REPAIRED_AWAITING_TARGETED_REREVIEW
---

# SSP-298 Repair 01 證據

## Checkpoint

- Branch：`cc/ssp298-aiwr-boundary`。
- Repair commit：`c91c02dee2129547cb6cf25c3cbf7abf0c4e94b9`。
- Parent：`5bf7357cf47900435f9b2695b606eac018916fb0`（原 evidence+handoff commit）。
- Tree：`bf2ac237d93e36196f244cf981b0febce5e011aa`。
- 原 review 目標 `67f76fa5fdc7aadee178e45d76af05c66bb5139c` 維持 immutable。

## 變更（`5bf7357..c91c02d`）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP298-REPAIR-01-20260907.md` | `66e5485047716b10d913001457cf1a95d2202486` |
| M | `scripts/validate_ai_work_record_boundary_contract.rb` | `9b8009b990f519685eda63d72759242aa254cba8` |
| M | `規格/v0.1/ai-work-record-boundary.yaml` | `4f14cbc4011c8c6274cb7698a1c1f0ce444d3b0e` |
| M | `規格/v0.1/fixtures/ai-work-record-boundary-negative-fixtures.json` | `d75ea488e4a29b9d10985c2d47aafbdd692f312a` |
| M | `規格/v0.1/fixtures/ai-work-record-boundary-positive-fixtures.json` | `811437e3027ad30828a3bfdc88c209c677a64528` |

## Finding 處置

### SSP298-RV-004（P1）CLOSED

`promotion_path_failure` 重寫：

- 移除舊 `PROMOTION_PREREQ_ORDER` 與「只有含 RECORD 才檢查」的邏輯。
- `steps` 必須是 `EXPECTED_PROMOTION_STEPS` 由 index 0 起的連續 prefix（`steps == EXPECTED_PROMOTION_STEPS[0, steps.length]`）。含 `PERSONAL_MEMORY_RECORD` 時 prefix 長度必為 6 = 完整 sequence。
- duplicate token → `PROMOTION_DUPLICATE_STEP`（在 unknown-step 之後、prefix 檢查之前）。
- 空 `steps` → `PROMOTION_EMPTY_PATH`。
- 第一個偏離位置的「應有步驟」決定 code：`RAW_EVIDENCE_ENVELOPE` → `PROMOTION_SKIPS_EVIDENCE`；`VERIFICATION` / `PERSONAL_ACCEPTANCE` → `PROMOTION_SKIPS_VERIFICATION_OR_ACCEPTANCE`；其餘 → `PROMOTION_STEPS_OUT_OF_ORDER`。
- candidate-only 正例 `[TASK_CARD_OR_WORK_RECORD, RAW_EVIDENCE_ENVELOPE, PERSONAL_MEMORY_CANDIDATE]` 是合法 3-prefix，仍 allow。

### SSP298-RV-005（P1）CLOSED

- `automated_step_failure(step, allowed_gate_positions)`：`input_schema_ref` / `output_schema_ref` 改用 `present?`（null / 空字串視為缺 → `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`）。
- 契約新增 `automated_step_contract.human_acceptance_gate_position`（列入 `required_per_step`）與 `allowed_gate_positions: [VERIFICATION, PERSONAL_ACCEPTANCE]`。
- validator：`human_acceptance_gate_position` 缺 → `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`；不在 `allowed_gate_positions` → `AUTOMATED_STEP_GATE_POSITION_INVALID`。structural 斷言 `required_per_step` 與 `allowed_gate_positions` exact-set。
- 新負例：`AIWR_AS_NEG_NULL_SCHEMA_REF`、`AIWR_AS_NEG_MISSING_GATE_POSITION`（→ `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`）、`AIWR_AS_NEG_BAD_GATE_POSITION`（→ `AUTOMATED_STEP_GATE_POSITION_INVALID`）。既有 `AIWR_AS_NEG_NO_HUMAN_GATE` / `AIWR_AS_NEG_FAIL_SILENT` 補 `human_acceptance_gate_position` 使唯一缺陷為原意圖項。

### SSP298-RV-006（P2）CLOSED

`validate_ai_work_record_boundary_contract.rb` structural 斷言：

- `sorted_set(task_card.forbidden_authority) == sorted_set([memory_acceptance, verification, canonical_writer])`。
- `sorted_set(work_record.forbidden_authority) == sorted_set([memory_acceptance, canonical_writer])`。
- 常數 `EXPECTED_TASK_CARD_FORBIDDEN_AUTHORITY` / `EXPECTED_WORK_RECORD_FORBIDDEN_AUTHORITY`。

### SSP298-RV-007（P2）CLOSED

- `promotion_path_failure`：`promotion["origin"] == "TASK_CARD"` 且 `steps == [TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_RECORD]` → `TASK_CARD_DIRECT_TO_RECORD`（在 duplicate 檢查之後、prefix 檢查之前）。
- `AIWR_PP_NEG_TASK_CARD_DIRECT_TO_RECORD` 的 `steps` 由 `[PERSONAL_MEMORY_RECORD]` 改為 `[TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_RECORD]`，`origin: TASK_CARD`；`expected_failure_code` 仍 `TASK_CARD_DIRECT_TO_RECORD`，但現在打的是真正 contract-shaped 的直接跳躍。
- 新增 `AIWR_PP_NEG_DUPLICATE_STEP` → `PROMOTION_DUPLICATE_STEP`；`AIWR_PP_NEG_PARTIAL_SKIPS_EVIDENCE`（`[TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_CANDIDATE]`，無 RECORD）→ `PROMOTION_SKIPS_EVIDENCE`。

## Gate 結果（在 `c91c02d`）

| Gate | 結果 |
|---|---|
| `ruby scripts/validate_ai_work_record_boundary_contract.rb` | `PASS` |
| `ruby scripts/validate_personal_memory_contract.rb` | `PASS` |
| STD schema engine | `PASS` |
| STD-00 / 01 / 02 / 03 | `PASS` × 4 |
| Cross-layer | `PASS`；8 negatives rejected |
| `git diff --check 5bf7357 c91c02d` | clean |
| JSON / YAML parse | OK |

7 個原必備 `EXPECTED_BOUNDARY_NEGATIVE_LABELS` 仍全覆蓋。

## Mutation 探針（對 `c91c02d`，還原乾淨）

| 探針 | 結果 |
|---|---|
| 停用 prefix 檢查（`if false`） | RED ✓ |
| 移除 duplicate-step guard | RED ✓ |
| `input_schema_ref` 改回 `key?` | RED ✓ |
| 移除 gate-position membership 檢查 | RED ✓ |
| YAML `allowed_gate_positions` 竄改 | RED ✓ |
| YAML `task_card.forbidden_authority` 移除 `canonical_writer` | RED ✓ |
| `AIWR_PP_NEG_TASK_CARD_DIRECT_TO_RECORD` 宣告錯誤 code | RED ✓ |

還原後 `git diff --stat` 為空。

## 待辦

- 定點複審交接卡：`.work/handoff/SSP298-REREVIEW-01-20260907.md`（範圍 `5bf7357..c91c02d`）。
- 複審只驗 `SSP298-RV-004/005/006/007` 關閉與 regression。
