---
id: SSP293-REPAIR-01-20260909
type: evidence
card: .work/CARD-SSP293-REPAIR-01-20260909.md
review_line: SSP293-REVIEW-20260909
status: REPAIRED_AWAITING_TARGETED_REREVIEW
---

# SSP-293 Repair 01 證據

## Checkpoint

- Branch：`cc/ssp293-correction-supersession`。
- Repair commit：`0b58535b8e9ff318915ac49f9bf15176ae2e8650`。
- Parent：`1f0fbc6b10028e73ff9698d7605ef58eb6aafbe6`（原 evidence+handoff commit）。
- Tree：`d5e67f9d75c2f16978c1b75a6a77601216c847aa`。
- 原 review 目標 `8ca640b1417189bd8d8637c7035afa00843e96c3` 維持 immutable。

## 變更（`1f0fbc6..0b58535`）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP293-REPAIR-01-20260909.md` | `7ea166eccd41e304344f978b99dbf8318df03950` |
| M | `scripts/validate_personal_memory_contract.rb` | `2030b9b8efbfe5ed66279a8e0a6a72ea280a9b72` |
| M | `規格/v0.1/personal-harness-integration.yaml` | `442b45fb532040d506d5b378e17cd77f64b85feb` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `a118ce717a5707ed7ddca2ab20edda129ffc44a2` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `1a2a9414aa4f8b4baeea7a36c2202110474d00a3` |

## Finding 處置

### SSP293-RV-001（P0）CLOSED
- 新 helper `omos_urn?(value, namespace)`（`urn:omos:<ns>:<...>` string，前綴比對 + pattern）。
- `supersession_receipt_failure`：`verification_receipt_ref` 必須 `omos_urn?(_, "verification")`、`personal_acceptance_ref` 必須 `omos_urn?(_, "acceptance")`；否則 `CORRECTION_SKIPS_VERIFICATION` / `CORRECTION_SKIPS_ACCEPTANCE`。`PASS + 任意 scalar` 不再能通過 gate。
- 負例：`CORR_RECEIPT_NEG_FAKE_VERIFICATION_REF`（`123`）→ `CORRECTION_SKIPS_VERIFICATION`；`CORR_RECEIPT_NEG_FAKE_ACCEPTANCE_REF`（`true`）→ `CORRECTION_SKIPS_ACCEPTANCE`。

### SSP293-RV-002（P1）CLOSED
- `correction_proposal_failure`：`correction_evidence_refs` 每項必須 `omos_urn?(_, "evidence")`；`[null]` / `[{...}]` → `CORRECTION_MISSING_EVIDENCE`。
- 負例：`CORR_PROP_NEG_INVALID_EVIDENCE_REF`（`[{"fake": true}]`）→ `CORRECTION_MISSING_EVIDENCE`。

### SSP293-RV-003（P1）CLOSED
- `EXPECTED_CORRECTION_PROPOSAL_FIELDS` / `EXPECTED_SUPERSESSION_RECEIPT_FIELDS` 的 `created_at` → `chronology.created_at`（與既有 `PersonalMemoryCandidate` / `Record` 一致）。
- yaml `proposal_required_fields` / `supersession_receipt_required_fields` 同步改。
- evaluator：missing-field 檢查改用 `present?(dig_dotted(obj, field))`（支援 dotted path）。
- fixtures：所有 correction / supersession 物件的 `created_at` 遷移為 `chronology.created_at`。
- 卡片 Acceptance #1 與 machine contract 現在對齊。

### SSP293-RV-004（P3）CLOSED
- 新 structural cross-assert：讀 `PersonalMemoryRecord.lifecycle.allowed_transitions.ACTIVE`，每個 `correction_kind` 目標 status 必須同時 ∈ `record_status` enum **且** ∈ ACTIVE 的 allowed transitions。

## Gate 結果（在 `0b58535`）

| Gate | 結果 |
|---|---|
| `ruby scripts/validate_personal_memory_contract.rb` | `PASS`（含新 correction 分支；SSP-290 6 + SSP-292 6 negative label 未 regress） |
| STD schema engine | `PASS` |
| STD-00 / 01 / 02 / 03 | `PASS` × 4 |
| Cross-layer | `PASS`；8 negatives rejected |
| `git diff --check 1f0fbc6 0b58535` | clean |
| JSON / YAML parse | OK |

## Mutation 探針（對 `0b58535`，還原乾淨）

| 探針 | 結果 |
|---|---|
| `verification_receipt_ref` 還原成 `present?`-only | RED ✓ |
| `personal_acceptance_ref` 還原成 `present?`-only | RED ✓ |
| 移除 `correction_evidence_refs` 的 urn 檢查 | RED ✓ |
| yaml `proposal` 的 `chronology.created_at` 還原成 `created_at`（與 `EXPECTED_*` 不符） | RED ✓ |
| `correction_kinds.INVALIDATE` 改成非 ACTIVE-transition 目標 | RED ✓ |
| `CORR_RECEIPT_NEG_FAKE_VERIFICATION_REF` 宣告錯誤 code | RED ✓ |

還原後 `git diff --stat` 為空。

## 待辦

- 定點複審交接卡：`.work/handoff/SSP293-REREVIEW-01-20260909.md`（範圍 `1f0fbc6..0b58535`）。
