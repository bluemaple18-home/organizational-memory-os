# PMCORE-00／01 驗收證據｜2026-09-02

## Scope

- 任務卡：`.work/CARD-PMCORE-00-01-20260902.md`
- 規格：`規格/v0.1/personal-harness-integration.yaml`
- 主文件：`文件/個人知識庫Harness提案查核與整合裁決-20260830.md`
- Validator：`scripts/validate_personal_memory_contract.rb`
- Fixtures：
  - `規格/v0.1/fixtures/personal-memory-positive-fixtures.json`
  - `規格/v0.1/fixtures/personal-memory-negative-fixtures.json`

## Evidence

```text
git fetch origin main
HEAD = origin/main = FETCH_HEAD = 91610b249041956f3e004e378c30a6600221a33d
```

```text
CodeGraph semantic query
CONTEXT_DEGRADED: repo has no .codegraph/ index; fallback used scoped rg/read over PMCORE files.
```

```text
ruby scripts/validate_personal_memory_contract.rb
PASS personal memory contract validation
```

```text
ruby -e 'require "yaml"; require "json"; YAML.load_file("規格/v0.1/personal-harness-integration.yaml"); JSON.parse(File.read("規格/v0.1/fixtures/personal-memory-positive-fixtures.json")); JSON.parse(File.read("規格/v0.1/fixtures/personal-memory-negative-fixtures.json")); puts "PASS parse yaml/json"'
PASS parse yaml/json
```

```text
git diff --check
PASS: no whitespace errors
```

## Acceptance Mapping

- PMCORE-00：`ownership_mode` 與 `visibility_scope` 已分軸；三種 personal scope mode 均有 owner、visibility、consent/notice、retention、offboarding 語意。
- PMCORE-00：employee／manager／admin／reviewer／system 對 READ／WRITE／DELETE／EXPORT／PROMOTE 的 actor-action policy 已可由 validator 覆蓋。
- PMCORE-00：validator 已鎖住 L1～L4 四等級共用同一核心契約，避免不同員工等級分裂 lifecycle／schema。
- PMCORE-00：positive fixture 必須預期 `allow`、negative fixture 必須預期 `deny`，避免 fixture 透過錯誤 expected 值自證。
- PMCORE-00：negative fixtures 覆蓋 admin default search、跨員工讀取、未授權 connector、model-confidence promotion、history erasure、stale warning、role lifecycle fork、AI Core layout 誤升 universal schema、shared context 夾帶 private material、promotion bypass review。
- PMCORE-01：PersonalMemoryCandidate／PersonalMemoryRecord／support／conflict／correction／supersession／context pack／promotion proposal 的 contract registry 與 minimum fields 已落在 machine-readable YAML。
- PMCORE-01：目前仍是 contract/schema slice；Connector、DB、Hook、Loop、Harness、Hermes 均未啟動。

## Status

GO for owner review; not merged, not pushed, not deployed.

## P1 Repair Verification

- P1-1：`EMPLOYEE` 與 `SYSTEM` 對 `SHARED_WORK_CONTEXT` 的 `WRITE` policy 均直接要求 `private_material_redacted`；兩個缺此條件的 fixture 均為 `deny`，不依賴 `HARD_DENY_FLAGS`。
- P1-2：`MANAGER` 對 `COMPANY_MANAGED_PERSONAL` 的 `PROMOTE` 要求 business need、公司 promotion policy 與員工 notice；對 `SHARED_WORK_CONTEXT` 要求 business need 與 source ACL。validator 直接斷言上述 policy 結構，fixtures 同時覆蓋完整授權的 `allow` 及缺 notice／source ACL 的 `deny`。

```text
ruby -c scripts/validate_personal_memory_contract.rb
Syntax OK

ruby scripts/validate_personal_memory_contract.rb
PASS personal memory contract validation

ruby -e 'require "yaml"; require "json"; YAML.load_file("規格/v0.1/personal-harness-integration.yaml"); JSON.parse(File.read("規格/v0.1/fixtures/personal-memory-positive-fixtures.json")); JSON.parse(File.read("規格/v0.1/fixtures/personal-memory-negative-fixtures.json")); puts "PASS parse yaml/json"'
PASS parse yaml/json

git diff --check
PASS: no whitespace errors
```
