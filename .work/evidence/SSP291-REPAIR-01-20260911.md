# SSP-291 Repair 01 — SSP291-F-01 准入值驗證　evidence

日期：2026-09-11　branch：`cc/ssp291-personal-evidence-profile`
原 review commit：`17893f0`（immutable）　base：`5e5d9a8`

## 收的 finding

`SSP291-F-01`（P1）唯一一筆。不拆卡、不擴張 scope。

## 修法

reviewer 指定的定點修法，逐條照做：

1. 本檔新增 `admission_value?(value)` = **非空白 String**。不造 URI scheme、不連外解析。
2. `ADMISSION_CONTAINER_TYPES = { "connector_grants" => Array, "capture_scope" => Hash }`
   —— 這是本卡自有的准入形狀，不是上游列舉，不違反「零列舉重述」。
3. `within_capture_scope?` 先驗兩個 scope 清單是 Array、且 capture 帶合法識別值，再比成員。
4. projected identity 的相等比較前，右手邊兩個值已被保證為合法識別值（見下方「刪掉的死分支」）。
5. **錯誤碼集合維持 16 個、順序不變。**
6. 契約新增 `admission_value_constraints` 四條 rule，由 validator 斷言存在。

### 共用 helper 逐字未改

```
git diff --stat scripts/lib/omos_contract_helpers.rb  →  (空)
```

reviewer 明確要求不得改共用 `present?` 的全域語意（會影響本輪未受審的呼叫端），已遵守。

### 刪掉的死分支（本輪新發現，主動回報）

我一開始在 projected identity 比較前加了兩條型別 guard：

```ruby
return "EPROFILE_ADAPTER_MAPPING_MISMATCH" unless admission_value?(projected["source_system"])
return "EPROFILE_PROJECTED_IDENTITY_INCONSISTENT" unless admission_value?(projected["source_instance_id"])
```

parity probe 顯示這兩條是 **GREEN(bad)** —— 中和後 gate 仍綠，因為相等比較本身就已拒絕
`nil` / `false` / 錯型別：`source_system` 由結構斷言保證 mapping 必須提供非空值，
`capture["source_instance_id"]` 已由 `within_capture_scope?` 保證是非空白字串。

依照上一輪 reviewer 對 `EPROFILE_ADAPTER_MAPPING_NOT_BOUND` 的裁決邏輯（不留永遠踩不到的
死分支），我把這兩條刪掉，並在原處留註解寫明**它們依賴哪兩個前提**，
若日後鬆動任一前提必須補回。

## 驗證

### 1. reviewer 列出的 8 個放行情境 —— 全部轉 deny

直接用 frozen 的正例當 base，逐項套用 reviewer 描述的單欄修改，呼叫修好的 evaluator：

| 情境 | 修好後結果 |
| --- | --- |
| A1 `consent_or_notice_ref = false` | DENY `EPROFILE_NO_CONSENT_OR_NOTICE` |
| A2 Jira `grant_ref = false` | DENY `EPROFILE_NO_CONNECTOR_GRANT` |
| A3 `retention_policy_ref = false` | DENY `EPROFILE_PROFILE_FIELD_MISSING` |
| A4 `consent_or_notice_ref = "   "` | DENY `EPROFILE_NO_CONSENT_OR_NOTICE` |
| A5 DOCUMENT 正例 `consent = false` | DENY `EPROFILE_NO_CONSENT_OR_NOTICE` |
| A6 DOCUMENT 正例 `grant_ref = false` | DENY `EPROFILE_NO_CONNECTOR_GRANT` |
| B1 `containers` 含 null + capture 無 `container` | DENY `EPROFILE_OUTSIDE_CAPTURE_SCOPE` |
| B2 instance 清單含 null + capture/projected 皆無 `source_instance_id` | DENY `EPROFILE_OUTSIDE_CAPTURE_SCOPE` |

**仍被放行：0 / 8。** 重播腳本 `$CLAUDE_JOB_DIR/tmp/replay_f01.rb`（內容已收進交付包附錄）。

### 2. Evaluator guard parity（19 條）

中和方式：把條件改成永不觸發（`unless true` / `if false`），保留 code 字面量。
備份還原用 `cp`，不用 `git checkout --`。

**19 RED / 0 GREEN。其中 assertion-RED 17、exception-RED 2。**

exception-RED 兩條都是移除物件型別 guard 後產生 Ruby 例外，**不是乾淨的契約拒絕**：

- `group_value.is_a?(Hash)`（profile 群組型別）
- `container.is_a?(container_type)`（容器欄位型別）

依 reviewer 上一輪的要求，這裡分開列，不寫成「每條都得到乾淨的契約拒絕」。

### 3. 值約束本身的 probe（9 條，本輪新增）

這組是專門用來證明「新加的值約束是承重的，不是裝飾」：

| # | 動作 | 結果 |
| --- | --- | --- |
| H1 | `admission_value?` 退回共用 `present?` 語意 | RED/assertion |
| H2 | 移除 scope 兩個清單的 Array 型別檢查 | RED/exception |
| H3 | 移除 `capture.source_instance_id` 識別值檢查 | RED/assertion |
| H4 | 移除 `capture.container` 識別值檢查 | RED/assertion |
| H5 | `grant_ref` 退回 `present?` | RED/assertion |
| H6 | 容器欄位型別檢查退回 `present?` | RED/exception |
| H7 | profile 欄位值檢查退回 `present?` | RED/assertion |
| H8 | consent 值檢查退回 `present?` | RED/assertion |
| H9 | 契約 `admission_value_constraints` 區塊改名 | RED/assertion |

**9 RED / 0 GREEN（assertion 7 / exception 2）。**
H1 是關鍵：把新約束退回舊語意就轉紅，證明修正確實承重。

### 4. 結構／跨契約 probe（6 條，回歸）

| # | 動作 | 結果 |
| --- | --- | --- |
| P1 | evaluator 改名 code、YAML 未同步 | RED/assertion |
| P2 | 移除 DOCUMENT adapter binding | RED/assertion |
| P3 | 改 `jira-adapter-mapping.yaml` 的 `source_system` 值 | RED/assertion |
| P4 | 上游 `phase_1_pilot.first_sources` 多一個來源 | RED/assertion |
| P5 | `upstream_bindings` 指向不存在的上游路徑 | RED/exception |
| P6 | authority floor 少一項禁止授權 | RED/assertion |

**6 RED / 0 GREEN（assertion 5 / exception 1）。** P5 是例外轉紅，與 reviewer 上一輪的觀察一致。
所有 probe 後上游檔案 `git diff --name-only` 為空。

### 5. Fixture

- 正例 **2**（`JIRA_ENTITY` / `DOCUMENT` 各一，即兩個 phase-1 來源的合法控制例），結果不變。
- 負例 **19 → 39**（新增 20 個值約束負例，含 DOCUMENT 來源的錯值負例 3 個）。
  既有 19 個結果與 frozen 版本逐一相同。

**mutation 形狀分類（修正上一輪的不精確措辭）：**

| 形狀 | 數量 |
| --- | --- |
| 單一 leaf mutation | 29 |
| 單一 subtree replacement（整個物件／陣列換掉，如 `source_policy = null`、`connector_grants = []`） | 7 |
| 多點 scenario（reviewer 的「兩邊都缺值」原樣重播，本質上就需要改 2~3 處） | 3 |

上一輪 handoff 寫「19 個全部恰好差一個 leaf」是不精確的，reviewer 指出正確。
本輪不為了湊 leaf 數而拆掉必要的物件型別測試。

### 6. Gate

```
ruby scripts/validate_*.rb                     → 21 PASS / 0 FAIL
validate_cc_cross_layer_contract.py            → PASS
validate_document_adapter_mapping_instances.py → PASS
validate_jira_adapter_mapping_instances.py     → PASS
validate_std_schema_engine.py                  → PASS
git diff --check                               → clean
```

validator **351 行**（`< 400` 硬上限）。

## 措辭修正（reviewer 指出的三處，全部照改）

1. 負例 mutation 形狀 → 已改為上表三分類。
2. parity 結果 → 已分列 assertion-RED 與 exception-RED，不寫「乾淨的契約拒絕」。
3. **adapter 綁定的保證範圍** → 本卡證明的是
   **「准入來源與 adapter 的來源標籤（`source_system`）一致」**，
   不是「這一筆完整 RawEvidence 已通過 adapter／STD instance 驗證」。
   正例只帶 projected identity 的兩個欄位，沒有完整 RawEvidence instance。
   交付包已同步改寫，不再把兩者混為一談。

## 本輪沒有做的事

- 沒有改共用 `present?`。
- 沒有新增 connector、URI resolver、registry、runtime。
- 沒有改任何上游契約或 schema。
- 沒有為了綠燈新增 role source 或放寬 phase 邊界
  （`CLIENT_FEEDBACK` / `CRM_ENTITY` / `CLIENT_DOCUMENT` 維持 fail-closed 拒絕，留上游 Owner 裁決）。
