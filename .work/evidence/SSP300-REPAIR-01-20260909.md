---
id: SSP300-REPAIR-01-20260909
type: evidence
card: .work/CARD-SSP300-REPAIR-01-20260909.md
review_line: SSP300-REVIEW-20260909
status: REPAIRED_AWAITING_TARGETED_REREVIEW
---

# SSP-300 Repair 01 證據

## Checkpoint

- Branch：`cc/ssp300-work-record-skill`。
- Repair commit：`f630164860f0d5124d4e6a4eefc6cf28deaa8176`。
- Parent：`cf15496d1030660ae29e578459f650aaa1b8bdaf`（原 evidence+handoff commit）。
- Tree：`b92729305c87a7f4574109ff26d00d60775a7b87`。
- 原 review 目標 `ce154ecd642c9e7198ef84925ea497297d5cb2f8` 維持 immutable。

## 變更（`cf15496..f630164`）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP300-REPAIR-01-20260909.md` | `3b4d065da22c3ccf38b925463b7a7c9facf6db7c` |
| M | `scripts/validate_ai_work_record_skill_contract.rb` | `2d178f748ef98a33151a1c82f1edbfa734459f38` |
| M | `規格/v0.1/ai-work-record-skill.yaml` | `bc5611f533b37a1aa42eef73fc93155458f9a244` |
| M | `規格/v0.1/fixtures/ai-work-record-skill-negative-fixtures.json` | `5af24da3d0488a8c85559d7edc2c67eaed02b287` |

`ai-work-record-skill-positive-fixtures.json` 未改（既有 2 個正例在新規則下仍通過）。

## Finding 處置

### SSP300-RV-001（P1）CLOSED
- 新 `replay_lifecycle(events, card_record_spec)`：讀 `ai-task-card-record.allowed_status_transitions`，逐步 replay；任一步 target 不在 `transitions[status]` → `["ILLEGAL_STATUS_TRANSITION", nil]`。`skill_transform_failure` 在 `SKILL_LIFECYCLE_MUST_START` 之後呼叫它，`replayed_status` 用 walk 結果（不再 `event_map.fetch(events.last)`）。
- 負例：`SKILL_NEG_ILLEGAL_LIFECYCLE_TRANSITION`（`[start, complete]`，OPEN→DONE）→ `ILLEGAL_STATUS_TRANSITION`。

### SSP300-RV-002（P1）CLOSED
- `draft_card_contract_valid?` 補 `return false if card["sensitive_content_copied"] == true`（SSP-299 parity）。
- 函式加 NOTE：這是 SSP-299 record 檢查的第二份實作，repo-wide Refactor 卡會抽共用 helper；在那之前必須逐項對齊 `task_card_record_failure`，唯一刻意省略是 SSP-299 的 lifecycle 一致性 —— 由本卡 RV-004 的 `DRAFT_CARD_STATUS_MISMATCH` 覆蓋。
- 負例：`SKILL_NEG_DRAFT_CARD_SENSITIVE_CONTENT` → `DRAFT_CARD_INVALID`。

### SSP300-RV-003（P1）CLOSED
- YAML `authority.human_acceptance_gate_position: PERSONAL_ACCEPTANCE`；`error_contract` 加 `ILLEGAL_STATUS_TRANSITION` / `DRY_RUN_NOT_BOOLEAN` / `DRAFT_CARD_STATUS_MISMATCH`。
- structural：`present?(authority["human_acceptance_gate_position"]) && boundary_gate_positions.include?(_)`（`boundary.automated_step_contract.allowed_gate_positions`）。
- 探針：移除該欄位 / 改成非 allowed 值 → RED。

### SSP300-RV-004（P2）CLOSED
- `skill_transform_failure`：`output.draft_card.status != output.replayed_status` → `DRAFT_CARD_STATUS_MISMATCH`（在 `REPLAYED_STATUS_MISMATCH` 之後）。
- 負例：`SKILL_NEG_DRAFT_CARD_STATUS_MISMATCH`（draft `IN_REVIEW` vs replay `OPEN`）→ `DRAFT_CARD_STATUS_MISMATCH`。

### SSP300-RV-005（P2）CLOSED
- `return "DRY_RUN_NOT_BOOLEAN" unless [true, false].include?(output["dry_run"])`（在 `EVIDENCE_INLINE_CONTENT` 之後）。
- 負例：`SKILL_NEG_DRY_RUN_NOT_BOOLEAN`（`"banana"`）→ `DRY_RUN_NOT_BOOLEAN`。

### SSP300-RV-006（P2）CLOSED
- `FAIL_SILENT`：`present?(output["error"]) && output["ok"] != false`（error 時必須明確 `ok: false` 才不算 fail-silent）。
- 既有 `SKILL_NEG_FAIL_SILENT` 改成 `{error: "boom"}`（無 `ok`）—— 仍 → `FAIL_SILENT`。

`EXPECTED_SKILL_NEGATIVE_LABELS` 與 yaml `required_negative_fixtures` 由 7 → 11。

## Gate 結果（在 `f630164`）

| Gate | 結果 |
|---|---|
| `ruby scripts/validate_ai_work_record_skill_contract.rb` | `PASS`（264 行 < 400） |
| `ruby scripts/validate_ai_task_card_record_contract.rb`（regression） | `PASS` |
| `ruby scripts/validate_ai_work_record_boundary_contract.rb`（regression） | `PASS` |
| `ruby scripts/validate_personal_memory_contract.rb`（regression） | `PASS` |
| STD schema engine / STD-00~03 / cross-layer | `PASS` |
| `git diff --check cf15496 f630164` | clean |
| JSON / YAML parse | OK |

## Mutation 探針（對 `f630164`，還原乾淨）

| 探針 | 結果 |
|---|---|
| 移除 illegal-transition replay | RED ✓ |
| 移除 draft-card sensitive-content 檢查 | RED ✓ |
| YAML 移除 `human_acceptance_gate_position` | RED ✓ |
| YAML gate position 改成非 allowed 值 | RED ✓ |
| 移除 `dry_run` bool 檢查 | RED ✓ |
| 移除 `draft_card.status` vs `replayed_status` 檢查 | RED ✓ |
| `FAIL_SILENT` 放寬回 `== true` | RED ✓ |

還原後 `git diff --stat` 為空。

## 待辦

- 定點複審交接卡：`.work/handoff/SSP300-REREVIEW-01-20260909.md`（範圍 `cf15496..f630164`）。
