---
id: SSP299-REPAIR-01-20260908
type: evidence
card: .work/CARD-SSP299-REPAIR-01-20260908.md
review_line: SSP299-REVIEW-20260908
status: REPAIRED_AWAITING_TARGETED_REREVIEW
---

# SSP-299 Repair 01 證據

## Checkpoint

- Branch：`cc/ssp299-task-card-format`。
- Repair commit：`a49b11f881bf0aa6ca7ce8f7a1c6f872481127f4`。
- Parent：`22a16057aea515f183447ee946910827cb8cc0cc`（原 evidence+handoff commit）。
- Tree：`58ba9296252b4a270566f22848aceaa715ac4074`。
- 原 review 目標 `86aaf8e79631580f486eb7a6508078cd2bbdc326` 維持 immutable。

## 變更（`22a1605..a49b11f`）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP299-REPAIR-01-20260908.md` | `f34d5ce6d707596c8e29a40ac6e4325435c24410` |
| M | `scripts/validate_ai_task_card_record_contract.rb` | `2e61a4c20973e0cb723eaa2b79205dc5ade36a90` |
| M | `規格/v0.1/fixtures/ai-task-card-record-negative-fixtures.json` | `7b8f0388e1834c3c99653381fa503521bbbdcdba` |
| M | `規格/v0.1/fixtures/ai-task-card-record-positive-fixtures.json` | `b2d8b2e2bd3aa788c41a7fd394a409e526ac5bb7` |

`規格/v0.1/ai-task-card-record.yaml` 不在本次 diff（`cross_reference.must_match.*_from` pointer 早已存在；本次只在 validator 鎖定它們）。

## Finding 處置

### SSP299-RV-001（P1）CLOSED
- `task_card_record_failure` 新增型別檢查：`card_id` 必須符合 `CARD_ID_URN`（`urn:omos:task-card:<uuid>`）；`evidence_refs` 每項必須符合 `OMOS_URN`（`urn:omos:<ns>:<...>` string）。不符 → `CARD_INVALID_FIELD_TYPE`（在 `CARD_MISSING_REQUIRED_FIELD` 之後、`CARD_INVALID_STATUS` 之前）。
- 負例：`TCR_NEG_INVALID_CARD_ID`（`card_id: 12345`）、`TCR_NEG_INVALID_EVIDENCE_REF`（`["not-a-urn", 123]`）→ `CARD_INVALID_FIELD_TYPE`。

### SSP299-RV-002（P2）CLOSED
- `reconstruction_failure(card, events, boundary_forbidden_fields)`：組合 `task_card_record_failure`（nil）＋ `lifecycle_replay_failure`（nil）＋ walk 出的 final status == `card["status"]`，否則 `CARD_STATUS_LIFECYCLE_MISMATCH`。
- fixtures：`integrated_reconstruction_cases`（`TCR_IR_POS_CONSISTENT`：card DONE + `[start, submit_review, complete]`）；`integrated_reconstruction_negative_cases`（`TCR_IR_NEG_STATUS_MISMATCH`：card OPEN 但 events replay 到 DONE）→ `CARD_STATUS_LIFECYCLE_MISMATCH`。

### SSP299-RV-003（P2）CLOSED
- structural：`cross_reference.must_match.{status_enum_from, required_fields_from, forbidden_fields_from, forbidden_authority_from}` 四個 pointer 字串鎖為 exact boundary path；boundary `task_card.forbidden_authority` 必須 `present?`。
- 定位：**pointer binding**，非 value-copy parity —— 本 slice 不複製 boundary 的 `forbidden_authority` 值，只鎖 pointer 指向與 boundary 欄位存在（validator 內註明）。

## Gate 結果（在 `a49b11f`）

| Gate | 結果 |
|---|---|
| `ruby scripts/validate_ai_task_card_record_contract.rb` | `PASS` |
| `ruby scripts/validate_ai_work_record_boundary_contract.rb`（regression） | `PASS` |
| `ruby scripts/validate_personal_memory_contract.rb`（regression） | `PASS` |
| STD schema engine | `PASS` |
| STD-00 / 01 / 02 / 03 | `PASS` × 4 |
| Cross-layer | `PASS`；8 negatives rejected |
| `git diff --check 22a1605 a49b11f` | clean |
| JSON / YAML parse | OK |

7 個原必備 record negative label + 新 label 全覆蓋。

## Mutation 探針（對 `a49b11f`，還原乾淨）

| 探針 | 結果 |
|---|---|
| 移除 `card_id` URN 檢查 | RED ✓ |
| 移除 `evidence_refs` URN 檢查 | RED ✓ |
| 移除 reconstruction status-vs-lifecycle 檢查 | RED ✓ |
| YAML `forbidden_authority_from` pointer 竄改 | RED ✓ |
| `TCR_NEG_INVALID_CARD_ID` 宣告錯誤 code | RED ✓ |
| `TCR_IR_NEG_STATUS_MISMATCH` 宣告錯誤 code | RED ✓ |

（pointer-tamper 探針第一次因 perl 縮排寫 2 空格、實際 4 空格誤顯 PASS，改正後確認 RED。）還原後 `git diff --stat` 為空。

## 待辦

- 定點複審交接卡：`.work/handoff/SSP299-REREVIEW-01-20260908.md`（範圍 `22a1605..a49b11f`）。
- 複審只驗 `SSP299-RV-001/002/003` 關閉與 regression。
