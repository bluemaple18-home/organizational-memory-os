---
id: SSP301-REPAIR-01-20260909
status: ACCEPTED_GO_20260909
type: implementation-repair
jira: SSP-301
lane: B
tier: T1
parent_card: .work/CARD-SSP301-AIWR-HOOK-20260909.md
review_line: .work/handoff/SSP301-REVIEW-20260909.md
frozen_review_sha: acafeb44e0cf4bf4d1c761c644d4b4ae5b292215
---

# Repair 01｜SSP-301 大 review NO_GO（2×P1 + 1×P2）

## 帶回的 findings

| id | severity | 內容 |
|---|---|---|
| F-01 | P1 | `hook_capture_failure` 的 `disabled` early-return 在 authority 檢查之前 → 停用狀態下 `emitted` 仍可夾帶 `personal_acceptance_ref` / `canonical_write_receipt_ref` 等 forbidden batch field，`HOOK_EXCEEDS_AUTHORITY` 不會執行 → authority gate 可繞過。 |
| F-02 | P1 | failure isolation 只擋 `host_task_impact == "BLOCKED"` 一個字串；`"FAILED"` / `"CANCELLED"` / `"MUTATED"` 等同樣代表宿主任務被改變的值會通過，違反 `failure_isolation.on_error = ISOLATE_FROM_HOST_TASK`「host task outcome is never changed」。 |
| F-03 | P2 | 同一 `event_key` 帶不同 `event`（例如一筆 `block`、一筆 `cancel`）時，`raw.uniq { event_key }` 無條件 first-wins，第二個矛盾 lifecycle 事件被靜默丟棄，可能遺失 `cancel` / `complete` 訊號。 |

## 定點修法（同 branch `cc/ssp301-aiwr-hook`，原 review SHA `acafeb44` immutable）

1. **F-01**：把 authority（`performs_memory_acceptance` / `writes_company_knowledge` /
   `EXPECTED_FORBIDDEN_BATCH_FIELDS`）與 `FAIL_SILENT` 兩組「不論停用與否都適用」的檢查
   **移到 `disabled` early-return 之前**。停用只豁免 dedupe / ordering / skill-input-shape，
   不豁免 authority 與 fail-loud。新增負例
   `HOOK_CAP_NEG_DISABLED_AUTHORITY_FIELD`（`disabled: true` + 空批次 + `canonical_write_receipt_ref`）
   → `HOOK_EXCEEDS_AUTHORITY`。
2. **F-02**：改為 fail-closed 狀態模型。新增 `EXPECTED_HOST_TASK_IMPACT_ALLOWED = [nil, "UNAFFECTED"]`；
   `return "HOOK_FAILURE_BLOCKS_HOST_TASK" unless EXPECTED_HOST_TASK_IMPACT_ALLOWED.include?(capture["host_task_impact"])`。
   yaml `failure_isolation` 補 `host_task_impact_allowed: [UNAFFECTED]` + `host_task_impact_rule`，
   validator 結構斷言鎖定。新增負例
   `HOOK_CAP_NEG_HOST_TASK_NOT_ISOLATED`（`host_task_impact: "FAILED"`）→ `HOOK_FAILURE_BLOCKS_HOST_TASK`。
3. **F-03**：dedupe 前先掃一遍 `event_key` → `event` 對映，同 key 不同 event 直接
   `return "HOOK_DUPLICATE_CONFLICT"`（fail closed）。yaml `error_contract` 補
   `HOOK_DUPLICATE_CONFLICT: hook.error.duplicate_conflict`。新增負例
   `HOOK_CAP_NEG_DUPLICATE_CONFLICT`（同 `event_key` 一筆 `block`、一筆 `cancel`）
   → `HOOK_DUPLICATE_CONFLICT`。
4. `required_negative_fixtures` 與 `EXPECTED_HOOK_NEGATIVE_LABELS` 各 +3（12 → 15）。

ordering / `occurred_at` 排序 reviewer 明確不另開 finding（契約以 de-duplicated sequence
做 transition，未宣告需按 `occurred_at` 排序），留 backlog。

## 驗證（定點：只驗原 finding + 行為 regression）

### 原 findings 消除

| finding | 負例 | 結果 |
|---|---|---|
| F-01 | `HOOK_CAP_NEG_DISABLED_AUTHORITY_FIELD` | `HOOK_EXCEEDS_AUTHORITY` |
| F-02 | `HOOK_CAP_NEG_HOST_TASK_NOT_ISOLATED`（`"FAILED"`）；既有 `HOOK_CAP_NEG_FAILURE_BLOCKS_HOST`（`"BLOCKED"`）仍 RED | `HOOK_FAILURE_BLOCKS_HOST_TASK` |
| F-03 | `HOOK_CAP_NEG_DUPLICATE_CONFLICT` | `HOOK_DUPLICATE_CONFLICT` |

### enforcement parity（移除 enforcement → gate 轉紅）

`authority forbidden batch field` / `host_task_impact fail-closed` / `event_key conflict` /
`dedupe idempotency`（regression） / `disabled still emitting`（regression）—— 逐一 neutralize →
`ruby scripts/validate_ai_work_record_hook_contract.rb` exit 1（RED）；全部還原後 `PASS`。

### 行為 regression

- 全 gate suite 綠：14 Ruby validator + `std_schema_engine`（coverage 12/12、16/16、9/9 不變）
  + cross-layer + `git diff --check`。
- 既有正例 3 筆（happy / dedupe / disabled-emits-nothing）仍 PASS；authority / fail-silent
  提前不影響（正例本就無這些欄位）。
- 上游 `ai-work-record-boundary` / `ai-task-card-record` / `ai-work-record-skill` 未改動。
- validator 301 行（< 400）。

## 檔案

- `scripts/validate_ai_work_record_hook_contract.rb`（evaluator 重排 + `HOST_TASK_IMPACT_ALLOWED` +
  `HOOK_DUPLICATE_CONFLICT` + 結構斷言 + 3 負 label）
- `規格/v0.1/ai-work-record-hook.yaml`（`host_task_impact_allowed` / `host_task_impact_rule` /
  `HOOK_DUPLICATE_CONFLICT` / +3 required_negative_fixtures）
- `規格/v0.1/fixtures/ai-work-record-hook-negative-fixtures.json`（+3 case）
- `.work/evidence/SSP301-AIWR-HOOK-20260909.md`（append repair 段）
- `.work/CARD-SSP301-AIWR-HOOK-20260909.md`（status note）

## 給定點複審

- review line 不變：`.work/handoff/SSP301-REVIEW-20260909.md`。原 frozen review SHA `acafeb44` 不動；
  repair 是同 branch 新 commit。
- 只需驗：(a) F-01/F-02/F-03 是否消除（對應 3 負例 + enforcement parity）；(b) 有無新 regression
  （其餘 validator、gate suite、上游契約、正例）。
- 無 P0/P1 → 寫 `.work/evidence/SSP301-REREVIEW-01-20260909.yaml` → Owner accept →
  merge `cc/ssp301-aiwr-hook` → `main` → Lane B → `SSP-302`。
