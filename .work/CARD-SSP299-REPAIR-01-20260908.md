---
id: SSP299-REPAIR-01-20260908
status: REPAIRED_AWAITING_TARGETED_REREVIEW
type: repair
jira: SSP-299
lane: B
tier: T1
review_line: SSP299-REVIEW-20260908
base_review_commit: 86aaf8e79631580f486eb7a6508078cd2bbdc326
---

# SSP-299 Repair 01

外部大 review `NO_GO`（P0=0 / P1=1 / P2=2）。沿同一 review line 定點修，`86aaf8e` 保持 immutable。

## Findings 與處置

### SSP299-RV-001（P1）— 宣告 URN 型別但 instance evaluator 沒驗
- 事實：`card_id` 只 `present?`，`evidence_refs` 只 `is_a?(Array)`；`card_id: 12345`、`evidence_refs: [123, false, {}]` 可能通過。field-level format 的主要功能沒 enforce。
- 修：新增 `CARD_INVALID_FIELD_TYPE`。`card_id` 必須 String + `urn:omos:task-card:<uuid>` pattern；`evidence_refs` 每項必須是 `urn:omos:<ns>:<...>` string。補 `TCR_NEG_INVALID_CARD_ID`、`TCR_NEG_INVALID_EVIDENCE_REF`。

### SSP299-RV-002（P2）— lifecycle final 與 card.status 無交叉驗證
- 事實：`task_card_record_cases` 與 `lifecycle_replay_cases` 平行，兩 evaluator 各自綠；`card.status = OPEN` + `events = [start, submit_review, complete]`（final DONE）可同時通過。
- 修：新增 `integrated_reconstruction_cases` / `_negative_cases`（同時帶 `card` + `lifecycle_events`），`reconstruction_failure` 組合：card 通過、lifecycle 通過、且 replay final status == `card.status`，否則 `CARD_STATUS_LIFECYCLE_MISMATCH`。

### SSP299-RV-003（P2）— Acceptance #7 宣稱 forbidden_authority cross-reference 但未驗
- 事實：validator 驗 `is_personal_memory` / `status_enum` / `required_fields`，`forbidden_fields` 只要求 boundary 非空；`forbidden_authority` 與 `cross_reference.must_match.*_from` pointer 皆未鎖。
- 修：structural assert 鎖住四個 `*_from` pointer 字串為 exact，並 assert boundary `task_card.forbidden_authority` 存在且非空。本 slice 不複製 authority 值 —— 明確定位為 pointer binding，evidence 中據實說明（非 value-copy parity）。

## Constraints
- 只改 `ai-task-card-record.yaml`、`validate_ai_task_card_record_contract.rb`、`ai-task-card-record-*-fixtures.json` 與 backlog；不動 `ai-work-record-boundary.yaml`、既有 STD / EMEM 契約。
- validator 仍薄判斷；`allowed_status_transitions` / `lifecycle_event_to_status` 仍是資料表。不新增 package dependency。推同 branch。

## Acceptance
1. `card_id` 非 `urn:omos:task-card:<uuid>` string、或 `evidence_refs` 任一項非 URN string → `CARD_INVALID_FIELD_TYPE`；正例仍通過。
2. `integrated_reconstruction`：card + events 一致 → allow；replay final ≠ `card.status` → `CARD_STATUS_LIFECYCLE_MISMATCH`。
3. structural：`cross_reference.must_match.{status_enum_from, required_fields_from, forbidden_fields_from, forbidden_authority_from}` 四個 pointer 字串 exact；boundary `task_card.forbidden_authority` 非空。
4. 每個新 code 有 mutation 探針或負例證明 fail-closed；7 個原 record negative label + 新 label 全覆蓋。
5. `ruby scripts/validate_ai_task_card_record_contract.rb`、`ruby scripts/validate_ai_work_record_boundary_contract.rb`（regression）、personal-memory、STD schema engine、STD-00~03、cross-layer、JSON/YAML parse、`git diff --check` 全 PASS。

## Likely files
- `規格/v0.1/ai-task-card-record.yaml`
- `scripts/validate_ai_task_card_record_contract.rb`
- `規格/v0.1/fixtures/ai-task-card-record-positive-fixtures.json`
- `規格/v0.1/fixtures/ai-task-card-record-negative-fixtures.json`
- `.work/evidence/SSP299-REPAIR-01-20260908.md`

## Evidence
`.work/evidence/SSP299-REPAIR-01-20260908.md`
