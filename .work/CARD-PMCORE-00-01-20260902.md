# PMCORE-00／01｜員工個人記憶契約封版

- Objective：修復 Personal Memory machine-readable 規格，統一版本／Backlog ID，並完成 Ownership／Privacy 兩軸契約。
- Scope：`規格/v0.1/personal-harness-integration.yaml`、對應主文件／Backlog、最小 validator 與 fixtures；保留既有 untracked `.work` 與 HTML。
- Constraints：不做 Connector、DB、Hook、Loop、Harness、Hermes；不改 AI Core；不 push／deploy；文件與說明使用繁中。
- Acceptance：YAML 可解析；Markdown／YAML 的 EMEM-00～08 一致；ownership_mode 與 visibility_scope 分軸；actor-action policy、retention／offboarding／export／delete／legal-hold／promotion widening 可表達；正負 fixtures 與 `git diff --check` 通過。
- Status：COMPLETE／GO；Implementation worker：GPT-5.5 high；P1 repair：Terra medium；Final read-only re-review：兩項 P1 CLOSED／GO；Mainline：已重跑驗證並完成最終驗收；Evidence：`.work/evidence/PMCORE-00-01-20260902.md`。

## 驗證計畫

1. 修復並統一規格 → 以 YAML parser 與 registry consistency check 驗證。
2. 補 Ownership／Privacy contract → 以 synthetic positive／negative fixtures 驗證允許與拒絕路徑。
3. 檢查變更範圍 → 執行受影響測試、`git diff --check`、檢閱 staged／unstaged diff。

## Do not touch

- `.DS_Store`
- 既有 `.work/CARD-KM-*`、`.work/CARD-SSP285-*`
- `km-mvp-engineering-tunnel.html`
- 遠端 Git、部署與外部服務
