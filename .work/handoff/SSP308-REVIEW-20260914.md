# SSP-308 AIWR-10 Claude Code Native Adapter — Big Review 交付包

平台中立。以下都可從 branch 直接重現。

## 1. 審什麼

把 Claude Code 公開文件記載的 hook 事件名稱分類成既有 Hook 契約接受的
`lifecycle_events` 項目，或明確的非生命週期事件。**這是 SSP-307（Codex
Native Adapter，`ACCEPTED_GO @ 32b3f82`）的手足卡**，同一套治理形狀。

## 2. Frozen commit

```
base             9a6fd1c
review_commit    90bc32d881abb5a3fae9490a6cfffd2f283c5729
branch           cc/ssp308-claude-code-native-adapter（已 push origin）
```

```
git fetch origin && git checkout cc/ssp308-claude-code-native-adapter
git diff 9a6fd1c..90bc32d --stat
```

## 3. 範圍縮減：本輪沒有 runtime probe（請先看這段）

SSP-307 需要全量掃描 818 個真實 Codex session，因為 Codex 原生事件集合
沒有公開規格。**Claude Code 的 hook 事件不同**：9 個名稱是公開文件裡的
封閉集合，完整性可以對照文件驗證，不需要即時掃描——這是不同的驗證方式，
不是較弱的方式，因為要分類的是文件化且穩定的**名稱**，不是未知的行為。

這個決策由 Owner 透過 `AskUserQuestion` 明確選定（見契約
`scope_reduction_from_ssp307` 區塊），不是我自己抄捷徑。真實 payload
形狀（例如 `SessionEnd.reason` 的實際值）留給
`.work/CARD-SSP308-RUNTIME-PROBE-BACKLOG-20260914.md`。

我先讀過另一條路：本工具自己的 session transcript
（`~/.claude/projects/**/*.jsonl`，Owner 授權後只讀頂層 `type`，9 個檔）——
發現那是 UI 訊息粒度（`assistant`/`user`/`system`/...），沒有乾淨的
「start」訊號，會重演 SSP-307 F-01 的設計缺陷。這是我放棄那條路、改用
公開 hook 文件的原因，寫在 evidence 裡。

## 4. 設計：SSP-307 三個 finding 的教訓，一開始就避開

| SSP-307 finding | 本卡怎麼避開 |
| --- | --- |
| F-01：`complete` 產生 `OPEN→DONE` 非法邊 | `Stop → submit_review`，不是 `complete` |
| F-02：`turn_aborted → cancel` 錯殺可恢復事件 | `SessionEnd` 留在 non-lifecycle，不猜終態 |
| F-03：`error_contract` 手寫清單互比 | 從第一版就綁 `scripts/lib/loop_return_contract.rb`（SSP-302 建立） |

F-04（單向相等檢查）在本卡沒有對應場景——沒有第二份 runtime-sample evidence
需要互相綁定；`documented_native_vocabulary.hook_events` 與 `classified`
的比對是 `sorted_set(...) == sorted_set(...)`，天生雙向。

**請直接挑戰這三個設計選擇**，特別是 `Stop → submit_review`——如果你認為
還有別的合法邊更合適，這是可討論的點。

## 5. Mutation 要求

**A. 兩個 evaluator 的 guard parity**（中和條件成永不觸發，保留 code 字面量）：

| evaluator | 我的結果 |
| --- | --- |
| `claude_code_mapping_failure`（15 條） | 15 RED / 0 GREEN（assertion 14 / exception 1） |
| `claude_code_rollback_failure`（6 條） | 6 RED / 0 GREEN（assertion 5 / exception 1） |

**第一輪 probe 就全紅，沒有任何 GREEN(bad)** —— fixture 覆蓋是照著 SSP-307
guard parity 最終版本的模式先寫好的，不是先寫最小 fixture 再被 probe 逼補。

**B. 結構／跨契約 probe（5 條）**

| # | 動作 | 我的結果 |
| --- | --- | --- |
| P1 | `lifecycle_event_map` 指向不存在的 lifecycle key | RED |
| P2 | `error_contract` 改名一個 code、evaluator 未同步 | RED |
| P3 | `non_lifecycle_event_types` 與 `lifecycle_event_map` 重疊 | RED |
| P4 | 上游 `lifecycle_event_to_status` 移除 `submit_review` | RED |
| P5 | `hook_events` 文件清單多一個未分類的事件 | RED |

還原請用 `cp` 備份，不要用 `git checkout --`。

**C. 上游未被改動**

```
git diff 9a6fd1c..90bc32d --name-only
```

應**不包含** `規格/v0.1/ai-task-card-record.yaml`、
`規格/v0.1/ai-work-record-hook.yaml`、`規格/v0.1/codex-native-adapter.yaml`。

## 6. 邊界

本卡**不做**：hook 實際註冊、connector、runtime probe（延後）、AIWR
`complete`／acceptance／canonical writer authority、重定義任何上游契約、
registry／FSM／DB。

## 7. 已跑的 gate

```
ruby scripts/validate_*.rb                     → 23 PASS / 0 FAIL
validate_cc_cross_layer_contract.py            → PASS
validate_document_adapter_mapping_instances.py → PASS
validate_jira_adapter_mapping_instances.py     → PASS
validate_std_schema_engine.py                  → PASS
git diff --check                               → clean
```

檔案大小：validator 263 行、契約 224 行（< 400 硬上限）。

## 8. 已知取捨

1. 本輪沒有 runtime probe（見 §3）。
2. `SessionEnd.reason` 分布完全未知，`SessionEnd` 是否該拆更細的
   native_event_type 留給延後卡。
3. `loop_return_contract` 模組現在有三個消費者（`ai-work-record-loop`／
   `codex-native-adapter`／本卡），名稱仍承襲最初命名，是累積技術債，
   本卡未處理（跨三條 review line，需要一次單獨的 housekeeping 卡）。

請以 `P0 / P1 / P2 / P3` 分級給 `GO` 或 `NO_GO`。
