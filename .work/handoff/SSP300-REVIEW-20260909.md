---
id: SSP300-REVIEW-20260909
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP300-WORK-RECORD-SKILL-20260909.md
evidence: .work/evidence/SSP300-WORK-RECORD-SKILL-20260909.md
---

# SSP-300 大 review 交接卡

AIWR-03 工作紀錄 Skill 契約。Branch 已 push origin；審查 diff 用 `git fetch origin && git diff 1ea01cf..ce154ec`。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`1ea01cfb1af0668cf0f9c3f7c935030bec2dd22c`（`cc/ssp299-task-card-format` accepted tip）
- Review commit：`ce154ecd642c9e7198ef84925ea497297d5cb2f8`
- Review tree：`952f1a638372b67e701851476ae04cc858d5450d`
- 唯一審查 diff：`1ea01cf..ce154ec`
- Branch：`cc/ssp300-work-record-skill`

```sh
git fetch origin
git show -s --format='%H %P %T' ce154ecd642c9e7198ef84925ea497297d5cb2f8
git diff --name-status 1ea01cfb1af0668cf0f9c3f7c935030bec2dd22c ce154ecd642c9e7198ef84925ea497297d5cb2f8
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（5 路徑，全新增）

| 狀態 | 路徑 | review blob OID |
|---|---|---|
| A | `規格/v0.1/ai-work-record-skill.yaml` | `206e1faa19a6fbf379b5e7f12d0de2c11271c6f6` |
| A | `scripts/validate_ai_work_record_skill_contract.rb` | `8eb598cab71033010d3188598a71332b23056917` |
| A | `規格/v0.1/fixtures/ai-work-record-skill-positive-fixtures.json` | `f3abde1769d1303faa267b75e1d4d796a9bf484a` |
| A | `規格/v0.1/fixtures/ai-work-record-skill-negative-fixtures.json` | `c479c3ed48bcf474bca565f57db80b50f7365800` |
| A | `.work/CARD-SSP300-WORK-RECORD-SKILL-20260909.md` | `696da5ea8633ea3621b77a1047535ac688123f88` |

## 3. 明確排除

- `SSP-298` 的 `ai-work-record-boundary.yaml` 與 `SSP-299` 的 `ai-task-card-record.yaml`（本卡只讀 + 交叉驗證，未改）、既有所有 validator / schema / fixture。
- Hook（`SSP-301`）、Loop（`SSP-302`）、Harness（`SSP-303`）、Hermes（`SSP-304`）、主管視圖（`SSP-305`）。
- `SSP-290`～`SSP-293` / EPM、STD。全 repo re-scan。

## 4. Normative authority 與可重跑命令

- `ai-work-record-skill.yaml` 是本 slice 的 authority；boundary 與 card-record spec 仍是各自 semantics 的唯一來源，本卡以 `cross_reference` pointer binding 綁定。
- validator 為薄判斷：structural + `skill_transform_failure`（純函式）。`draft_card_contract_valid?` 資料驅動讀 `ai-task-card-record` spec 的 `fields` / `status_enum`，不 require 跨檔。

```sh
export UV_CACHE_DIR=/tmp/ssp300-review-uv
ruby scripts/validate_ai_work_record_skill_contract.rb
ruby scripts/validate_ai_task_card_record_contract.rb
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 1ea01cf ce154ec
```

Mainline 實測全 PASS（見 evidence）。不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement | 證明 fixture（→ expected code） |
|---|---|---|
| input 必含 lifecycle_events + seed_fields（objective/scope/constraints/acceptance） | `skill_transform_failure` 首段；structural `input_contract` 清單 exact | `SKILL_NEG_MISSING_SEED_FIELD` → `MISSING_INPUT_FIELD` |
| lifecycle event 必須是 AIWR-02 map 的 key，首個為 `start` | `event_map.key?` 檢查 + `events.first != "start"` | `SKILL_NEG_UNKNOWN_LIFECYCLE_EVENT` → `UNKNOWN_LIFECYCLE_EVENT`；`SKILL_LIFECYCLE_MUST_START` 由分支覆蓋 |
| output draft_card 必須 contract-valid（AIWR-02 卡片格式） | `draft_card_contract_valid?`（讀 card-record spec 的 fields/status_enum + card_id URN + evidence URN array + non-empty + boundary forbidden + DONE 需 evidence） | `SKILL_NEG_DRAFT_CARD_INVALID`（draft_card 缺 objective）→ `DRAFT_CARD_INVALID` |
| evidence_refs reference only，不得內嵌內容 | `evidence.all? { urn?(ref, OMOS_URN) }` | `SKILL_NEG_EVIDENCE_INLINE_CONTENT`（含物件項）→ `EVIDENCE_INLINE_CONTENT` |
| Skill 不接受 Memory、不寫 Company Knowledge | `output["writes_company_knowledge"] == true` 或 output/draft_card 帶 forbidden_output_field → `SKILL_EXCEEDS_AUTHORITY`；structural `authority.accepts_memory/writes_company_knowledge == false`、`forbidden_output_fields` exact-set | `SKILL_NEG_OUTPUT_CARRIES_MEMORY_ACCEPTANCE`（`personal_acceptance_ref`）→ `SKILL_EXCEEDS_AUTHORITY` |
| replayed_status 必須等於 events 的 replay final | `output["replayed_status"] != event_map[events.last]` → `REPLAYED_STATUS_MISMATCH` | `SKILL_NEG_REPLAYED_STATUS_MISMATCH`（events → DONE、宣告 OPEN）→ `REPLAYED_STATUS_MISMATCH` |
| FAIL_LOUD：error 時 output 不得宣稱成功 | `present?(output["error"]) && output["ok"] == true` → `FAIL_SILENT`；structural `authority.error_behavior == FAIL_LOUD` | `SKILL_NEG_FAIL_SILENT` → `FAIL_SILENT` |
| runtime independence | structural `runtime_independence == true`；evaluator 純函式、fixture 無 runtime 欄位 | `SKILL_POS_*`（無 runtime 欄位仍通過） |
| cross-reference pointer binding | structural：三個 `must_match.*_from` pointer exact + 引用目標存在非空 | 探針：改 pointer 字串 → RED |
| 負例覆蓋 7 個必備 label | `EXPECTED_SKILL_NEGATIVE_LABELS - covered` 必須為空；每個 `actual == expected_failure_code` | 7 個 `covers_skill_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| `draft_card_contract_valid?` 是 `SSP-299` 邏輯的第二份實作（drift 風險） | 資料驅動讀 card-record spec 的 `fields` / `status_enum`，但 `card_id` URN pattern / DONE-evidence / non-empty 規則是本檔重寫 | 逐項比對本檔的 draft-card 檢查與 `validate_ai_task_card_record_contract.rb` 的 `task_card_record_failure`；找出兩者不一致之處（例如 SSP-299 有的 `CARD_STATUS_LIFECYCLE_MISMATCH` 這裡沒有）—— 判斷是否 P2（本卡只驗「合規草稿」不驗 lifecycle 一致，那由 SSP-299 的 integrated check 負責；但 Skill output 的 `replayed_status` 已另外驗）|
| Precedence | 見 evidence：MISSING → UNKNOWN_EVENT → MUST_START → MISSING(output) → EXCEEDS_AUTHORITY → DRAFT_CARD_INVALID → EVIDENCE_INLINE → REPLAYED_MISMATCH → FAIL_SILENT | 讓一個 case 同時 draft_card 無效且帶 `personal_acceptance_ref` → 應回 `SKILL_EXCEEDS_AUTHORITY`（較前）；同時 replayed mismatch 且 fail-silent → `REPLAYED_STATUS_MISMATCH` |
| `FAIL_SILENT` 訊號模型 | `output["error"]` present 且 `output["ok"] == true` | 攻擊：`ok` 用字串 `"true"`、`error` 為空字串、`error` present 但無 `ok` key —— 判斷模型是否夠嚴（可能 P3） |
| draft_card 的 `status` 與 `replayed_status` 未交叉驗 | 本卡分開驗 draft_card 合規與 `output.replayed_status` == events final；未 assert `draft_card.status == replayed_status` | 判斷：draft_card.status DONE 但 events `[start]`（final OPEN）且 output.replayed_status OPEN —— 目前 allow。是否 P2？（可補一條 `DRAFT_CARD_STATUS_MISMATCH`）|
| structural exact vs sorted | `output_contract.required_fields` / `seed_field_keys` 用 `==`；`forbidden_output_fields` / `input_contract.required_fields` 用 `sorted_set` | 各 list 改順序 / 增刪 / 改字，確認 `==` 全轉紅、`sorted_set` 只在內容變時轉紅 |
| 與 SSP-298 / SSP-299 no-regression | 新檔，未改既有 validator | 跑三個 AIWR validator，確認 boundary 7 label + task-card-record 7 label 未受影響 |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新 fixture / yaml 放 duplicate key，確認 fail-closed |

## 7. Mainline mutation 探針

見 evidence：7 項竄改（`accepts_memory`、`forbidden_output_fields`、`runtime_independence`、evaluator 移除 replayed-mismatch / evidence-inline / fail-silent 三分支、負例錯 code）全部使 validator RED，還原後 diff 空。

## 8. 結構化輸出契約

```yaml
review_input:
  base_sha: 1ea01cfb1af0668cf0f9c3f7c935030bec2dd22c
  review_sha: ce154ecd642c9e7198ef84925ea497297d5cb2f8
  tree_sha: 952f1a638372b67e701851476ae04cc858d5450d
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP300-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    ai_work_record_skill: PASS|FAIL|NOT_RUN
    ai_task_card_record: PASS|FAIL|NOT_RUN
    ai_work_record_boundary: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = Skill 可寫 memory acceptance / company knowledge、evidence 可內嵌內容、fail-silent 可通過；P1 = 主要 I/O 契約可被繞過、draft_card 檢查與 SSP-299 分歧到可放行無效卡、負例 false coverage、SSP-298/299 regress；P2 = 可控邊界或覆蓋缺口（例：draft_card.status 與 replayed_status 未交叉驗、FAIL_SILENT 訊號模型偏鬆）；P3 = 非阻塞。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane B 後續（`SSP-301` 起）。
- P2 / P3 → 記 backlog；若能寫 memory / 內嵌 evidence / fail-silent 通過，升 P1。
- 無 P0 / P1 且 binding / structural / fixture parity / mutation / zero-mutation 均 PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane B 開 `SSP-301`。
- 出 P1 → 沿同一 review line 定點複審，新 repair commit 另附 SHA / parent / tree / blob；`ce154ec` 維持 immutable。
