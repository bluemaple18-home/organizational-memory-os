---
id: SSP292-REVIEW-20260908
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP292-RECALL-CONTEXT-PACK-20260908.md
evidence: .work/evidence/SSP292-RECALL-CONTEXT-PACK-20260908.md
---

# SSP-292 大 review 交接卡

EMEM-03 Recall / MemoryContextPack 契約。Branch 已 push origin；審查 diff 用 `git fetch origin && git diff 5b7b883..6bdf56f` 產生。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`5b7b88374966a64ca4bdcda8682bc016c76f938a`（`cc/ssp290-personal-knowledge-levels` accepted tip）
- Review commit：`6bdf56f982955e0badceb8e9b342faa987e207af`
- Review tree：`4c33c63128e7134411078fac6ba957250ee2b4d7`
- 唯一審查 diff：`5b7b883..6bdf56f`
- Branch：`cc/ssp292-recall-context-pack`

```sh
git fetch origin
git show -s --format='%H %P %T' 6bdf56f982955e0badceb8e9b342faa987e207af
git diff --name-status 5b7b88374966a64ca4bdcda8682bc016c76f938a 6bdf56f982955e0badceb8e9b342faa987e207af
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（5 路徑）

| 狀態 | 路徑 | review blob OID |
|---|---|---|
| M | `規格/v0.1/personal-harness-integration.yaml` | `441b7e9d88c18d41ef441e77777a5e463057c24e` |
| M | `scripts/validate_personal_memory_contract.rb` | `ef75ae6ad40ccac091c6229a1f5e0cfb43766234` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `41d65eff9301ab1ad4fb70c42e6a0b3ef1396e21` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `fafbf7ae15cd9fe609b6c9c06437ca56f3291dac` |
| A | `.work/CARD-SSP292-RECALL-CONTEXT-PACK-20260908.md` | `79a56c95355e9c876c8bd401443fe6d556d623ad` |

以 `git diff-tree --no-commit-id --raw -r --abbrev=40 5b7b883 6bdf56f` 重算。

## 3. 明確排除

- SSP-290 已通過的 `capability_matrix` / `level_transitions` / `capability_safety_floor` 及其 fixtures（本 diff 未改；只驗未 regress）。
- `recall_context_pack.input` / `output` / `permission_strategy` / `unrestricted_global_personal_memory_search`（未改）。
- EMEM-00 / 01 已鎖契約、`SSP-298` / AIWR、STD 契約。
- 實際 retrieval / 向量 / rerank / context-budget 演算法 / LLM 呼叫（本卡明確不做）。
- `文件/待辦重整.md`、backlog 狀態。

## 4. Normative authority 與可重跑命令

- `recall_context_pack.contract` 是本 slice 的 authority；`MemoryContextPack.required_fields_ref` 指回它。
- `context_pack_failure` 是薄 evaluator：只驗 request/pack shape、permission ordering、gap notice、跨 owner intersection、以及 forbidden 的 search-all-then-prompt-guard 路徑。fixture 的 `request` / `pack` 為自足物件，不依賴 record registry。

```sh
export UV_CACHE_DIR=/tmp/ssp292-review-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 5b7b883 6bdf56f
```

Mainline 實測全 PASS（見 evidence）。不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement | 證明 fixture（→ expected code） |
|---|---|---|
| request 必含 6 欄位，缺任一 fail closed | `context_pack_failure` 首個分支；structural `contract.request_required_fields` exact 順序 | `CTXPACK_NEG_REQUEST_MISSING_BUDGET` → `RECALL_REQUEST_MISSING_FIELD` |
| pack 必含 12 欄位 | 第二分支 `!pack.key?(field)`；structural `pack_required_fields` exact 順序 | `CONTEXT_PACK_MISSING_FIELD` 由 evaluator 分支覆蓋 |
| 不存在 search-all-then-prompt-guard 路徑 | `pack["search_strategy"] == "GLOBAL_THEN_PROMPT_GUARD"` → 拒；structural `forbidden` exact-set 含 `SEARCH_ALL_THEN_PROMPT_GUARD` | `CTXPACK_NEG_SEARCH_ALL_THEN_PROMPT_GUARD` → `SEARCH_ALL_THEN_PROMPT_GUARD` |
| permission decision 先於 candidate set | `pack["permission_decision_before_selection"] != true` → 拒；structural `permission_ordering.decision_before_candidate_set == true` | `PERMISSION_DECISION_AFTER_SELECTION` 由 evaluator 分支覆蓋；探針 flip structural |
| decision scope 必須涵蓋 selected | `(selected_refs - covered_refs).empty?`；structural `decision_scope_covers_selected == true` | `CTXPACK_NEG_DECISION_SCOPE_TOO_NARROW` → `PERMISSION_DECISION_SCOPE_TOO_NARROW` |
| 跨 owner selection 需 permission_intersection | 逐一比對 `memory.owner_ref != request.requester_identity` → 需 `permission_intersection_refs` 有對應 `memory_ref` | `CTXPACK_NEG_CROSS_OWNER_WITHOUT_INTERSECTION` → `CROSS_OWNER_WITHOUT_INTERSECTION`；`CTXPACK_POS_STALE_BUDGET_CROSS_OWNER_ALL_NOTICED`（帶 intersection 通過） |
| stale 記憶必須標 freshness + gap notice | selected 有 `freshness == "STALE"` 時要求 `pack.freshness.stale_present == true` 且 `omitted_or_gap_notice` 含 `reason: STALE_PRESENT` | `CTXPACK_NEG_STALE_WITHOUT_FRESHNESS` → `STALE_WITHOUT_FRESHNESS`；正例 stale case 通過 |
| budget-omitted 必須有 gap notice | `budget_usage.omitted_count > 0` → `omitted_or_gap_notice` 含 `reason: BUDGET_OMITTED` | `CTXPACK_NEG_BUDGET_OMITTED_WITHOUT_NOTICE` → `BUDGET_OMITTED_WITHOUT_NOTICE`（notice 只有 `NOT_APPLICABLE_FILTERED`） |
| gap notice 理由來自鎖定 vocabulary | structural `gap_notice_reasons` exact-set | — |
| 負例覆蓋 6 個必備 label | `EXPECTED_RECALL_NEGATIVE_LABELS - covered` 必須為空；每個 `actual == expected_failure_code` | 6 個 `covers_recall_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| Evaluator 是「宣告一致性」檢查、不是真 retrieval | `context_pack_failure` 只讀 fixture 內宣告的 `selected_memories` / `covered_refs` / `intersection_refs` | 攻擊：`covered_refs` 放一個 selected 沒有的 ref（多而非少）、`selected_memories` 空但 `omitted_or_gap_notice` 亂填、`permission_intersection_refs` 指向不在 selected 的 memory_ref —— 確認判別合理且不誤放 |
| Precedence | 順序見 evidence | 讓一個 pack 同時 scope-too-narrow 且 cross-owner-missing-intersection，確認回 `PERMISSION_DECISION_SCOPE_TOO_NARROW`（先），且 fixture 宣告與之一致；反向組合驗 stale vs budget |
| `present?` 語義 | request 欄位用 `present?`（nil / "" / 空集合視為缺）；pack 欄位用 `key?` | request 給 `context_budget: 0`（present? 對 0 為 true，數字 0 是合法 budget 嗎？）、`intent: ""`；pack 給 `selected_memories: null` vs `[]` |
| structural exact-order vs exact-set | `request_required_fields` / `pack_required_fields` 用 `==`（順序敏感）；`gap_notice_reasons` / `forbidden` 用 `sorted_set` | 各 list 改順序 / 增刪 / 改字，確認 `==` 全轉紅、`sorted_set` 只在內容變時轉紅 |
| 與 SSP-290 no-regression | 同一 validator 檔；本 diff 只加 recall 區塊 | 確認 `capability_*` structural 斷言與 6 個 capability negative label 覆蓋未被破壞；跑完整 validator |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新增 fixture / yaml 區塊放 duplicate key，確認 fail-closed |
| `MemoryContextPack.required_fields_ref` 只是字串 | structural 斷言字串等於 `recall_context_pack.contract.pack_required_fields` 路徑 | 這是 pointer 不是解析；確認若要真綁定需 reviewer 提 finding（可能 P3） |

## 7. Mainline mutation 探針

見 evidence：6 項竄改（forbidden 移除、`decision_before_candidate_set` 翻轉、request 欄位移除、evaluator 移除 scope-covers / cross-owner、負例錯 code）全部使 validator RED，還原後 diff 空。

## 8. 結構化輸出契約

```yaml
review_input:
  base_sha: 5b7b88374966a64ca4bdcda8682bc016c76f938a
  review_sha: 6bdf56f982955e0badceb8e9b342faa987e207af
  tree_sha: 4c33c63128e7134411078fac6ba957250ee2b4d7
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP292-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    personal_memory_contract: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    cross_layer: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = permission-before-retrieval 可完全繞過 / 跨 owner 記憶可無 intersection 進 pack；P1 = 主要 recall 契約可被繞過、gap notice 可被略過、負例 false coverage、SSP-290 regress；P2 = 可控邊界或覆蓋缺口；P3 = 非阻塞（例如 `required_fields_ref` 只是字串 pointer）。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane A 後續（`SSP-293` 起）。
- P2 / P3 → 記 backlog；若能繞過 permission ordering 或 gap notice，升 P1。
- 無 P0 / P1 且 binding / structural / fixture parity / mutation / zero-mutation 均 PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane A 開 `SSP-293`。
- 出 P1 → 沿同一 review line 定點複審，新 repair commit 另附 SHA / parent / tree / blob；`6bdf56f` 維持 immutable。
