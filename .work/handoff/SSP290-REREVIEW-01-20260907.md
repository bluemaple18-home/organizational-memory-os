---
id: SSP290-REREVIEW-01-20260907
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP290-REVIEW-20260907
original_review_commit: a93313d18884b0dd73b33632e813d18b7f255b7b
repair_card: .work/CARD-SSP290-REPAIR-01-20260907.md
evidence: .work/evidence/SSP290-REPAIR-01-20260907.md
---

# SSP-290 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP290-RV-001/002/003` 是否關閉，以及是否對 SSP-290 契約造成 regression。**不重開全 review。** 原 review 目標 `a93313d` 維持 immutable。

Branch 已 push origin；審查 diff 用 `git fetch origin && git diff d5b7c54..73313c8` 產生（無附 companion 檔）。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base（原 review line tip）：`d5b7c54fc87a78b1b7cba93faa4b8216239c7564`
- Repair commit：`73313c8a2da731a49dc0120a1dce479dcea601af`
- Expected parent：`d5b7c54fc87a78b1b7cba93faa4b8216239c7564`
- Repair tree：`cf4113f4e7394cf1e31b94ba67209c79d7e93888`
- 唯一審查 diff：`d5b7c54..73313c8`
- Branch：`cc/ssp290-personal-knowledge-levels`（已 push origin）

```sh
git fetch origin
git show -s --format='%H %P %T' 73313c8a2da731a49dc0120a1dce479dcea601af
git diff --name-status d5b7c54fc87a78b1b7cba93faa4b8216239c7564 73313c8a2da731a49dc0120a1dce479dcea601af
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（4 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP290-REPAIR-01-20260907.md` | `86a699833d7d5785fd7b916541ff4f4d1832bf01` |
| M | `scripts/validate_personal_memory_contract.rb` | `cf424dd70da66404fb6bf803a0e4a7ef2e43a537` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `23fe2f9e411223052f8e1c10aef3865d9b38e01e` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `8763424a467701eb3075c350678cecd17b7669d7` |

`規格/v0.1/personal-harness-integration.yaml` 不在本次 diff（YAML 契約未改；被鎖的欄位早已存在）。

## 3. 明確排除

- 原 review 已通過、非本次 findings 的部分（`capability_matrix.grants`、`capability_safety_floor` 既有鎖定值、EMEM-00/01）。
- `SSP-298` / AIWR。
- 全 repo re-scan。

## 4. 可重跑命令（在 `73313c8`）

```sh
export UV_CACHE_DIR=/tmp/ssp290-rereview-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check d5b7c54 73313c8
```

## 5. Finding-by-finding acceptance

### SSP290-RV-001（P1）— 欄位名一致

- `level_transition_failure` 現讀 `transition["authorized_actor"]`（原 `actor_authorized`）。
- 全部 transition fixtures（4 正 + 5 負）用 `authorized_actor`。
- 驗：evaluator 讀的欄位 == `transition_requirements` 宣告；不存在第二套未宣告欄位；把 evaluator 改回舊名（fixtures 不變）→ 正例轉 RED。

### SSP290-RV-002（P2）— branch RED fixture

- 4 個新負例：`TR_NEG_UNAUTHORIZED_ACTOR` → `LEVEL_TRANSITION_UNAUTHORIZED_ACTOR`；`TR_NEG_REASON_NOT_RECORDED` → `LEVEL_TRANSITION_REASON_NOT_RECORDED`；`TR_NEG_FORKS_CORE_CONTRACT` → `LEVEL_TRANSITION_FORKS_CORE_CONTRACT`；`CAP_NEG_SAFETY_FLOOR_INVARIANT_MISSING` → `SAFETY_FLOOR_INVARIANT_MISSING`。
- 驗：每個負例單一 mutation，`actual == expected_failure_code`；刪掉對應 evaluator branch → 該負例轉 RED（不再被無關 code 代替）。
- 驗：這 4 個負例的 `expected_failure_code` 與 evaluator 分支形成雙向 parity —— 讓某負例同時可觸發兩個 code（例如 `authorized_actor:false` 且 `reason_recorded:false`），確認 evaluator precedence（`UNAUTHORIZED_ACTOR` 先於 `REASON_NOT_RECORDED`）與 fixture 宣告一致。

### SSP290-RV-003（P2）— YAML policy 鎖定

- 新 structural 斷言：`level_transitions.downgrade_rules.reduces_automation_depth_only == true`；`level_transitions.forbidden` exact-set == `[multi_step_jump, unauthorized_actor, downgrade_that_removes_safety_floor, transition_that_forks_core_contract]`。
- 驗：把 `reduces_automation_depth_only` 改 `false`、或增/刪/改任一 `forbidden` 值 → structural gate 轉 RED。

## 6. Regression acceptance

- `personal-memory` validator PASS；6 個原必備 capability negative label 仍全覆蓋。
- STD schema engine PASS；STD01 12/12、STD02 16/16、STD03 9/9。
- STD-00~03、cross-layer（8 negatives）、JSON/YAML parse、`git diff --check` 全 PASS。
- Zero repository mutation。

## 7. 輸出契約

```yaml
review_input:
  base_sha: d5b7c54fc87a78b1b7cba93faa4b8216239c7564
  repair_sha: 73313c8a2da731a49dc0120a1dce479dcea601af
  tree_sha: cf4113f4e7394cf1e31b94ba67209c79d7e93888
  allowlist_count: 4
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP290-RV-001: CLOSED|OPEN
  SSP290-RV-002: CLOSED|OPEN
  SSP290-RV-003: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 三個 finding 任一 `OPEN` 或出新 P0/P1 → `NO_GO`，回同一 review line 下一輪 repair。
- 三個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane A 開 `SSP-292`。
