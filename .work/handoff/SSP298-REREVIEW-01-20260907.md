---
id: SSP298-REREVIEW-01-20260907
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP298-REVIEW-20260907
original_review_commit: 67f76fa5fdc7aadee178e45d76af05c66bb5139c
repair_card: .work/CARD-SSP298-REPAIR-01-20260907.md
evidence: .work/evidence/SSP298-REPAIR-01-20260907.md
---

# SSP-298 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP298-RV-004/005/006/007` 是否關閉，以及是否對 SSP-298 契約造成 regression。**不重開全 review。** 原 review 目標 `67f76fa` 維持 immutable。

Branch 已 push origin；審查 diff 用 `git fetch origin && git diff 5bf7357..c91c02d` 產生。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base（原 review line tip）：`5bf7357cf47900435f9b2695b606eac018916fb0`
- Repair commit：`c91c02dee2129547cb6cf25c3cbf7abf0c4e94b9`
- Expected parent：`5bf7357cf47900435f9b2695b606eac018916fb0`
- Repair tree：`bf2ac237d93e36196f244cf981b0febce5e011aa`
- 唯一審查 diff：`5bf7357..c91c02d`
- Branch：`cc/ssp298-aiwr-boundary`

```sh
git fetch origin
git show -s --format='%H %P %T' c91c02dee2129547cb6cf25c3cbf7abf0c4e94b9
git diff --name-status 5bf7357cf47900435f9b2695b606eac018916fb0 c91c02dee2129547cb6cf25c3cbf7abf0c4e94b9
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（5 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP298-REPAIR-01-20260907.md` | `66e5485047716b10d913001457cf1a95d2202486` |
| M | `scripts/validate_ai_work_record_boundary_contract.rb` | `9b8009b990f519685eda63d72759242aa254cba8` |
| M | `規格/v0.1/ai-work-record-boundary.yaml` | `4f14cbc4011c8c6274cb7698a1c1f0ce444d3b0e` |
| M | `規格/v0.1/fixtures/ai-work-record-boundary-negative-fixtures.json` | `d75ea488e4a29b9d10985c2d47aafbdd692f312a` |
| M | `規格/v0.1/fixtures/ai-work-record-boundary-positive-fixtures.json` | `811437e3027ad30828a3bfdc88c209c677a64528` |

`personal-harness-integration.yaml` 不在本次 diff。

## 3. 明確排除

- 原 review 已通過、非本次 findings 的部分（signal 清單、transient-kind 交叉引用、cross_reference、hard_stops、`purpose.no_second_workflow_authority`）。
- `SSP-290` / EPM。
- 全 repo re-scan。

## 4. 可重跑命令（在 `c91c02d`）

```sh
export UV_CACHE_DIR=/tmp/ssp298-rereview-uv
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 5bf7357 c91c02d
```

## 5. Finding-by-finding acceptance

### SSP298-RV-004（P1）— partial promotion path

- `promotion_path_failure`：`steps` 必須 `== EXPECTED_PROMOTION_STEPS[0, steps.length]`（由 index 0 起的連續 prefix）；含 `PERSONAL_MEMORY_RECORD` → prefix 長度必為 6 = 完整 sequence。duplicate token → `PROMOTION_DUPLICATE_STEP`；空 → `PROMOTION_EMPTY_PATH`。
- 驗：`["PERSONAL_MEMORY_CANDIDATE"]`、`["PERSONAL_MEMORY_RECORD"]`、少掉第一步 `TASK_CARD_OR_WORK_RECORD` 的完整鏈、`RAW_EVIDENCE_ENVELOPE` 出現在 `CANDIDATE` 之後、重複 token —— 全部要被拒且回對應 code。
- 驗：candidate-only 正例（合法 3-prefix）仍 allow；full path 正例仍 allow。
- 加壓：`steps` 是合法 prefix 但長度 5（到 `PERSONAL_ACCEPTANCE`）且 `personal_acceptance_ref` 缺 → `PROMOTION_MISSING_RECEIPT`（prefix 合法才會走到 receipt 檢查）。

### SSP298-RV-005（P1）— automated-step gate

- schema ref 用 `present?`（null / `""` → `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`）。
- 契約 `human_acceptance_gate_position` 必填、`allowed_gate_positions == [VERIFICATION, PERSONAL_ACCEPTANCE]`（structural exact-set）。
- evaluator：position 缺 → `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`；不在集合 → `AUTOMATED_STEP_GATE_POSITION_INVALID`。
- 驗：`input_schema_ref: null`、無 `human_acceptance_gate_position` key、`human_acceptance_gate_position: "RAW_EVIDENCE_ENVELOPE"` 各自回對應 code；把 evaluator 改回 `key?` 或移除 membership 檢查 → 對應負例轉 RED。
- 驗 precedence：一個 step 同時 `error_behavior: SWALLOW` 且 `human_acceptance_gate: false` → 應回 `AUTOMATED_STEP_FAIL_SILENT`（先於 gate 檢查）。

### SSP298-RV-006（P2）— forbidden authority exact-set

- structural：`task_card.forbidden_authority` exact-set == `[memory_acceptance, verification, canonical_writer]`；`work_record.forbidden_authority` exact-set == `[memory_acceptance, canonical_writer]`。
- 驗：YAML 移除 `canonical_writer`、或 TaskCard 移除 `verification`、或多加一項 → structural gate 轉 RED。

### SSP298-RV-007（P2）— `TASK_CARD_DIRECT_TO_RECORD` 真 coverage

- `promotion_path_failure`：`origin == "TASK_CARD"` 且 `steps == [TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_RECORD]` → `TASK_CARD_DIRECT_TO_RECORD`（在 duplicate 之後、prefix 之前）。
- fixture `AIWR_PP_NEG_TASK_CARD_DIRECT_TO_RECORD` 現為 `[TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_RECORD]` + `origin: TASK_CARD`。
- 驗：`origin: WORK_RECORD` 的相同 steps 應回 `PROMOTION_SKIPS_EVIDENCE`（不是 direct-to-record）；把該 fixture 的 `expected_failure_code` 改錯 → RED。
- 驗雙向 parity：這個 edge 與 `AIWR_PP_NEG_PARTIAL_SKIPS_EVIDENCE`（`[TASK_CARD_OR_WORK_RECORD, PERSONAL_MEMORY_CANDIDATE]`）分別回 `TASK_CARD_DIRECT_TO_RECORD` 與 `PROMOTION_SKIPS_EVIDENCE`，確認 origin + step shape 判別正確。

## 6. Regression acceptance

- `ai-work-record-boundary` validator PASS；7 個原必備 boundary negative label 仍全覆蓋。
- `personal-memory` validator PASS（cross_reference 未受影響）。
- STD schema engine、STD-00~03、cross-layer（8 negatives）、JSON/YAML parse、`git diff --check` 全 PASS。
- Zero repository mutation。

## 7. 輸出契約

```yaml
review_input:
  base_sha: 5bf7357cf47900435f9b2695b606eac018916fb0
  repair_sha: c91c02dee2129547cb6cf25c3cbf7abf0c4e94b9
  tree_sha: bf2ac237d93e36196f244cf981b0febce5e011aa
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP298-RV-004: CLOSED|OPEN
  SSP298-RV-005: CLOSED|OPEN
  SSP298-RV-006: CLOSED|OPEN
  SSP298-RV-007: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 任一 finding `OPEN` 或出新 P0/P1 → `NO_GO`，回同一 review line 下一輪 repair。
- 四個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane B 開 `SSP-299`。
