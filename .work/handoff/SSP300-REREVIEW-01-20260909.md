---
id: SSP300-REREVIEW-01-20260909
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP300-REVIEW-20260909
original_review_commit: ce154ecd642c9e7198ef84925ea497297d5cb2f8
repair_card: .work/CARD-SSP300-REPAIR-01-20260909.md
evidence: .work/evidence/SSP300-REPAIR-01-20260909.md
---

# SSP-300 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP300-RV-001..006` 是否關閉與 regression。**不重開全 review。** 原 review 目標 `ce154ec` 維持 immutable。Branch 已 push；審查 diff 用 `git fetch origin && git diff cf15496..f630164`。

## 1. 綁定

- Base（原 review line tip）：`cf15496d1030660ae29e578459f650aaa1b8bdaf`
- Repair commit：`f630164860f0d5124d4e6a4eefc6cf28deaa8176`
- Expected parent：`cf15496d1030660ae29e578459f650aaa1b8bdaf`
- Repair tree：`b92729305c87a7f4574109ff26d00d60775a7b87`
- 唯一審查 diff：`cf15496..f630164`
- Branch：`cc/ssp300-work-record-skill`

```sh
git fetch origin
git show -s --format='%H %P %T' f630164860f0d5124d4e6a4eefc6cf28deaa8176
git diff --name-status cf15496d1030660ae29e578459f650aaa1b8bdaf f630164860f0d5124d4e6a4eefc6cf28deaa8176
```

## 2. In-scope allowlist（4 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP300-REPAIR-01-20260909.md` | `3b4d065da22c3ccf38b925463b7a7c9facf6db7c` |
| M | `scripts/validate_ai_work_record_skill_contract.rb` | `2d178f748ef98a33151a1c82f1edbfa734459f38` |
| M | `規格/v0.1/ai-work-record-skill.yaml` | `bc5611f533b37a1aa42eef73fc93155458f9a244` |
| M | `規格/v0.1/fixtures/ai-work-record-skill-negative-fixtures.json` | `5af24da3d0488a8c85559d7edc2c67eaed02b287` |

正例 fixtures 未改（既有 2 個在新規則下仍通過）。

## 3. 明確排除

- boundary / task-card-record spec（只讀 + 交叉驗證，未改）、`SSP-290`~`SSP-293` / EPM、STD。全 repo re-scan。
- `draft_card_contract_valid?` 是第二份實作 —— 已標註待 repo-wide Refactor 卡抽共用 helper；本輪只驗它與 SSP-299 逐項 parity（含新補的 sensitive-content），**不將「第二份實作」本身列 finding**（已排定處理）。

## 4. 可重跑命令（在 `f630164`）

```sh
export UV_CACHE_DIR=/tmp/ssp300-rereview-uv
ruby scripts/validate_ai_work_record_skill_contract.rb
ruby scripts/validate_ai_task_card_record_contract.rb
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check cf15496 f630164
```

## 5. Finding-by-finding acceptance

### SSP300-RV-001（P1）— illegal lifecycle replay
- `replay_lifecycle` 逐步走 `ai-task-card-record.allowed_status_transitions`；違法步 → `ILLEGAL_STATUS_TRANSITION`；`replayed_status` = walk 結果。
- 驗：`[start, complete]`（OPEN→DONE 非法）、`[start, complete, cancel]`、`[start, submit_review, block]`（IN_REVIEW→BLOCKED 非法）→ 全部 `ILLEGAL_STATUS_TRANSITION`；合法序列 replayed_status 正確；移除 replay → `SKILL_NEG_ILLEGAL_LIFECYCLE_TRANSITION` 轉 RED。
- 加壓:與 SSP-299 `validate_ai_task_card_record_contract.rb` 的 `lifecycle_replay_failure` 逐項比對(是否同一 transition table、同一非法判定)。

### SSP300-RV-002（P1）— draft-card parity（sensitive content）
- `draft_card_contract_valid?` 現拒 `sensitive_content_copied == true`。
- 驗：逐項比對本函式 vs SSP-299 `task_card_record_failure` —— fields presence、4 non-empty string、`card_id` URN、evidence URN array、status enum、boundary forbidden_fields、DONE-needs-evidence、sensitive_content_copied。找出任何仍不一致處(唯一應存在的差異:SSP-299 的 `CARD_STATUS_LIFECYCLE_MISMATCH`,本卡改由 RV-004 覆蓋)。

### SSP300-RV-003（P1）— human gate position
- YAML `authority.human_acceptance_gate_position: PERSONAL_ACCEPTANCE`；structural assert ∈ `boundary.automated_step_contract.allowed_gate_positions`（`[VERIFICATION, PERSONAL_ACCEPTANCE]`）。
- 驗：移除欄位 → RED；改成 `VERIFICATION` → 應仍 PASS；改成 `RAW_EVIDENCE_ENVELOPE` / `""` → RED。

### SSP300-RV-004（P2）— draft_card.status vs replayed_status
- `output.draft_card.status != output.replayed_status` → `DRAFT_CARD_STATUS_MISMATCH`（在 `REPLAYED_STATUS_MISMATCH` 之後）。
- 驗：`SKILL_NEG_DRAFT_CARD_STATUS_MISMATCH`；同時 replayed 也錯 → 應先回 `REPLAYED_STATUS_MISMATCH`（precedence）。

### SSP300-RV-005（P2）— dry_run bool
- `[true, false].include?(output["dry_run"])` 否則 `DRY_RUN_NOT_BOOLEAN`。
- 驗:`"banana"` / `1` / `"true"` / `nil` / 缺 key(缺 key 先被 output MISSING_INPUT_FIELD 攔)→ 有值非 bool 時 `DRY_RUN_NOT_BOOLEAN`。

### SSP300-RV-006（P2）— FAIL_LOUD 收緊
- `present?(output["error"]) && output["ok"] != false` → `FAIL_SILENT`。
- 驗:`{error:"boom"}`(無 ok)、`{error:"boom", ok:"true"}`、`{error:"boom", ok:true}` → 全 `FAIL_SILENT`；`{error:"boom", ok:false}` → 放行(loud failure 正確宣告);`{error:""}` → `present?("")` false → 不算(空 error 視為無 error,判斷是否合理)。

## 6. Regression acceptance

- skill validator PASS(264 行 < 400)；11 個 skill negative label 全覆蓋。
- task-card-record / boundary / personal-memory / STD schema engine / STD-00~03 / cross-layer(8 negatives)/ JSON·YAML parse / `git diff --check` / zero-mutation 全 PASS。

## 7. 輸出契約

```yaml
review_input:
  base_sha: cf15496d1030660ae29e578459f650aaa1b8bdaf
  repair_sha: f630164860f0d5124d4e6a4eefc6cf28deaa8176
  tree_sha: b92729305c87a7f4574109ff26d00d60775a7b87
  allowlist_count: 4
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP300-RV-001: CLOSED|OPEN
  SSP300-RV-002: CLOSED|OPEN
  SSP300-RV-003: CLOSED|OPEN
  SSP300-RV-004: CLOSED|OPEN
  SSP300-RV-005: CLOSED|OPEN
  SSP300-RV-006: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 任一 finding `OPEN` 或出新 P0/P1 → `NO_GO`。
- 六個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline accept 後：Lane B 下一張是 **repo-wide validator Refactor 卡**（在 SSP-301 之前）。
