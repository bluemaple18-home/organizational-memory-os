---
id: SSP299-REREVIEW-01-20260908
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP299-REVIEW-20260908
original_review_commit: 86aaf8e79631580f486eb7a6508078cd2bbdc326
repair_card: .work/CARD-SSP299-REPAIR-01-20260908.md
evidence: .work/evidence/SSP299-REPAIR-01-20260908.md
---

# SSP-299 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP299-RV-001/002/003` 是否關閉與 regression。**不重開全 review。** 原 review 目標 `86aaf8e` 維持 immutable。Branch 已 push；審查 diff 用 `git fetch origin && git diff 22a1605..a49b11f`。

## 1. 綁定

- Base（原 review line tip）：`22a16057aea515f183447ee946910827cb8cc0cc`
- Repair commit：`a49b11f881bf0aa6ca7ce8f7a1c6f872481127f4`
- Expected parent：`22a16057aea515f183447ee946910827cb8cc0cc`
- Repair tree：`58ba9296252b4a270566f22848aceaa715ac4074`
- 唯一審查 diff：`22a1605..a49b11f`
- Branch：`cc/ssp299-task-card-format`

```sh
git fetch origin
git show -s --format='%H %P %T' a49b11f881bf0aa6ca7ce8f7a1c6f872481127f4
git diff --name-status 22a16057aea515f183447ee946910827cb8cc0cc a49b11f881bf0aa6ca7ce8f7a1c6f872481127f4
```

## 2. In-scope allowlist（4 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP299-REPAIR-01-20260908.md` | `f34d5ce6d707596c8e29a40ac6e4325435c24410` |
| M | `scripts/validate_ai_task_card_record_contract.rb` | `2e61a4c20973e0cb723eaa2b79205dc5ade36a90` |
| M | `規格/v0.1/fixtures/ai-task-card-record-negative-fixtures.json` | `7b8f0388e1834c3c99653381fa503521bbbdcdba` |
| M | `規格/v0.1/fixtures/ai-task-card-record-positive-fixtures.json` | `b2d8b2e2bd3aa788c41a7fd394a409e526ac5bb7` |

`ai-task-card-record.yaml` 不在本次 diff（`cross_reference.must_match.*_from` pointer 早已存在）。

## 3. 明確排除

- 原 review 已通過、非本次 findings 的部分（`fields` keys/required、`allowed_status_transitions`、`lifecycle_event_to_status`、7 個原 record negative label）。
- `SSP-298` 的 `ai-work-record-boundary.yaml`（只讀 + 交叉驗證）、`SSP-290` / EPM、STD。全 repo re-scan。

## 4. 可重跑命令（在 `a49b11f`）

```sh
export UV_CACHE_DIR=/tmp/ssp299-rereview-uv
ruby scripts/validate_ai_task_card_record_contract.rb
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 22a1605 a49b11f
```

## 5. Finding-by-finding acceptance

### SSP299-RV-001（P1）— URN 型別 enforcement
- `card_id` 必須 String 且符合 `urn:omos:task-card:<uuid>`；`evidence_refs` 每項必須符合 `urn:omos:<ns>:<...>` string。不符 → `CARD_INVALID_FIELD_TYPE`。
- 驗：`card_id` 給 number / 非 task-card URN / 大寫 hex / 缺 uuid；`evidence_refs` 給 `["x"]` / `[123]` / `[{}]` / `["urn:other:..."]` → 全 `CARD_INVALID_FIELD_TYPE`；正例仍通過；移除任一 URN 檢查 → 對應負例轉 RED。
- 加壓：`evidence_refs: []`（空陣列，`.all?` 對空為 true）—— OPEN 時應 allow，DONE 時應 `CARD_DONE_WITHOUT_EVIDENCE`（不因空陣列誤判型別）。

### SSP299-RV-002（P2）— lifecycle final vs card.status
- `reconstruction_failure`：card 通過 + lifecycle 通過 + replay final == `card.status`，否則 `CARD_STATUS_LIFECYCLE_MISMATCH`。
- 驗：`TCR_IR_NEG_STATUS_MISMATCH`（card OPEN、events → DONE）→ `CARD_STATUS_LIFECYCLE_MISMATCH`；card 本身無效或 events 非法時 → 應先回 card/lifecycle 的 code（precedence）；移除 status 比對 → RED。
- 加壓：card 與 events 都合法但 events 少一步（final = IN_REVIEW）而 card.status = DONE。

### SSP299-RV-003（P2）— cross-reference pointer binding
- structural：四個 `must_match.*_from` pointer 鎖為 exact boundary path；boundary `task_card.forbidden_authority` 必須非空。
- 驗：改任一 pointer 字串、或清空 boundary `forbidden_authority` → structural gate 轉 RED。
- 定位確認：這是 **pointer binding**，不是 value-copy parity（本 slice 不複製 boundary 的 `forbidden_authority` 值）。若 reviewer 認為 Acceptance #7 要求 value parity 才算滿足，可提 finding 要求把值也帶進本卡並 assert 相等。

## 6. Regression acceptance

- `ai-task-card-record` validator PASS；7 個原 record negative label + 新 label 全覆蓋。
- `ai-work-record-boundary` validator PASS（7 個 boundary negative label 未受影響）。
- `personal-memory`、STD schema engine、STD-00~03、cross-layer（8 negatives）、JSON/YAML parse、`git diff --check`、zero-mutation 全 PASS。

## 7. 輸出契約

```yaml
review_input:
  base_sha: 22a16057aea515f183447ee946910827cb8cc0cc
  repair_sha: a49b11f881bf0aa6ca7ce8f7a1c6f872481127f4
  tree_sha: 58ba9296252b4a270566f22848aceaa715ac4074
  allowlist_count: 4
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP299-RV-001: CLOSED|OPEN
  SSP299-RV-002: CLOSED|OPEN
  SSP299-RV-003: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 任一 finding `OPEN` 或出新 P0/P1 → `NO_GO`，回同一 review line 下一輪 repair。
- 三個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane B 開 `SSP-300`。
