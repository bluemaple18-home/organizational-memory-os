---
id: SSP292-REREVIEW-01-20260908
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP292-REVIEW-20260908
original_review_commit: 6bdf56f982955e0badceb8e9b342faa987e207af
repair_card: .work/CARD-SSP292-REPAIR-01-20260908.md
evidence: .work/evidence/SSP292-REPAIR-01-20260908.md
---

# SSP-292 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP292-RV-001/002/003/004` 是否關閉與 regression。**不重開全 review。** 原 review 目標 `6bdf56f` 維持 immutable。Branch 已 push；審查 diff 用 `git fetch origin && git diff 07ce413..2b507a0`。

## 1. 綁定

- Base（原 review line tip）：`07ce413f4e3019941d7421eb4b784db660d59d62`
- Repair commit：`2b507a0f71044be76e0a8fb3150088a4b3194867`
- Expected parent：`07ce413f4e3019941d7421eb4b784db660d59d62`
- Repair tree：`34c89cbdad452d64abbba754deffc006e245a955`
- 唯一審查 diff：`07ce413..2b507a0`
- Branch：`cc/ssp292-recall-context-pack`

```sh
git fetch origin
git show -s --format='%H %P %T' 2b507a0f71044be76e0a8fb3150088a4b3194867
git diff --name-status 07ce413f4e3019941d7421eb4b784db660d59d62 2b507a0f71044be76e0a8fb3150088a4b3194867
```

## 2. In-scope allowlist（5 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP292-REPAIR-01-20260908.md` | `713f20f371f534e766c2281f98c1adb42f899215` |
| M | `scripts/validate_personal_memory_contract.rb` | `51cb6c3bc9d289862dcd620c4f51812c59f6e176` |
| M | `規格/v0.1/personal-harness-integration.yaml` | `62798c63879c66b1a78505d594ae3fe17c241fda` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `a006bb3e592b28e794d7a46c70036616879b80bd` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `ca15ab5c24843fdda58642fd072772578bdc1d81` |

## 3. 明確排除

- SSP-290 已通過的 `capability_*`（只驗未 regress）。
- `recall_context_pack` 原有非本次 findings 的部分（`input` / `output` / `permission_strategy`）。
- EMEM-00/01、`SSP-298`/AIWR、STD。全 repo re-scan。

## 4. 可重跑命令（在 `2b507a0`）

```sh
export UV_CACHE_DIR=/tmp/ssp292-rereview-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 07ce413 2b507a0
```

## 5. Finding-by-finding acceptance

### SSP292-RV-001（P0）— cross-owner intersection ref
- evaluator 現在要求 matching entry 存在且 `present?(entry["intersection_ref"])`；`permission_intersection_entry_fields` 契約鎖定為 `[memory_ref, intersection_ref]`。
- 驗：`intersection_ref: ""` / `null` / 缺 key / entry `memory_ref` 對不上 → 全部 `CROSS_OWNER_WITHOUT_INTERSECTION`；把 evaluator 還原成只比 `memory_ref` → `CTXPACK_NEG_CROSS_OWNER_EMPTY_INTERSECTION_REF` 轉 RED。
- 加壓：`permission_intersection_refs` 放一個 selected 沒有的 `memory_ref`（多而非少）；同一 memory 兩筆 entry 一有效一無效。

### SSP292-RV-002（P0）— permission decision ref present
- `pack_id` / `request_ref` / `permission_decision_ref` 用 `present?`；`permission_decision_covered_memory_refs` / `permission_intersection_refs` 必須是 Array。
- 驗：三者各給 `null` / `""` → `CONTEXT_PACK_MISSING_FIELD`；`permission_decision_covered_memory_refs` 給物件或字串 → 同 code。
- 加壓：`permission_decision_before_selection: true` 但 `permission_decision_ref: ""` —— 確認回 `CONTEXT_PACK_MISSING_FIELD`（先於 `PERMISSION_DECISION_AFTER_SELECTION`）。

### SSP292-RV-003（P1）— NOT_APPLICABLE_FILTERED enforcement
- `pack_required_fields` 加 `not_applicable_filtered_count`（structural 鎖定第 11 位）；evaluator `>0` 且 gap notice 無 `NOT_APPLICABLE_FILTERED` → `NOT_APPLICABLE_WITHOUT_NOTICE`。
- 驗：count 2 + notice 空 → 該 code；count 2 + notice 含 `NOT_APPLICABLE_FILTERED` → allow；移除 evaluator 分支或 yaml 欄位 → RED。
- 加壓：count 為字串 `"2"`（`.to_i` 行為）、負數。

### SSP292-RV-004（P1）— forbidden search strategies
- `forbidden_search_strategies` 對映表在契約與常數（`RECALL_FORBIDDEN_SEARCH_STRATEGIES`）雙鎖；evaluator 查表命中即回對映 code。
- 驗：`search_strategy: "UNRESTRICTED_GLOBAL_SEARCH"` → `UNRESTRICTED_GLOBAL_SEARCH`；`"GLOBAL_THEN_PROMPT_GUARD"` → `SEARCH_ALL_THEN_PROMPT_GUARD`；yaml 對映表少一項或 evaluator 還原成單策略 → RED。
- 加壓：`search_strategy` 為未知非 forbidden 值（例如 `"HYBRID"`）—— 應不因此被擋（不在 forbidden 表就放行）。

## 6. Regression acceptance

- `personal-memory` validator PASS；6 個原必備 recall negative label + 新 label 全覆蓋；SSP-290 6 個 capability negative label 未 regress。
- STD schema engine PASS（STD01 12/12、STD02 16/16、STD03 9/9）；STD-00~03、cross-layer（8 negatives）、JSON/YAML parse、`git diff --check`、zero-mutation 全 PASS。

## 7. 輸出契約

```yaml
review_input:
  base_sha: 07ce413f4e3019941d7421eb4b784db660d59d62
  repair_sha: 2b507a0f71044be76e0a8fb3150088a4b3194867
  tree_sha: 34c89cbdad452d64abbba754deffc006e245a955
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP292-RV-001: CLOSED|OPEN
  SSP292-RV-002: CLOSED|OPEN
  SSP292-RV-003: CLOSED|OPEN
  SSP292-RV-004: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 任一 finding `OPEN` 或出新 P0/P1 → `NO_GO`，回同一 review line 下一輪 repair。
- 四個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane A 開 `SSP-293`。
