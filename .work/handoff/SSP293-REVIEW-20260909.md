---
id: SSP293-REVIEW-20260909
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP293-CORRECTION-SUPERSESSION-20260909.md
evidence: .work/evidence/SSP293-CORRECTION-SUPERSESSION-20260909.md
---

# SSP-293 大 review 交接卡

EMEM-04 Correction / Supersession 契約。Branch 已 push origin；審查 diff 用 `git fetch origin && git diff 8e7985f..8ca640b`。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`8e7985f97aa47eb0cba0996792a697601adf9ca4`（`cc/ssp292-recall-context-pack` accepted tip）
- Review commit：`8ca640b1417189bd8d8637c7035afa00843e96c3`
- Review tree：`859dd5d9e79b6d75d57645e6e329af51704dc3aa`
- 唯一審查 diff：`8e7985f..8ca640b`
- Branch：`cc/ssp293-correction-supersession`

```sh
git fetch origin
git show -s --format='%H %P %T' 8ca640b1417189bd8d8637c7035afa00843e96c3
git diff --name-status 8e7985f97aa47eb0cba0996792a697601adf9ca4 8ca640b1417189bd8d8637c7035afa00843e96c3
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（5 路徑）

| 狀態 | 路徑 | review blob OID |
|---|---|---|
| M | `規格/v0.1/personal-harness-integration.yaml` | `2aac98e566bb8324f6c2af4e62fe348d4a9a1cfd` |
| M | `scripts/validate_personal_memory_contract.rb` | `b766df66785384db41f47817e60819faa5ce4123` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `879eb1a93c135e80297ccd9d0eab2251f1dd083b` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `5cbc941f9a0f905293b0e1341bf37b745669d697` |
| A | `.work/CARD-SSP293-CORRECTION-SUPERSESSION-20260909.md` | `dbba0bc5fada98a4c633b18b75759a668a5bdd9b` |

## 3. 明確排除

- SSP-290 `capability_*`、SSP-292 `recall_context_pack.contract`（本 diff 未改，只驗未 regress）。
- `personal_memory_resource_contracts` 的 4 個 resource（未動；correction/supersession 契約放在 `correction_flow.contract`，不進 resource exact-4 清單）。
- `correction_flow.steps`、EMEM-00/01 已鎖欄位、`core_invariants` 文字、`SSP-298`/AIWR、STD。全 repo re-scan。

## 4. Normative authority 與可重跑命令

- `correction_flow.contract` 是本 slice 的 authority；`MemoryCorrectionProposal` / `MemorySupersessionReceipt` 的 `required_fields_ref` 指回它。
- `correction_proposal_failure` / `supersession_receipt_failure` 是薄 evaluator：只驗 shape、verify→accept gate、`correction_kind → resulting_record_status` 映射、no in-place overwrite、receipt immutability、no history erasure、SUPERSEDE 需 distinct `new_record_ref`。fixture 的 `proposal` / `receipt` 為自足物件。

```sh
export UV_CACHE_DIR=/tmp/ssp293-review-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 8e7985f 8ca640b
```

Mainline 實測全 PASS（見 evidence）。不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement | 證明 fixture（→ expected code） |
|---|---|---|
| correction 由 Evidence + Proposal 啟動 | `correction_proposal_failure`：8 欄位 + `correction_evidence_refs` 非空陣列 | `CORR_PROP_NEG_NO_EVIDENCE` → `CORRECTION_MISSING_EVIDENCE`；`CORR_PROP_POS_AMEND`（正例） |
| 修正前必過 verification (PASS) | `supersession_receipt_failure`：`verification_status != PASS` 或缺 `verification_receipt_ref` → `CORRECTION_SKIPS_VERIFICATION`；structural `gate.verification_status_must_be == PASS` | `CORR_RECEIPT_NEG_SKIPS_VERIFICATION` → `CORRECTION_SKIPS_VERIFICATION` |
| 修正前必過 personal acceptance | 缺 `personal_acceptance_ref` → `CORRECTION_SKIPS_ACCEPTANCE`（gate_enforced_fields，不放 required list 以免被 missing-field 攔掉） | `CORR_RECEIPT_NEG_SKIPS_ACCEPTANCE` → `CORRECTION_SKIPS_ACCEPTANCE` |
| 舊 Record 不原地覆寫 | `in_place_overwrite == true` 或 `in_place_content_patch` present → `CORRECTION_IN_PLACE_OVERWRITE`；structural `forbidden` 含 `in_place_record_overwrite` | `CORR_RECEIPT_NEG_IN_PLACE_OVERWRITE` → `CORRECTION_IN_PLACE_OVERWRITE` |
| history 不得抹除 | 缺 `old_record_ref`（→ MISSING_FIELD）或 `history_erasure == true` → `CORRECTION_HISTORY_ERASURE`；structural `correction_flow.history_erasure == forbidden` | `CORR_RECEIPT_NEG_HISTORY_ERASURE` → `CORRECTION_HISTORY_ERASURE` |
| `correction_kind` → lifecycle status 固定映射 | `resulting_record_status != EXPECTED_CORRECTION_KINDS[kind]` → `CORRECTION_KIND_LIFECYCLE_MISMATCH`；structural `correction_kinds` == 鎖定映射且目標 ∈ `record_status` enum | `CORR_RECEIPT_NEG_KIND_LIFECYCLE_MISMATCH`（AMEND + INVALIDATED）→ `CORRECTION_KIND_LIFECYCLE_MISMATCH` |
| supersession receipt immutable | `mutated == true` 或 `receipt_superseded_by` present → `CORRECTION_RECEIPT_MUTATED`；structural `immutable_receipt == true`、`forbidden` 含 `receipt_mutation` | `CORR_RECEIPT_NEG_MUTATED_AFTER_THE_FACT` → `CORRECTION_RECEIPT_MUTATED` |
| SUPERSEDE 必須有 distinct new_record_ref | kind == SUPERSEDE 時要求 `new_record_ref` 為 record urn 且 ≠ `old_record_ref` → `CORRECTION_SUPERSEDE_MISSING_NEW_RECORD`；structural `supersede_requires_new_record_ref == true` | `CORR_RECEIPT_POS_SUPERSEDE_NEW_RECORD`（正例）；負向由 evaluator 分支覆蓋 |
| 負例覆蓋 7 個必備 label | `EXPECTED_CORRECTION_NEGATIVE_LABELS - covered` 必須為空；每個 `actual == expected_failure_code` | 7 個 `covers_correction_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| Evaluator precedence（見 evidence）| MISSING → INVALID_ID → MUTATED → HISTORY_ERASURE → IN_PLACE → KIND_INVALID → SKIPS_VERIFICATION → SKIPS_ACCEPTANCE → KIND_LIFECYCLE_MISMATCH → SUPERSEDE_MISSING_NEW | 讓一個 receipt 同時 `mutated:true` 且 `history_erasure:true` 且 skips verification —— 確認回 `CORRECTION_RECEIPT_MUTATED`（最前），且 fixture 宣告一致；反向組合逐一驗 |
| `gate_enforced_fields` 不在 required list | `verification_receipt_ref` / `personal_acceptance_ref` 缺 → SKIPS_VERIFICATION / SKIPS_ACCEPTANCE（不是 MISSING_FIELD） | 確認這是刻意設計（語意正確性 > 通用 missing-field）；`verification_receipt_ref` 缺但 `verification_status: PASS` → 應回 `CORRECTION_SKIPS_VERIFICATION` |
| SUPERSEDE `new_record_ref == old_record_ref` | evaluator 明確擋 | 給相同 ref、給非 record urn、給缺 key —— 全 `CORRECTION_SUPERSEDE_MISSING_NEW_RECORD` |
| `correction_kind` 目標 status 與 PMR lifecycle | structural 交叉檢查目標 ∈ `record_status` enum；但未檢查 PMR `allowed_transitions[ACTIVE]` 是否包含該目標 | 判斷：AMEND/SUPERSEDE→SUPERSEDED、INVALIDATE→INVALIDATED 都在 `ACTIVE` 的 allowed_transitions 內（`SUPERSEDED`/`INVALIDATED`）；若 reviewer 認為應直接 assert transition 合法可提 P3 |
| `allowed_resource_ref?` 泛用性 | 用 `start_with?("urn:omos:personal-memory:correction-proposal:")` 等；不驗 uuid 格式 | 給 `urn:omos:personal-memory:correction-proposal:`（空 suffix）、大小寫、多冒號 —— 判斷是否要收緊（可能 P3，SSP-290/292 同一 helper 行為一致） |
| 與 SSP-290 / SSP-292 no-regression | 同一 validator 檔；本 diff 只加 correction 區塊 | 跑完整 validator，確認 capability（6）+ recall（6）negative label 覆蓋與 structural 斷言未破壞 |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新增 fixture / yaml 區塊放 duplicate key，確認 fail-closed |

## 7. Mainline mutation 探針

見 evidence：7 項竄改（`correction_kinds` 映射、gate status、`forbidden` 移除、evaluator 移除 skips-acceptance / kind-lifecycle / missing-evidence 三分支、負例錯 code）全部使 validator RED，還原後 diff 空。

## 8. 結構化輸出契約

```yaml
review_input:
  base_sha: 8e7985f97aa47eb0cba0996792a697601adf9ca4
  review_sha: 8ca640b1417189bd8d8637c7035afa00843e96c3
  tree_sha: 859dd5d9e79b6d75d57645e6e329af51704dc3aa
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP293-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    personal_memory_contract: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    cross_layer: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = correction 可跳過 verification/acceptance、舊 record 可被原地覆寫、history 可被抹除；P1 = 主要 correction 契約可被繞過、kind→lifecycle 映射可錯、receipt 可事後改寫、負例 false coverage、SSP-290/292 regress；P2 = 可控邊界或覆蓋缺口；P3 = 非阻塞（例如 URN 格式收緊、直接 assert PMR transition）。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane A 後續（`SSP-294` 起）。
- P2 / P3 → 記 backlog；若能跳過 verify/accept 或抹除歷史，升 P1。
- 無 P0 / P1 且 binding / structural / fixture parity / mutation / zero-mutation 均 PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane A 開 `SSP-294`。
- 出 P1 → 沿同一 review line 定點複審，新 repair commit 另附 SHA / parent / tree / blob；`8ca640b` 維持 immutable。
