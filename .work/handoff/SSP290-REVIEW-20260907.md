---
id: SSP290-REVIEW-20260907
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP290-PERSONAL-KNOWLEDGE-LEVELS-20260907.md
evidence: .work/evidence/SSP290-PERSONAL-KNOWLEDGE-LEVELS-20260907.md
companion_diff: .work/handoff/SSP290-REVIEW-20260907.diff
---

# SSP-290 大 review 交接卡

Personal Knowledge L1～L4 能力等級契約。平台無關、對凍結 commit 自足。任何具備 repo 的 reviewer 依第 4 節命令重跑；無 repo 的 reviewer 依 companion diff `.work/handoff/SSP290-REVIEW-20260907.diff`（`78e570a..a93313d` 的完整 unified diff）審。

## 1. 審查身份與不可變邊界

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`78e570a74af45f5337d7db8376cd83b67e131b26`
- Review commit：`a93313d18884b0dd73b33632e813d18b7f255b7b`
- Review tree：`d838e8bdc4cea03ca094dff8e4ddc241070a0809`
- 唯一審查 diff：`78e570a74af45f5337d7db8376cd83b67e131b26..a93313d18884b0dd73b33632e813d18b7f255b7b`
- Branch label：`cc/ssp290-personal-knowledge-levels`（本地 worktree，未 push；label 僅供取得物件，審查輸入以上述 commit／tree／blob 為準）。

Review 開始後 `a93313d` 凍結。不得把 branch tip、後續 commit、working tree 或未追蹤檔案納入同一輪 review。Reviewer 只讀 git object，在 repo 外產生 stdout／receipt，不得寫 `.work/current/*`、`status.md` 或任何 repository path。

綁定檢查：

```sh
git cat-file -e a93313d18884b0dd73b33632e813d18b7f255b7b^{commit}
git cat-file -e 78e570a74af45f5337d7db8376cd83b67e131b26^{commit}
git show -s --format='%H %P %T' a93313d18884b0dd73b33632e813d18b7f255b7b
git diff --name-status 78e570a74af45f5337d7db8376cd83b67e131b26 a93313d18884b0dd73b33632e813d18b7f255b7b
```

預期 `git show` 三欄依序為 review SHA、base SHA、tree SHA。任一不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist 與 commit blob OID

`git diff-tree -r 78e570a..a93313d` 的完整結果（5 路徑，全 `M`）：

| 路徑 | review blob OID |
|---|---|
| `規格/v0.1/personal-harness-integration.yaml` | `cf963e2d69da15745f01c4f9736b386d58195691` |
| `scripts/validate_personal_memory_contract.rb` | `9bfb30535985525912c058e0d8287f5439c3ab4e` |
| `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `c8b4083a045b11d45b8cc3568ae22e8786da860b` |
| `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `5942c5dc0a76cad3b27e4bad947df557a1a16f3a` |
| `.work/CARD-SSP290-PERSONAL-KNOWLEDGE-LEVELS-20260907.md` | `a09a49ccaaf346cef91ddb9cda44fd45d3a15754` |

以 `git diff-tree --no-commit-id --raw -r --abbrev=40 78e570a a93313d` 重算並逐列比對。不得以 working-tree `hash-object` 取代 commit blob。

## 3. 明確排除範圍

- `.ai/cc_task_*`、其他 `.work/` 卡片與 evidence（除本卡對應者）。
- `SSP-298` / AIWR 線的任何檔案。
- `capability_levels` 既有 `behavior` 清單的措辭（本卡不改）。
- EMEM-00 / EMEM-01 已鎖資源契約、`actor_action_policy`、`promotion_widening_gate`（除交叉引用外不改）。
- runtime / connector / DB / per-tenant override / UI。
- `文件/待辦重整.md`、backlog 狀態（大 review `GO` 後才由 Owner 更新）。

## 4. Normative authority 與可重跑命令

- `personal-harness-integration.yaml` 是 employee personal memory standard 的 authority（`schema.version 0.3.0`）。
- `validate_personal_memory_contract.rb` 為 imperative validator：structural 斷言 + fixture 評估；`read_json` 用 `StrictJsonObject`（duplicate JSON key fail-closed），`read_yaml` 用 `assert_unique_yaml_mapping_keys` + `YAML.safe_load(aliases:false)`。
- 本卡新增部分只擴充 `capability_matrix` / `level_transitions` / `capability_safety_floor` 三個 top-level block 及對應 validator 分支與 fixtures，不改既有 request-policy / resource-contract 邏輯。

可重跑（在 `a93313d`，乾淨 detached checkout）：

```sh
export UV_CACHE_DIR=/tmp/ssp290-review-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 78e570a a93313d
```

Mainline 實測全 PASS（見 evidence）。Reviewer 不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement（`validate_personal_memory_contract.rb`） | 證明 fixture（authority → expected code） |
|---|---|---|
| L1～L4 各級能力授予固定；higher ⊇ lower（cumulative） | 第 589 行起：`capability_matrix.grants` 逐級 `== EXPECTED_LEVEL_GRANTS[level]`；各級 disjoint；聯集 `== EXPECTED_MATRIX_CAPABILITIES`；`cumulative == true` | `CAP_POS_L1..L4`（正例）；`CAP_NEG_L1_CLAIMS_L3_CAPABILITY`（`capability_profile_failure` → `CAPABILITY_ABOVE_LEVEL_GRANT`） |
| profile 不得宣稱超出該級 effective grant 的 capability | `capability_profile_failure`（第 477 行）＋`effective_capability_grant`（第 467 行） | `CAP_NEG_L1_CLAIMS_L3_CAPABILITY` → `CAPABILITY_ABOVE_LEVEL_GRANT` |
| profile 引用不存在的 level 必須被拒 | `capability_profile_failure` 首個分支 | `CAP_NEG_PROFILE_UNKNOWN_LEVEL` → `UNKNOWN_CAPABILITY_LEVEL` |
| level 不得 fork 核心 lifecycle（`same_contract_all_levels`） | `capability_profile_failure` `forks_core_contract` 分支；structural：`capability_levels.same_contract_all_levels == true` | `CAP_NEG_LEVEL_FORKS_CORE_CONTRACT` → `LEVEL_FORKS_CORE_CONTRACT` |
| L4 的 correction / promotion proposal 不可跳過 authority gate | `capability_profile_failure` L4 分支：`claimed & L4_AUTHORITY_GATED_ACTIONS` 非空且 `authority_gate_satisfied != true` | `CAP_NEG_L4_AUTO_PROMOTE_WITHOUT_GATE` → `L4_ACTION_BYPASSES_AUTHORITY_GATE` |
| level 轉換只能單步（無 L1→L3 跳躍） | `level_transition_failure`（第 500 行）：`allowed_upgrades/downgrades[from]` 不含 `to` → 拒 | `TR_NEG_L1_TO_L3_MULTI_STEP_JUMP` → `LEVEL_TRANSITION_NOT_SINGLE_STEP` |
| 轉換須 `authorized_actor` 且 `reason_recorded` | `level_transition_failure` 對應分支；structural：`transition_requirements` set 相符 | `TR_POS_*`（正例帶兩旗標）；反向由 evaluator 分支覆蓋 |
| 降級不得移除任一 safety-floor invariant | `level_transition_failure` downgrade 分支比對 `safety_floor_after` 全 `true`；structural：`downgrade_preserves_all_invariants == true`、`downgrade_rules.must_preserve_safety_floor == true` | `TR_NEG_DOWNGRADE_DROPS_PERMISSION_FLOOR` → `DOWNGRADE_REMOVES_SAFETY_FLOOR`；`TR_POS_DOWN_*`（floor 全 true 通過） |
| safety floor（Permission-before-Retrieval、Provenance、Canonical Single Writer、candidate acceptance gate、promotion widening gate）適用所有 level | structural：`capability_safety_floor.invariants == EXPECTED_CAPABILITY_SAFETY_FLOOR`、`applies_to_all_levels == true`；`capability_profile_failure` `safety_floor_state` 分支 | `CAP_POS_L1..L4`（floor 全 true）；`SAFETY_FLOOR_INVARIANT_MISSING` 由 evaluator 分支覆蓋 |
| L4 authority gate 常在 | structural：`l4_authority_gate.authority_gate_required == true`、`gated_actions == [correction_proposal, promotion_proposal]`；交叉檢查 `capability_levels.L4.authority_gate_required == true` | 竄改探針（第 7 節）驗證 fail-closed |
| 負例覆蓋 6 個必備 label | 第 745 行起：`EXPECTED_CAPABILITY_NEGATIVE_LABELS - covered` 必須為空；每個負例 `actual == expected_failure_code` | 6 個 `covers_capability_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面（不可被 mainline PASS 掩蓋）

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| False coverage：負例因無關原因被拒 | 每個 capability 負例斷言 `actual == expected_failure_code`（宣告 code parity） | 逐一確認 `expected_failure_code` 與該負例唯一 mutation 對應；試著讓某負例同時觸發兩個 code，確認斷言仍精確 |
| Cumulative grant 邊界 | `effective_capability_grant` = 本級 ∪ 較低級 | 驗 L2 profile 宣稱純 L1 capability 應 allow；L3 profile 宣稱 L4 capability 應 `CAPABILITY_ABOVE_LEVEL_GRANT`；空 `claimed_capabilities` 行為 |
| Downgrade safety-floor 檢查僅在 downgrade | `level_transition_failure` `unless is_upgrade` 分支 | 確認 upgrade case 不誤要求 `safety_floor_after`；確認 downgrade 缺 `safety_floor_after` 整個 key 時的行為（目前 `.fetch(...,{})` → 任一 invariant `!= true` → 拒） |
| Structural 斷言 vs YAML 實體 | grants 逐級 `==` 鎖定 array（順序敏感） | 改 grants 內元素順序、增刪一項、跨級搬移一項，確認全部轉紅；改 `capabilities` 清單順序（用 `sorted_set` 比對，應仍綠）與內容（應紅） |
| `capability_levels` 未被破壞 | 交叉檢查 `capability_levels.L4.authority_gate_required`、`same_contract_all_levels` | 確認新 block 是 sibling、未改 `EXPECTED_CAPABILITY_KEYS` 既有斷言；`capability_levels` keys 仍剛好 5 個 |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新增 fixture / yaml block 內放 duplicate key，確認 fail-closed |
| L4_AUTHORITY_GATED_ACTIONS 與 yaml 三處一致 | `level_gated_actions.L4`、`l4_authority_gate.gated_actions`、常數 | 改任一處使三者不一致，確認轉紅 |

「未有 dedicated fixture」不先判 finding，但須以讀碼或 repo 外 probe 判 parity。

## 7. Mainline 已做的 mutation 探針

見 evidence「Mutation 探針」表：9 項竄改（grants row、safety-floor invariant、`downgrade_preserves_all_invariants`、L4 gate、L1→L3 轉換、負例錯 code、負例失效、正例越權、grants.L1 竄改）全部使 validator RED，還原後 `git diff --stat` 空。此為內部 checkpoint，不降低外部門檻。

## 8. 結構化輸出契約

回一份 stdout／repo 外 receipt：

```yaml
review_input:
  repository: bluemaple18-home/organizational-memory-os
  base_sha: 78e570a74af45f5337d7db8376cd83b67e131b26
  review_sha: a93313d18884b0dd73b33632e813d18b7f255b7b
  tree_sha: d838e8bdc4cea03ca094dff8e4ddc241070a0809
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP290-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    personal_memory_contract: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    cross_layer: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = 契約破壞 / 安全底線可繞過完全失效；P1 = 主要契約可被繞過、safety floor 可被降級移除、負例可被無關原因假裝覆蓋、level 越權授予；P2 = 可控邊界或覆蓋缺口；P3 = 非阻塞維護性。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane A 後續（`SSP-292` 起）。
- P2 / P3 → 記 `文件/待辦重整.md` backlog；若其實能繞過 safety floor 或 level 授權，升 P1。
- 無 P0 / P1 且 binding、structural、fixture parity、mutation、zero-mutation 均 PASS → 外部 gate 可 `GO`。Mainline 收到後自驗 binding / redaction / zero-mutation，再更新 backlog + Jira，Owner accept。
- 出 P1 → 修復後沿同一 review line 定點複審（只驗原 finding + regression），新 repair commit 另附完整 SHA / parent / tree / blob；`a93313d` 維持 immutable。

Zero-mutation 收尾：

```sh
git diff --exit-code
git diff --cached --exit-code
git status --porcelain=v1
```

最後 `git status --porcelain=v1` 必須為空。
