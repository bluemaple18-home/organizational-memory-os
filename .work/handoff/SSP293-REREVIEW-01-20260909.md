---
id: SSP293-REREVIEW-01-20260909
status: READY_FOR_TARGETED_REREVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
review_line: SSP293-REVIEW-20260909
original_review_commit: 8ca640b1417189bd8d8637c7035afa00843e96c3
repair_card: .work/CARD-SSP293-REPAIR-01-20260909.md
evidence: .work/evidence/SSP293-REPAIR-01-20260909.md
---

# SSP-293 定點複審交接卡（Repair 01）

沿原 review line。只驗 `SSP293-RV-001/002/003/004` 是否關閉與 regression。**不重開全 review。** 原 review 目標 `8ca640b` 維持 immutable。Branch 已 push；審查 diff 用 `git fetch origin && git diff 1f0fbc6..0b58535`。

## 1. 綁定

- Base（原 review line tip）：`1f0fbc6b10028e73ff9698d7605ef58eb6aafbe6`
- Repair commit：`0b58535b8e9ff318915ac49f9bf15176ae2e8650`
- Expected parent：`1f0fbc6b10028e73ff9698d7605ef58eb6aafbe6`
- Repair tree：`d5e67f9d75c2f16978c1b75a6a77601216c847aa`
- 唯一審查 diff：`1f0fbc6..0b58535`
- Branch：`cc/ssp293-correction-supersession`

```sh
git fetch origin
git show -s --format='%H %P %T' 0b58535b8e9ff318915ac49f9bf15176ae2e8650
git diff --name-status 1f0fbc6b10028e73ff9698d7605ef58eb6aafbe6 0b58535b8e9ff318915ac49f9bf15176ae2e8650
```

## 2. In-scope allowlist（5 路徑）

| 狀態 | 路徑 | repair blob OID |
|---|---|---|
| A | `.work/CARD-SSP293-REPAIR-01-20260909.md` | `7ea166eccd41e304344f978b99dbf8318df03950` |
| M | `scripts/validate_personal_memory_contract.rb` | `2030b9b8efbfe5ed66279a8e0a6a72ea280a9b72` |
| M | `規格/v0.1/personal-harness-integration.yaml` | `442b45fb532040d506d5b378e17cd77f64b85feb` |
| M | `規格/v0.1/fixtures/personal-memory-negative-fixtures.json` | `a118ce717a5707ed7ddca2ab20edda129ffc44a2` |
| M | `規格/v0.1/fixtures/personal-memory-positive-fixtures.json` | `1a2a9414aa4f8b4baeea7a36c2202110474d00a3` |

## 3. 明確排除

- SSP-290 `capability_*`、SSP-292 `recall_context_pack.contract`（只驗未 regress）。
- `correction_flow.steps`、EMEM-00/01 已鎖欄位、`SSP-298`/AIWR、STD。全 repo re-scan。
- **本檔已知 > 400 行（1119）→ 已排定 repo-wide Refactor 卡另處理；本輪不將檔案大小列 finding。**

## 4. 可重跑命令（在 `0b58535`）

```sh
export UV_CACHE_DIR=/tmp/ssp293-rereview-uv
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 1f0fbc6 0b58535
```

## 5. Finding-by-finding acceptance

### SSP293-RV-001（P0）— gate receipt refs 驗成 ref
- `omos_urn?(value, ns)`：`urn:omos:<ns>:<...>` string。`supersession_receipt_failure`：`verification_receipt_ref` 必須 `omos_urn?(_, "verification")`、`personal_acceptance_ref` 必須 `omos_urn?(_, "acceptance")`。
- 驗：`verification_receipt_ref` 給 `123` / `true` / `"foo"` / `"urn:other:x"` → `CORRECTION_SKIPS_VERIFICATION`；`personal_acceptance_ref` 同理 → `CORRECTION_SKIPS_ACCEPTANCE`；正例（真 urn）通過；還原成 `present?`-only → `CORR_RECEIPT_NEG_FAKE_VERIFICATION_REF` / `CORR_RECEIPT_NEG_FAKE_ACCEPTANCE_REF` 轉 RED。
- 加壓：`omos_urn?` pattern 是否夠嚴（`urn:omos:verification:` 空 suffix、大寫、多冒號）—— 與 SSP-290/292 用的 URN helper 行為一致性。

### SSP293-RV-002（P1）— correction evidence 每項驗 ref
- `correction_evidence_refs` 每項必須 `omos_urn?(_, "evidence")`。
- 驗：`[null]` / `[{"fake": true}]` / `["not-a-urn"]` / `[]` → `CORRECTION_MISSING_EVIDENCE`；混一個有效一個無效 → 仍拒；正例通過。

### SSP293-RV-003（P1）— chronology 對齊
- `EXPECTED_CORRECTION_PROPOSAL_FIELDS` / `EXPECTED_SUPERSESSION_RECEIPT_FIELDS` 與 yaml required lists 現在用 `chronology.created_at`；evaluator missing-field 檢查用 `present?(dig_dotted(obj, field))`；fixtures 全部 `chronology.created_at`。
- 驗：物件用 top-level `created_at`（無 `chronology`）→ `CORRECTION_PROPOSAL_MISSING_FIELD` / `SUPERSESSION_RECEIPT_MISSING_FIELD`；yaml required list 與 `EXPECTED_*` 不一致 → structural RED。卡片 Acceptance #1 與 machine contract 現在對齊。

### SSP293-RV-004（P3）— PMR allowed-transition cross-assert
- structural：每個 `correction_kind` 目標 status 必須同時 ∈ `record_status` enum **且** ∈ `PersonalMemoryRecord.lifecycle.allowed_transitions.ACTIVE`。
- 驗：把某 `correction_kind` 目標改成 enum 內但非 ACTIVE-transition 的值 → structural RED。

## 6. Regression acceptance

- `personal-memory` validator PASS；7 個原 correction negative label + 3 個新 label 全覆蓋；SSP-290 6 + SSP-292 6 label 未 regress。
- STD schema engine、STD-00~03、cross-layer（8 negatives）、JSON/YAML parse、`git diff --check`、zero-mutation 全 PASS。

## 7. 輸出契約

```yaml
review_input:
  base_sha: 1f0fbc6b10028e73ff9698d7605ef58eb6aafbe6
  repair_sha: 0b58535b8e9ff318915ac49f9bf15176ae2e8650
  tree_sha: d5e67f9d75c2f16978c1b75a6a77601216c847aa
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
closure:
  SSP293-RV-001: CLOSED|OPEN
  SSP293-RV-002: CLOSED|OPEN
  SSP293-RV-003: CLOSED|OPEN
  SSP293-RV-004: CLOSED|OPEN
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
regression: PASS|FAIL
verdict: GO|NO_GO
findings: []
```

- 任一 finding `OPEN` 或出新 P0/P1 → `NO_GO`。
- 四個全 `CLOSED`、無新 P0/P1、regression PASS、binding + zero-mutation PASS → `GO`。Mainline accept 後：Lane A 下一張是 **repo-wide validator Refactor 卡**（在 SSP-294 之前）。
