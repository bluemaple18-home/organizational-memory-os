# Jira 同步換手單（給 Codex）

產出日期：2026-09-11　產出者：Claude Code　repo：`organizational-memory-os`
所有 SHA 均已用 `git merge-base --is-ancestor <sha> main` 驗證為 `main` 祖先。

---

## A. 待轉 Done 的既有 ticket（12 張）

這 12 張在 repo 側都已 `ACCEPTED_GO` 並落到 `main`，Jira 狀態未同步。
請逐張轉 Done，並把「驗收依據」欄的 SHA 貼進 Jira comment 作為證據。

| # | Ticket | 內容 | 驗收依據（main 上的 commit） |
| --- | --- | --- | --- |
| 1 | `SSP-290` | EMEM-01 個人知識能力分級 L1~L4 | `581c833` |
| 2 | `SSP-292` | EMEM-03 Recall Context Pack | `581c833` |
| 3 | `SSP-293` | EMEM-04 更正與取代 | `581c833` |
| 4 | `SSP-298` | AIWR-01 邊界契約 | `86ee018` |
| 5 | `SSP-299` | AIWR-02 任務卡格式 | `86ee018` |
| 6 | `SSP-300` | AIWR-03 工作紀錄 Skill | `86ee018` |
| 7 | `SSP-301` | AIWR-04 Hook 事件擷取 | `b0e0fee` |
| 8 | `SSP-302` | AIWR-05 Loop 收口與缺口補登 | `47c8ea4`（驗收）、`5201f1e`（後續契約收緊） |
| 9 | `SSP-303` | AIWR-06 輕量 Harness 編排 | `df34064` |
| 10 | `SSP-304` | AIWR-07 Hermes 薄 Adapter | `b07353a` |
| 11 | `SSP-305` | AIWR-08 端到端驗收 + 主管進度視圖 | `0d68a3f` |
| 12 | `SSP-291` | EMEM-02 Personal Evidence Profile / Source Mapping | `7cfe2fe` |

註：`df34064` / `b07353a` / `0d68a3f` / `47c8ea4` 是 `docs: accept ...` 驗收 commit，
不是 merge commit。`待辦重整.md` 舊文字寫成「merge main @ X」，措辭不精確，以此表為準。

---

## B. 進行中，先不要動 Jira 狀態（0 張）

`SSP-291` 已於 2026-09-11 完成 repair-01 定點 re-review `GO` 並 merge（`7cfe2fe`），
已移入上方 A 節第 12 列。目前沒有進行中的 ticket。

## C. 需要新建的 Jira ticket（5 張）

以下 5 張目前只存在於 repo 的 `.work/CARD-*.md`，**Jira 上沒有對應 ticket**。
它們不是 `SSP-286`/`SSP-287` 原始拆解的一部分，是施工過程中從
`文件/個人知識庫Harness提案查核與整合裁決-20260830.md` 的 repo backlog 拆出來的。

建議全部掛在 `SSP-286`（個人知識庫）底下，除第 5 張掛 `SSP-287`。
父卡歸屬請 Owner 確認後再建，我不自行決定。

### C-1　Document Adapter Mapping（repo #2）
- 狀態：**已完成**，建議建立後直接轉 Done
- 摘要：文件來源 → RawEvidence / SourceAnchor / NormalizedDocument 的投影契約
- 交付：`規格/v0.1/document-adapter-mapping.yaml`、`scripts/validate_document_adapter_mapping_contract.rb`、`scripts/validate_document_adapter_mapping_instances.py`
- 驗收依據：`ebeaab2`
- 備註：大 review `NO_GO` 6 輪後觸發 Owner spec-freeze（FP-1~FP-5 已簽），
  凍結卡 `.work/CARD-DOC-ADAPTER-DETERMINISM-SPEC-FREEZE-20260910.md`
- 卡片：`.work/CARD-DOC-ADAPTER-MAPPING-20260909.md`

### C-2　Jira Adapter Mapping（repo #3）
- 狀態：**已完成**，建議建立後直接轉 Done
- 摘要：Jira Cloud 實體 → 同一份 RawEvidence 契約的投影與版本調和
- 交付：`規格/v0.1/jira-adapter-mapping.yaml`、`scripts/validate_jira_adapter_mapping_contract.rb`、`scripts/validate_jira_adapter_mapping_instances.py`
- 驗收依據：`0ac8c09`
- 卡片：`.work/CARD-JIRA-ADAPTER-MAPPING-20260909.md`

### C-3　權限／保存／刪除契約（repo #4）
- 狀態：**Blocked — 等 Owner 簽 5 個 spec-freeze point**（T2）
- 摘要：個人記憶的存取權限、保存期限、刪除與 offboarding 行為
- 為什麼重要：這是 `SSP-294`（EMEM-05 Promotion）的前置，也是 Phase-2 來源
  （Teams／Outlook）解禁的前置 —— `SSP-291` 的 phase gate 目前把它們一律擋掉
- 卡片：`.work/CARD-PERMISSION-RETENTION-DELETION-CONTRACT-20260909.md`

### C-4　Canonical 直寫稽核（repo #5）
- 狀態：**Blocked — 等 Owner 定 scope**（T3，觸及權威邊界）
- 摘要：稽核個人記憶是否存在繞過審核直寫公司 Canonical 知識的路徑
- 為什麼重要：同為 `SSP-294` 前置
- 卡片：`.work/CARD-CANONICAL-DIRECT-WRITE-AUDIT-20260909.md`

### C-5　AIWR Loop error code 覆蓋強化
- 狀態：**Ready — 卡片已寫好，隨時可施工**（非阻塞）
- 父卡建議：`SSP-287`（或直接掛 `SSP-302` 之下）
- 摘要：`SSP-302` 大 review 留下的兩個非阻塞 finding
  （TIGHTEN-F-01 error code 自我指涉綁定、TIGHTEN-F-02 evidence 未驗證宣稱）
- 卡片：`.work/CARD-SSP302-ERROR-CODE-COVERAGE-20260911.md`

---

## D. 順手請 Codex 一併處理

1. **repo 側卡片狀態未同步。** `.work/` 底下 16 張卡的 `status:` 仍停在
   `REPAIRED_AWAITING_TARGETED_REREVIEW` / `IMPLEMENTED_AWAITING_BIG_REVIEW` /
   `NO_GO_REPAIRED_04_...`，但對應的 Document／Jira Adapter Mapping 與
   `SSP302-CONTRACT-TIGHTEN` 三條線都已 GO + merged。這是我 merge 後只更新
   `文件/待辦重整.md`、沒回頭改卡片造成的。若由 Codex 統一補，請對照
   `ebeaab2` / `0ac8c09` / `5201f1e` 三個 merge 逐張改為 `ACCEPTED_GO_<date>`。
2. **`SSP-295` 尚未開卡。** 它只出現在 Lane A 的 DoD 敘述裡
   （MVP DoD = `SSP-291`~`295` 全部 accepted），repo 側沒有卡片。
   請確認 Jira 上是否已存在該 ticket。

---

## E. 對帳用的數字

- **Jira 子卡共 18 張**（`SSP-286` / `SSP-287` 是母 epic，不計入）：
  完成 12、審查中 0、未做 6（`SSP-294` `SSP-295` `SSP-307` `SSP-308` `SSP-309` `SSP-310`）。
- **repo 側 CC 施工卡 5 張**（即上方 C 節），Jira 上目前不存在。
- 兩者是不同的帳，請勿相加。
