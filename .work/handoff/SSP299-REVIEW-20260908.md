---
id: SSP299-REVIEW-20260908
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP299-TASK-CARD-FORMAT-20260908.md
evidence: .work/evidence/SSP299-TASK-CARD-FORMAT-20260908.md
---

# SSP-299 大 review 交接卡

AIWR-02 AI 任務卡自動紀錄格式。Branch 已 push origin；審查 diff 用 `git fetch origin && git diff b0401f3..86aaf8e` 產生。

## 1. 綁定

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`b0401f34ac44ff9f3d6d8dd672d8d7e4c95a9b27`（`cc/ssp298-aiwr-boundary` accepted tip）
- Review commit：`86aaf8e79631580f486eb7a6508078cd2bbdc326`
- Review tree：`4e09b11df8f7699a254a55f6731649fb1989ec72`
- 唯一審查 diff：`b0401f3..86aaf8e`
- Branch：`cc/ssp299-task-card-format`

```sh
git fetch origin
git show -s --format='%H %P %T' 86aaf8e79631580f486eb7a6508078cd2bbdc326
git diff --name-status b0401f34ac44ff9f3d6d8dd672d8d7e4c95a9b27 86aaf8e79631580f486eb7a6508078cd2bbdc326
```

不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist（5 路徑，全新增）

| 狀態 | 路徑 | review blob OID |
|---|---|---|
| A | `規格/v0.1/ai-task-card-record.yaml` | `4fc574da887017e999345fdf5a3686180d2907e1` |
| A | `scripts/validate_ai_task_card_record_contract.rb` | `f587f7dfaafa8012eb3f4039263c6992d2aaa4d8` |
| A | `規格/v0.1/fixtures/ai-task-card-record-positive-fixtures.json` | `6188549f2dada209dc1756a272d0b75a6b694846` |
| A | `規格/v0.1/fixtures/ai-task-card-record-negative-fixtures.json` | `99af2f08687917c7e1ad883fe52bc9d4efbec5e2` |
| A | `.work/CARD-SSP299-TASK-CARD-FORMAT-20260908.md` | `c579b752a5eb01d351a993ae6b39b171a05c04e0` |

## 3. 明確排除

- `SSP-298` 的 `ai-work-record-boundary.yaml`（本卡只讀 + 交叉驗證，未改）、既有所有 validator / schema / fixture。
- Skill（`SSP-300`）、Hook（`SSP-301`）、Loop（`SSP-302`）、Harness（`SSP-303`）、主管視圖（`SSP-305`）。
- `SSP-290` / EPM、STD 契約。
- `文件/待辦重整.md`、backlog 狀態。

## 4. Normative authority 與可重跑命令

- `ai-task-card-record.yaml` 是本 slice 的 authority；boundary contract 對 `task_card` 的 role / is_personal_memory / status_enum / required_fields / forbidden_fields / forbidden_authority 仍是唯一來源，本卡以 `cross_reference` 綁定並交叉驗證。
- validator 為薄判斷：structural + 兩個 evaluator（`task_card_record_failure`、`lifecycle_replay_failure` 純函式）。`allowed_status_transitions` / `lifecycle_event_to_status` 是資料表比對，不是狀態機引擎。

```sh
export UV_CACHE_DIR=/tmp/ssp299-review-uv
ruby scripts/validate_ai_task_card_record_contract.rb
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check b0401f3 86aaf8e
```

Mainline 實測全 PASS（見 evidence）。不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement | 證明 fixture（→ expected code） |
|---|---|---|
| 卡片必含 7 控制欄位，`objective`/`scope`/`constraints`/`acceptance` 非空 | `task_card_record_failure` 首段；structural `fields` keys/required/non_empty | `TCR_NEG_MISSING_SCOPE` → `CARD_MISSING_REQUIRED_FIELD` |
| DONE 但 `evidence_refs` 空 → 拒（不假完成） | `status == DONE && evidence_refs.empty?` → `CARD_DONE_WITHOUT_EVIDENCE`；非 DONE 允許空 | `TCR_NEG_DONE_EMPTY_EVIDENCE` → `CARD_DONE_WITHOUT_EVIDENCE`；`TCR_POS_OPEN_MINIMAL`（OPEN + `[]` 通過） |
| DONE 但 `acceptance` 空 → 拒 | `acceptance` 一律 non-empty（`EXPECTED_NON_EMPTY_STRING_FIELDS`）→ `CARD_MISSING_REQUIRED_FIELD` | `TCR_NEG_DONE_EMPTY_ACCEPTANCE` → `CARD_MISSING_REQUIRED_FIELD` |
| 非法 status 轉移 → 拒 | `lifecycle_replay_failure`：target 不在 `allowed_status_transitions[current]` | `TCR_LC_NEG_ILLEGAL_TRANSITION`（`[start, complete]`）→ `CARD_ILLEGAL_STATUS_TRANSITION` |
| 未知 lifecycle event → 拒 | `lifecycle_replay_failure` 首個分支 | `TCR_LC_NEG_UNKNOWN_EVENT` → `CARD_UNKNOWN_LIFECYCLE_EVENT` |
| 敏感內容只存 reference | `card["sensitive_content_copied"] == true` → 拒 | `TCR_NEG_SENSITIVE_CONTENT_COPIED` → `CARD_SENSITIVE_CONTENT_COPIED` |
| boundary 的 forbidden_field 不得出現在卡片 | 逐一比對 boundary `task_card.forbidden_fields` | `TCR_NEG_BOUNDARY_FORBIDDEN_FIELD`（`personal_acceptance_ref`）→ `CARD_CARRIES_FORBIDDEN_FIELD` |
| status_enum / required_fields / forbidden_fields / is_personal_memory 與 boundary 一致 | structural：交叉讀 `ai-work-record-boundary.yaml` 並斷言相等 | 探針：改 `status_enum` 使與 boundary 分歧 → RED |
| 事件序列 deterministic 重建 | `lifecycle_replay_failure` 純函式；validator 對每個正例 replay 兩次比對 | `TCR_LC_POS_*`（兩條 replay 通過且兩次結果一致） |
| `allowed_status_transitions` 是資料表非引擎 | 只做 `include?` 查表，無隱藏狀態機 | 讀 validator 全文確認 |
| 負例覆蓋 7 個必備 label | `EXPECTED_RECORD_NEGATIVE_LABELS - covered` 必須為空；每個 `actual == expected_failure_code` | 7 個 `covers_record_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| `task_card_record_failure` precedence | `MISSING → INVALID_STATUS → CARRIES_FORBIDDEN → SENSITIVE → DONE_WITHOUT_EVIDENCE` | 讓一張卡同時缺欄位且 status 非法、或同時帶 forbidden field 且 sensitive_content_copied，確認回較前面的 code 且 fixture 宣告一致；把某負例改成同時觸發兩個 code 驗斷言精確 |
| `acceptance` 空的假完成 vs 一般缺欄位 | 兩者都回 `CARD_MISSING_REQUIRED_FIELD` | 確認這是設計（`acceptance` 一律非空），或提 finding 主張 DONE 專屬 code（可能 P3） |
| lifecycle replay 未驗中間 status 與卡片 `status` 欄位一致 | `lifecycle_replay_failure` 只比 `final_status`（若提供）與最後映射 | 攻擊：正例 card `status: OPEN` 但 lifecycle 是 `[start, complete]`（final DONE）—— validator 目前不交叉驗 card.status 與 lifecycle final；判斷是否 P2/P3 缺口 |
| `blank_string?` 語義 | 非 String 或 `strip.empty?` 視為空 | 給 `objective: "   "`、`objective: 123`、`acceptance: null`；`evidence_refs: "urn:..."`（字串非陣列） |
| structural exact vs sorted | `status_enum` / `lifecycle_event_to_status` 用 `==`；transitions 逐 from 用 `.sort` 比對；keys 用 `sorted_set` | 改順序 / 增刪 / 改值，確認 `==` 全轉紅、`.sort` 只在內容變時轉紅 |
| 與 boundary 的 no-regression | 新檔，未改 boundary validator | 跑 `validate_ai_work_record_boundary_contract.rb` 確認 7 個 boundary negative label 未受影響 |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新 fixture / yaml 放 duplicate key，確認 fail-closed |

## 7. Mainline mutation 探針

見 evidence：6 項竄改（transitions 加項 ×2、event map 改值、evaluator 移除 DONE-without-evidence / forbidden-field 檢查、負例錯 code）全部使 validator RED，還原後 diff 空。

## 8. 結構化輸出契約

```yaml
review_input:
  base_sha: b0401f34ac44ff9f3d6d8dd672d8d7e4c95a9b27
  review_sha: 86aaf8e79631580f486eb7a6508078cd2bbdc326
  tree_sha: 4e09b11df8f7699a254a55f6731649fb1989ec72
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP299-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    ai_task_card_record: PASS|FAIL|NOT_RUN
    ai_work_record_boundary: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = 假完成可通過（DONE 無 evidence/acceptance）/ 敏感內容或 boundary forbidden field 可進卡片；P1 = 主要格式或事件映射可被繞過、負例 false coverage、與 boundary contract 分歧未被擋、boundary regress；P2 = 可控邊界或覆蓋缺口（例：lifecycle final 未與 card.status 交叉驗）；P3 = 非阻塞。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane B 後續（`SSP-300` 起）。
- P2 / P3 → 記 backlog；若能通過假完成或塞入 forbidden field，升 P1。
- 無 P0 / P1 且 binding / structural / fixture parity / mutation / zero-mutation 均 PASS → `GO`。Mainline 自驗後更新 backlog + Jira，Owner accept，Lane B 開 `SSP-300`。
- 出 P1 → 沿同一 review line 定點複審，新 repair commit 另附 SHA / parent / tree / blob；`86aaf8e` 維持 immutable。
