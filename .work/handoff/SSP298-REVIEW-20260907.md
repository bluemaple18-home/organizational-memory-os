---
id: SSP298-REVIEW-20260907
status: READY_FOR_BIG_REVIEW
review_mode: READ_ONLY_IMMUTABLE_DIFF
card: .work/CARD-SSP298-AIWR-BOUNDARY-20260907.md
evidence: .work/evidence/SSP298-AIWR-BOUNDARY-20260907.md
companion_diff: .work/handoff/SSP298-REVIEW-20260907.diff
---

# SSP-298 大 review 交接卡

AIWR-01：AI 工作紀錄與 Personal Memory 邊界契約。平台無關、對凍結 commit 自足。有 repo 的 reviewer 依第 4 節命令重跑；無 repo 的 reviewer 依 companion diff `.work/handoff/SSP298-REVIEW-20260907.diff`（`78e570a..67f76fa` 完整 unified diff）審。

## 1. 審查身份與不可變邊界

- Repository：`bluemaple18-home/organizational-memory-os`
- Base commit：`78e570a74af45f5337d7db8376cd83b67e131b26`
- Review commit：`67f76fa5fdc7aadee178e45d76af05c66bb5139c`
- Review tree：`041a9688c5aaef3cf8f1a32d0bdd17ee2f261583`
- 唯一審查 diff：`78e570a74af45f5337d7db8376cd83b67e131b26..67f76fa5fdc7aadee178e45d76af05c66bb5139c`
- Branch label：`cc/ssp298-aiwr-boundary`（本地 worktree，未 push）。

Review 開始後 `67f76fa` 凍結。不得把 branch tip、後續 commit、working tree 或未追蹤檔案納入。Reviewer 只讀 git object，在 repo 外產生 stdout／receipt，不寫 repository path。

綁定檢查：

```sh
git cat-file -e 67f76fa5fdc7aadee178e45d76af05c66bb5139c^{commit}
git cat-file -e 78e570a74af45f5337d7db8376cd83b67e131b26^{commit}
git show -s --format='%H %P %T' 67f76fa5fdc7aadee178e45d76af05c66bb5139c
git diff --name-status 78e570a74af45f5337d7db8376cd83b67e131b26 67f76fa5fdc7aadee178e45d76af05c66bb5139c
```

`git show` 三欄依序為 review SHA、base SHA、tree SHA。不符即停，回報 `INPUT_BINDING_FAILURE`。

## 2. In-scope allowlist 與 commit blob OID

| 狀態 | 路徑 | review blob OID |
|---|---|---|
| A | `規格/v0.1/ai-work-record-boundary.yaml` | `7a923a7fb6ab6ab0aec87feb77a8a3d518a20f1f` |
| A | `scripts/validate_ai_work_record_boundary_contract.rb` | `a8d01080f177e9aba70fc7c9875684b7ced1174a` |
| A | `規格/v0.1/fixtures/ai-work-record-boundary-positive-fixtures.json` | `b2261ec86626e371d108d50ae468abc9d13a5ecb` |
| A | `規格/v0.1/fixtures/ai-work-record-boundary-negative-fixtures.json` | `eb0c2696bb0f73c61fd5ba0c4445462b1ab33614` |
| M | `.work/CARD-SSP298-AIWR-BOUNDARY-20260907.md` | `1d411a0ae13dfce24248c1111bc8789f16fddb0c` |

以 `git diff-tree --no-commit-id --raw -r --abbrev=40 78e570a 67f76fa` 重算。不得以 working-tree `hash-object` 取代 commit blob。

## 3. 明確排除範圍

- `SSP-290` / EPM 線的任何檔案。
- 既有 `personal-harness-integration.yaml`（本卡只讀，未改）、既有所有 validator / schema / fixture。
- Task Card 完整 frontmatter schema（那是 `SSP-299`）。
- Hook / Loop / Harness / Hermes / connector / runtime。
- `文件/待辦重整.md`、backlog 狀態（大 review `GO` 後才由 Owner 更新）。

## 4. Normative authority 與可重跑命令

- `ai-work-record-boundary.yaml` 是本 slice 的 authority；`personal-harness-integration.yaml` 對 `not_long_lived_memory_by_default`、`runtime_policy`、`core_invariants` 仍是唯一來源，本卡以 `cross_reference` 綁定並交叉驗證。
- `validate_ai_work_record_boundary_contract.rb` 為薄 validator：結構斷言 + 三個 evaluator（`task_card_failure` / `promotion_path_failure` / `automated_step_failure`）+ fixture parity；`read_json` 用 `StrictJsonObject`，`read_yaml` 用 `assert_unique_yaml_mapping_keys` + `YAML.safe_load(aliases:false)`。

可重跑（在 `67f76fa`，乾淨 detached checkout）：

```sh
export UV_CACHE_DIR=/tmp/ssp298-review-uv
ruby scripts/validate_ai_work_record_boundary_contract.rb
ruby scripts/validate_personal_memory_contract.rb
uv run --script scripts/validate_std_schema_engine.py
ruby scripts/validate_std00_contract.rb
ruby scripts/validate_std01_raw_evidence_contract.rb
ruby scripts/validate_std02_source_anchor_contract.rb
ruby scripts/validate_std03_normalized_document_contract.rb
uv run --script scripts/validate_cc_cross_layer_contract.py
git diff --check 78e570a 67f76fa
```

Mainline 實測全 PASS（見 evidence）。不得只重跑 PASS 命令後輸出 GO。

## 5. Spec claim → exact enforcement → fixture parity

| 契約宣稱 / invariant | Exact enforcement（`validate_ai_work_record_boundary_contract.rb`） | 證明 fixture（authority → expected code） |
|---|---|---|
| Task Card 是工作控制／回報資料，不是 Personal Memory | 第 211 行結構斷言 `task_card.is_personal_memory == false`、`role`；`task_card_failure`（第 132 行）`is_personal_memory != false` 分支 | `AIWR_TC_POS_*`（正例）；`TASK_CARD_CLAIMS_PERSONAL_MEMORY` 由 evaluator 分支覆蓋 |
| Task Card 必含 7 個控制欄位 | `task_card.required_fields == EXPECTED_TASK_CARD_REQUIRED_FIELDS`；`task_card_failure` missing 分支 | `AIWR_TC_NEG_MISSING_CONTROL_FIELD` → `TASK_CARD_MISSING_CONTROL_FIELD` |
| Task Card 禁帶 memory acceptance 欄位 | `task_card.forbidden_fields` set 相符；`task_card_failure` forbidden 分支 | `AIWR_TC_NEG_CARRIES_PERSONAL_ACCEPTANCE_REF` → `TASK_CARD_CARRIES_MEMORY_ACCEPTANCE_FIELD` |
| 敏感內容只存 reference | `task_card.sensitive_content_rule == reference_only_never_copied`；`task_card_failure` `sensitive_content_copied` 分支 | `AIWR_TC_POS_DONE_CARD_REFERENCE_ONLY`（`sensitive_content_copied: false`） |
| WorkRecord 是 REBUILDABLE_PROJECTION、0..N candidates、無 acceptance authority | 第 231 行結構斷言 `work_record.role`、`may_generate_candidates`、`forbidden_authority` | 對照既有 `personal-harness-integration.yaml#work_record`（本卡未改，交叉一致） |
| 升格路徑固定六步，不得跳過 Evidence | `promotion_path.ordered_steps == EXPECTED_PROMOTION_STEPS`；`promotion_path_failure` `before_record` 不含 `RAW_EVIDENCE_ENVELOPE` 分支 | `AIWR_PP_NEG_WORK_RECORD_SKIPS_EVIDENCE` → `PROMOTION_SKIPS_EVIDENCE`；`AIWR_PP_POS_*_FULL_PATH`（正例） |
| Task Card 不得直接建 Record | `promotion_path.rules.task_card_direct_to_record == forbidden`；`promotion_path_failure` `before_record.empty?` 分支 | `AIWR_PP_NEG_TASK_CARD_DIRECT_TO_RECORD` → `TASK_CARD_DIRECT_TO_RECORD` |
| Candidate → Record 前必須有 VERIFICATION 與 PERSONAL_ACCEPTANCE，且順序正確 | `promotion_path_failure` `PROMOTION_PREREQ_ORDER` 存在性 + index 排序檢查 | `AIWR_PP_NEG_SKIPS_VERIFICATION` → `PROMOTION_SKIPS_VERIFICATION_OR_ACCEPTANCE`；`PROMOTION_STEPS_OUT_OF_ORDER` / `PROMOTION_MISSING_RECEIPT` 由 evaluator 分支覆蓋 |
| branch / worktree / runtime_completion / model_confidence 不具 acceptance authority | `non_acceptance_authority.signals` set 相符；`promotion_path_failure` 首個分支 | `AIWR_PP_NEG_RUNTIME_COMPLETION_AS_ACCEPTANCE`、`AIWR_PP_NEG_MODEL_CONFIDENCE_AUTO_ACCEPT` → `NON_ACCEPTANCE_SIGNAL_USED_AS_AUTHORITY` |
| transient kind 不得升為長期記憶 | `promotion_path_failure` 比對 `personal_spec.not_long_lived_memory_by_default` | `AIWR_PP_NEG_CURRENT_TASK_STATUS_PROMOTED`、`AIWR_PP_NEG_BRANCH_WORKTREE_STATE_PROMOTED` → `TRANSIENT_KIND_PROMOTED` |
| 自動步驟必須 FAIL_LOUD + human gate + dry-run | `automated_step_contract.required_per_step` / `error_behavior_enum == [FAIL_LOUD]` / `fail_silent == forbidden`；`automated_step_failure` 三分支 | `AIWR_AS_NEG_NO_HUMAN_GATE` → `AUTOMATED_STEP_BYPASSES_HUMAN_GATE`；`AIWR_AS_NEG_FAIL_SILENT` → `AUTOMATED_STEP_FAIL_SILENT`；`AIWR_AS_POS_FAIL_LOUD_WITH_GATE`（正例） |
| 不新增第二套 workflow authority | `purpose.no_second_workflow_authority == true`、`purpose.does_not_define` 含 registry/FSM/DB/canonical writer；`hard_stops` | 讀碼確認 validator 無 registry / FSM / DB / 狀態機 |
| cross-reference 與 personal spec 一致 | 第 261 行：`personal_spec.not_long_lived_memory_by_default` 非空、`runtime_policy.executor_authority_over_memory == false`、四條 `reused_core_invariants` 確實存在於 `personal_spec.core_invariants` | 探針：改 `executor_authority_over_memory_must_be`、移除 signal → RED |
| 負例覆蓋 7 個必備 label | 第 325 行：`EXPECTED_BOUNDARY_NEGATIVE_LABELS - covered` 必須為空；每個負例 `actual == expected_failure_code` | 7 個 `covers_boundary_negative_fixture` label 全覆蓋 |

## 6. 指定加壓面（不可被 mainline PASS 掩蓋）

| 攻擊面 | 目前 enforcement | 外部 reviewer 必做 |
|---|---|---|
| False coverage：負例因無關原因被拒 | 每個負例斷言 `actual == expected_failure_code` | 逐一確認每個負例只觸發一個 code；把某負例改成同時觸發兩個 code，確認 evaluator 回傳的優先順序符合意圖且斷言仍精確 |
| `promotion_path_failure` 的 evaluator 是敘述性字串序列，不是真的解析 lifecycle | evaluator 只讀 `steps` array 的 token 順序與存在性 | 攻擊：`steps` 放重複 token、放正確 token 但語意亂（例如 `PERSONAL_MEMORY_RECORD` 出現兩次）、`before_record` 有 `RAW_EVIDENCE_ENVELOPE` 但在 CANDIDATE 之後；確認 `PROMOTION_STEPS_OUT_OF_ORDER` 與 index 檢查真的擋 |
| transient kind 比對依賴 personal spec 未被同輪改動 | `cross_reference` 交叉讀，但兩份 yaml 在同一 review diff 外 | 確認 `personal-harness-integration.yaml` 不在本 diff（第 2 節 allowlist 僅 5 路徑，無 personal spec）；確認 `not_long_lived_memory_by_default` 7 項與 evaluator 期待一致 |
| 結構斷言用 `==`（順序敏感）vs `sorted_set`（順序無關） | `required_fields` / `ordered_steps` 用 `==`；`status_enum` / `forbidden_fields` / `signals` / `required_per_step` 用 `sorted_set` | 對每個 list：改順序、增刪一項、改一字，確認 `==` 的全轉紅、`sorted_set` 的只在內容變時轉紅 |
| `task_card_failure` presence 檢查 | `present?` 視 `nil` / 空字串 / 空集合為缺 | `required_fields` 給 `""`、`[]`、`{}`，確認 `TASK_CARD_MISSING_CONTROL_FIELD`；`forbidden_fields` 用 `key?`（存在即擋，即使值為 `null`）—— 確認這是預期語意 |
| `automated_step_failure` 用 `key?` 而非 `present?` | 缺 key → `AUTOMATED_STEP_MISSING_CONTRACT_FIELD`；key 存在但值為 `false` 的 `human_acceptance_gate` / `dry_run_supported` → 各自 code | 放 `human_acceptance_gate: null`、`dry_run_supported: "yes"`（字串），確認行為 |
| Duplicate key | `StrictJsonObject` / `assert_unique_yaml_mapping_keys` | 在新增 fixture / yaml 內放 duplicate key，確認 fail-closed |
| 「薄判斷」宣稱 | `purpose.no_second_workflow_authority`、`hard_stops` | 讀 validator 全文，確認沒有隱藏的 registry / 狀態機 / 檔案系統 side effect / 長流程引擎 |

「未有 dedicated fixture」不先判 finding，但須以讀碼或 repo 外 probe 判 parity。

## 7. Mainline 已做的 mutation 探針

見 evidence「Mutation 探針」表：8 項竄改（signal 移除、`task_card_direct_to_record` 放行、`ordered_steps` 移除 evidence、`required_per_step` 移除 human gate、`executor_authority_over_memory_must_be` 翻轉、負例錯 code、負例失效、正例翻轉）全部使 validator RED，還原後 diff 空。第一次探針因 perl 縮排寫錯誤顯 PASS，改正後確認 RED —— 此為內部 checkpoint，不降低外部門檻。

## 8. 結構化輸出契約

```yaml
review_input:
  repository: bluemaple18-home/organizational-memory-os
  base_sha: 78e570a74af45f5337d7db8376cd83b67e131b26
  review_sha: 67f76fa5fdc7aadee178e45d76af05c66bb5139c
  tree_sha: 041a9688c5aaef3cf8f1a32d0bdd17ee2f261583
  allowlist_count: 5
  binding: PASS|FAIL
  zero_mutation: PASS|FAIL
verdict: GO|NO_GO
counts: {P0: 0, P1: 0, P2: 0, P3: 0}
findings:
  - id: SSP298-RV-001
    severity: P0|P1|P2|P3
    axis: spec|correctness|regression|coverage|maintainability
    path: <repo-relative>
    line: 1
    spec_claim: <must/forbidden>
    trigger: <minimal reproducible mutation/input>
    observed: <actual validator behavior>
    risk: <concrete consequence>
    recommendation: <minimal repair + fixture>
verification:
  commands: [<exact commands run>]
  results:
    aiwr_boundary_contract: PASS|FAIL|NOT_RUN
    personal_memory_contract: PASS|FAIL|NOT_RUN
    std_schema_engine: PASS|FAIL|NOT_RUN
    cross_layer: PASS|FAIL|NOT_RUN
    git_diff_check: PASS|FAIL|NOT_RUN
  parity_summary:
    must_claims_checked: 0
    forbidden_claims_checked: 0
    unproven_claims: []
open_questions: []
```

Severity：P0 = 邊界完全失效（runtime 狀態可無阻礙成為 accepted memory）；P1 = 主要邊界可被繞過、transient kind 可升格、非 acceptance signal 被當授權、負例可被無關原因假裝覆蓋、契約偷渡第二套 workflow authority；P2 = 可控邊界或覆蓋缺口；P3 = 非阻塞維護性。只寫「沒跑測試」不算 finding。

## Gate 與 targeted re-review

- P0 / P1 → `NO_GO`，阻塞 Lane B 後續（`SSP-299` 起）。
- P2 / P3 → 記 `文件/待辦重整.md` backlog；若其實能繞過邊界或授權，升 P1。
- 無 P0 / P1 且 binding、structural、fixture parity、mutation、zero-mutation 均 PASS → 外部 gate 可 `GO`。Mainline 自驗 binding / redaction / zero-mutation 後更新 backlog + Jira，DoD 的「邊界契約與負例獲 Owner 接受」由 Owner 明示。
- 出 P1 → 修復後沿同一 review line 定點複審（只驗原 finding + regression），新 repair commit 另附完整 SHA / parent / tree / blob；`67f76fa` 維持 immutable。

Zero-mutation 收尾：

```sh
git diff --exit-code
git diff --cached --exit-code
git status --porcelain=v1
```

最後 `git status --porcelain=v1` 必須為空。
