# PMCORE-02｜Personal Memory Candidate／Record Contract

- Objective：把 `PersonalMemoryCandidate`、`PersonalMemoryRecord`、`MemorySupportLink`、`MemoryConflictSet` 從欄位清單提升為可程式驗證的 v0.1 契約。
- Traces to：`EMEM-01`；依賴 `PMCORE-00／01=GO`；後續解鎖 `EMEM-03／04／05`，但本卡不實作它們。
- Scope：`規格/v0.1/personal-harness-integration.yaml`、Personal Memory validator、專屬正負 fixtures、必要的主文件／Backlog一致性文字與 evidence。
- Constraints：保留目前未提交的 PMCORE-00／01 變更；不改其 Ownership／Privacy決策；不做 Connector、DB、Hook、Loop、Harness、Hermes、Task Card或 Runtime event；不 commit／push。
- Acceptance：四個 resource 有明確 identity、required fields、狀態／轉換與 cross-reference 約束；Candidate 不可直接等於 accepted Record；Record 必須有 evidence/source support、verification、personal acceptance、ACL／freshness／retention；transient 類型 fail closed；ConflictSet 不可自行決定 winner；validator、正負 fixtures、YAML／JSON parse、`git diff --check` 全綠。
- Status：COMPLETE／GO（契約／validator／fixtures 層）；Implementation worker：GPT-5.5 high；獨立 Reviewer：GPT-5.5 high，兩輪修復後 GO；Mainline：2026-09-03 驗收通過；Evidence：`.work/evidence/PMCORE-02-20260902.md`。

## 最小契約邊界

1. `PersonalMemoryCandidate`：未接受的 durable-memory proposal；需 candidate identity、employee owner、memory kind、content/applicability/validity、support refs、provenance、scope/ownership/visibility/sensitivity、verification/acceptance狀態與 chronology。
2. `PersonalMemoryRecord`：只允許由合格 Candidate + verification + personal acceptance 形成；需 immutable identity、support、governance、freshness、retention與supersession links。
3. `MemorySupportLink`：精確連 Candidate/Record 到 Evidence／SourceAnchor；不得靠模型摘要充當 support。
4. `MemoryConflictSet`：表達相互矛盾或時間／scope限定的 memories；只能引用 members與resolution status，不取得 Acceptance或Canonical Writer authority。

## 驗證計畫

1. RED：新增缺 support、未 acceptance、transient memory、跨 owner／scope、conflict auto-winner 等負向 fixtures，確認現況 validator 無法保障。
2. GREEN：補最小 schema與 validator，讓合法 Candidate→Record、support link與 unresolved conflict 正向 fixtures通過。
3. Regression：重跑 PMCORE-00／01 fixtures、YAML／JSON parse、`git diff --check`；獨立 read-only review後才可 GO。

## Do not touch

- `.DS_Store`
- 既有非 PMCORE `.work` 卡片與 `km-mvp-engineering-tunnel.html`
- 遠端 Git、部署與外部服務

## 收尾 Snapshot｜2026-09-03

- Root question：四種個人記憶資源能否在不新增 runtime 的前提下，具備可驗證且互相一致的契約？
- Current state：GO；原 P1 support resolution、ConflictSet forbidden winner 與 member/support cross-reference 均經獨立複驗關閉；主線重跑 syntax、validator、YAML／JSON parse、`git diff --check` 全過。
- Blocker：本卡無未解決 P0／P1。下一個來源映射範圍 `EMEM-02` 在 canonical backlog 仍標記 `BLOCKED_BY_STD-00_AND_RAW_EVIDENCE`。
- Remaining risk：非阻塞 P2：fixture index 尚未拒絕 duplicate resource id，positive `related_resources` 尚未完整驗證其 resource contract。現有 fixtures 經 reviewer 檢查沒有重複 ID；不得將這個 validator 當作 production ingestion gate。
- Candidate fork：無新增架構分岔；P2 留待 fixture harness 強化，不擴增 runtime。
- Next step：先核實 `EMEM-02` 的 STD-00／RawEvidence 前置證據，再決定是否解鎖來源映射任務卡；不直接啟動 Connector。
- Waiting conditions：本 checkpoint 等 Owner 下一步；無需 merge／push 即可重跑本卡驗證。
- Limits：沒有實作 Skill、Hook、Loop、Harness、Hermes、自動任務卡紀錄、DB 或 Connector；未 commit／push／deploy。既有非 PMCORE 檔案保留。
