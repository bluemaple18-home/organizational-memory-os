---
id: SSP290-REPAIR-01-20260907
status: IN_PROGRESS
type: repair
jira: SSP-290
lane: A
tier: T1
review_line: SSP290-REVIEW-20260907
base_review_commit: a93313d18884b0dd73b33632e813d18b7f255b7b
---

# SSP-290 Repair 01

外部大 review verdict `NO_GO`（P0=0 / P1=1 / P2=2 / P3=0）。沿同一 review line 定點修，只處理原 findings 與 regression，`a93313d` 保持 immutable。

## Findings 與處置

### SSP290-RV-001（P1）— `authorized_actor` / `actor_authorized` 契約不一致

- 事實：`level_transitions.transition_requirements` 宣告 `authorized_actor`，但 `level_transition_failure` 讀 `transition["actor_authorized"]`，fixtures 也用 `actor_authorized` → 形成未宣告欄位。
- 修：evaluator 與全部 transition fixtures 統一為 `authorized_actor`。補 `authorized_actor: false` 與 `reason_recorded: false` 的 exact-code 負例。

### SSP290-RV-002（P2）— 安全 evaluator branch 缺 dedicated RED fixture

- 事實：`LEVEL_TRANSITION_UNAUTHORIZED_ACTOR`、`LEVEL_TRANSITION_REASON_NOT_RECORDED`、`LEVEL_TRANSITION_FORKS_CORE_CONTRACT`、`SAFETY_FLOOR_INVARIANT_MISSING` 無負例；刪掉對應 branch 現有 fixtures 不轉 RED。
- 修：每個 branch 各補單一 mutation 負例 + exact failure code。

### SSP290-RV-003（P2）— YAML transition policy 部分 normative 欄位未鎖

- 事實：`downgrade_rules.reduces_automation_depth_only` 未驗；`forbidden` 只驗包含 `downgrade_that_removes_safety_floor`（非 exact set）。
- 修：鎖 `reduces_automation_depth_only == true`；`forbidden` 改 exact-set parity。

## Constraints

- 只改 `level_transitions` 相關 evaluator / structural 斷言 / transition fixtures 與 backlog；不動 `capability_matrix` grants、`capability_safety_floor` 既有鎖定值、EMEM-00/01 契約。
- 不新增 package dependency。不 merge / push（Owner 已授權 push branch，repair commit 推同 branch）。

## Acceptance

1. `level_transition_failure` 與所有 transition fixtures 使用單一欄位名 `authorized_actor`；structural `transition_requirements` 斷言不變。
2. 負例覆蓋：`LEVEL_TRANSITION_UNAUTHORIZED_ACTOR`、`LEVEL_TRANSITION_REASON_NOT_RECORDED`、`LEVEL_TRANSITION_FORKS_CORE_CONTRACT`、`SAFETY_FLOOR_INVARIANT_MISSING` 各一，`actual == expected_failure_code`。
3. structural 斷言新增：`level_transitions.downgrade_rules.reduces_automation_depth_only == true`；`level_transitions.forbidden` exact-set == `[multi_step_jump, unauthorized_actor, downgrade_that_removes_safety_floor, transition_that_forks_core_contract]`。
4. 每個新斷言有 mutation 探針證明 fail-closed。
5. `ruby scripts/validate_personal_memory_contract.rb`、STD schema engine、STD-00~03、cross-layer、JSON/YAML parse、`git diff --check` 全 PASS。

## Likely files

- `scripts/validate_personal_memory_contract.rb`
- `規格/v0.1/fixtures/personal-memory-positive-fixtures.json`
- `規格/v0.1/fixtures/personal-memory-negative-fixtures.json`
- `.work/evidence/SSP290-REPAIR-01-20260907.md`

## Evidence

`.work/evidence/SSP290-REPAIR-01-20260907.md`
