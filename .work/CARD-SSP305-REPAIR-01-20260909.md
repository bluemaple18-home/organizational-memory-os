---
id: SSP305-REPAIR-01-20260909
status: ACCEPTED_GO_20260909
type: implementation-repair
jira: SSP-305
lane: B
tier: T1
parent_card: .work/CARD-SSP305-E2E-MANAGER-VIEW-20260909.md
review_line: .work/handoff/SSP305-REVIEW-20260909.md
frozen_review_sha: 54891cc580d4ffaa169628be0e9c91a181f380a9
---

# Repair 01｜SSP-305 大 review NO_GO（4×P1 + 1×P2）

## 帶回的 findings

| id | severity | 內容 |
|---|---|---|
| F-01 | P1 | `manager_view.status` 只要求非空,沒綁 `final_status` → Jira 主管視圖可顯示與 e2e 判定不同的終止狀態並通過。 |
| F-02 | P1 | memory_promotion_gate 只驗 `candidate_ref + acceptance_ref`,正例甚至把缺 verification 的 record 當已升格 → 放寬 SSP-298 已鎖的 promotion_path（`RAW_EVIDENCE → CANDIDATE → VERIFICATION → PERSONAL_ACCEPTANCE → RECORD`,`skip_any_step forbidden`），跨卡 regression。 |
| F-03 | P1 | spec 宣稱 `manager_statuses` 有 DONE/FAILED/CANCELLED/BLOCKED/HUMAN_INTERVENTION,但 `terminal_events` 只有 complete/cancel、evaluator 強制 complete→DONE / cancel→CANCELLED → FAILED/BLOCKED/HUMAN_INTERVENTION 沒有任何合法 trace 可通過,DoD 尚未閉合。 |
| F-04 | P1 | evaluator 沒驗 `run["is_projection"] == true`;`is_projection: false` 的 run 仍能通過 → 繞過 projection-only authority boundary。 |
| F-05 | P2 | evaluator 沒 replay `ai-task-card-record.allowed_status_transitions` → 可認證 `start → cancel → complete` 這種上游不合法 trace（SSP-299 本身會擋,故 P2）。 |

## 定點修法（同 branch `cc/ssp305-e2e-manager-view`,原 review SHA `54891cc` immutable）

1. **F-01**：`e2e_acceptance_failure` 在狀態完整性後加 `return "E2E_MANAGER_STATUS_MISMATCH"
   if view["status"] != final_status`。yaml `manager_view.status_binding_rule`；`error_contract` 補該碼。
   新增負例 `E2E_NEG_MANAGER_STATUS_MISMATCH`（trace DONE,`view.status: FAILED`）。
2. **F-02**：memory_promotion_gate 改為要求完整 promotion-chain refs
   `[raw_evidence_ref, candidate_ref, verification_receipt_ref, personal_acceptance_ref,
   record_ref]` 皆為 URN。yaml `memory_promotion_gate` 補 `promotion_path_ref` +
   `promotion_refs`（step→ref），結構斷言鎖定 `promotion_refs` 值 = 完整清單、步驟都在
   boundary `promotion_path.ordered_steps`、且與 boundary `receipts_required`
   （VERIFICATION / PERSONAL_ACCEPTANCE）一致。`cross_reference.must_match` 補
   `promotion_path_from` / `promotion_receipts_from`。修正正例
   `E2E_POS_CANCELLED_WITH_PROMOTED_RECORD` 帶完整 5 個 refs;新增負例
   `E2E_NEG_PROMOTION_CHAIN_INCOMPLETE`（只有 candidate + acceptance）。
3. **F-03**：`terminal_events` 擴為 `[complete, cancel, block]`；`terminal_event_to_status`
   加 `block: BLOCKED`；新增 `unsubstantiated_outcome_statuses: [FAILED, HUMAN_INTERVENTION]`——
   這兩個 status 的 run 必須 trace 終止 event ≠ `complete` 且帶 `outcome_evidence_ref`（URN），
   否則 `E2E_UNSUBSTANTIATED_OUTCOME`。結構斷言鎖「五個 manager_status 都有終止路徑
   （3 mapped + 2 unsubstantiated）」。`error_contract` 補 `E2E_UNSUBSTANTIATED_OUTCOME`。
   新增正例 `E2E_POS_BLOCKED` / `E2E_POS_FAILED` / `E2E_POS_HUMAN_INTERVENTION`；
   新增負例 `E2E_NEG_UNSUBSTANTIATED_OUTCOME`（trace 收在 complete 卻宣稱 FAILED）。
4. **F-04**：evaluator authority 段首加 `return "E2E_EXCEEDS_AUTHORITY" unless
   run["is_projection"] == true`。yaml `authority.rule` 補述。新增負例 `E2E_NEG_NOT_PROJECTION`。
5. **F-05**：evaluator trace 段加 `allowed_status_transitions` replay（沿用 SSP-302 pattern）——
   非法 transition → `E2E_TRACE_ILLEGAL_TRANSITION`。yaml `e2e_trace.transition_replay_rule`；
   `cross_reference.must_match` 補 `allowed_transitions_from`；`error_contract` 補該碼。
   新增負例 `E2E_NEG_ILLEGAL_TRANSITION`（`start → cancel → complete`）。
6. `required_negative_fixtures` 與 `EXPECTED_E2E_NEGATIVE_LABELS` 各 12 → 16。

## 驗證（定點：只驗原 finding + 行為 regression）

### 原 findings 消除

| finding | 負例 / 正例 | 結果 |
|---|---|---|
| F-01 | `E2E_NEG_MANAGER_STATUS_MISMATCH` | `E2E_MANAGER_STATUS_MISMATCH` |
| F-02 | `E2E_NEG_PROMOTION_CHAIN_INCOMPLETE`;正例 `E2E_POS_CANCELLED_WITH_PROMOTED_RECORD` 帶完整 5 refs | `E2E_WORKRECORD_PROMOTED_WITHOUT_ACCEPTANCE` |
| F-03 | 正例 `E2E_POS_BLOCKED` / `E2E_POS_FAILED` / `E2E_POS_HUMAN_INTERVENTION` allow;負例 `E2E_NEG_UNSUBSTANTIATED_OUTCOME` | allow / `E2E_UNSUBSTANTIATED_OUTCOME` |
| F-04 | `E2E_NEG_NOT_PROJECTION` | `E2E_EXCEEDS_AUTHORITY` |
| F-05 | `E2E_NEG_ILLEGAL_TRANSITION` | `E2E_TRACE_ILLEGAL_TRANSITION` |

### enforcement parity（移除 enforcement → gate 轉紅）

`E2E_MANAGER_STATUS_MISMATCH` / promotion 完整 chain 檢查（弱化成 candidate+acceptance）/
`E2E_UNSUBSTANTIATED_OUTCOME` / `is_projection` 強制 / `E2E_TRACE_ILLEGAL_TRANSITION` /
`E2E_STATUS_UNMAPPED`(regression) —— 逐一移除 → `ruby` exit 1（RED）;原有 enforcement
亦重跑保持 load-bearing;全部還原後 `PASS`。

### 行為 regression

- 全 gate suite 綠：18 Ruby validator + `std_schema_engine`（coverage 12/12、16/16、9/9 不變）
  + cross-layer + `git diff --check`。
- 上游 `ai-task-card-record` / `ai-work-record-boundary` / `ai-work-record-harness` /
  `{hook,loop,skill,hermes-adapter}` 未改動。
- validator 281 行（< 400）。
- promotion_gate 結構斷言直接讀 SSP-298 `promotion_path.ordered_steps` /
  `receipts_required`,上游改動會自動失配。

## 檔案

- `scripts/validate_ai_work_record_e2e_acceptance_contract.rb`（`is_projection` 強制 + transition
  replay + `E2E_MANAGER_STATUS_MISMATCH` + `E2E_UNSUBSTANTIATED_OUTCOME` + 完整 promotion-chain
  refs + `EXPECTED_TERMINAL_EVENTS`/`EXPECTED_TERMINAL_EVENT_TO_STATUS`/`EXPECTED_UNSUBSTANTIATED_STATUSES`/
  `EXPECTED_PROMOTION_REF_FIELDS` + 4 負 label + promotion/transition 結構斷言）
- `規格/v0.1/ai-work-record-e2e-acceptance.yaml`（`e2e_trace.transition_replay_rule` /
  `manager_view.status_binding_rule` / `status_completeness` +block +unsubstantiated /
  `memory_promotion_gate` +promotion_path_ref +promotion_refs / `authority.rule` /
  `cross_reference.must_match` +3 / `error_contract` +3 / `required_negative_fixtures` +4）
- `規格/v0.1/fixtures/ai-work-record-e2e-acceptance-positive-fixtures.json`（2 → 5 case）
- `規格/v0.1/fixtures/ai-work-record-e2e-acceptance-negative-fixtures.json`（12 → 16 case）
- `.work/evidence/SSP305-E2E-MANAGER-VIEW-20260909.md`（append repair 段）
- `.work/CARD-SSP305-E2E-MANAGER-VIEW-20260909.md`（status note）

## 給定點複審

- review line 不變：`.work/handoff/SSP305-REVIEW-20260909.md`。原 frozen review SHA `54891cc` 不動。
- 只需驗：(a) F-01～F-05 是否消除（對應 3 個新正例 + 5 個新負例 + enforcement parity）;
  (b) 有無新 regression（其餘 validator、gate suite、上游契約）。
- 無 P0/P1 → 寫 `.work/evidence/SSP305-REREVIEW-01-20260909.yaml` → Owner accept →
  merge `cc/ssp305-e2e-manager-view` → `main` → **Lane B（AIWR 線）DoD 達成**。
