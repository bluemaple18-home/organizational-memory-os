# 下一台電腦從這裡開始

這個 Repo 是 **Organizational Memory OS／企業知識 SaaS** 的架構、研究與 backlog 交接 authority。

## 先鎖定：不要重新設計

接手後先讀本文件，再依序讀 `文件/` 內文件。不要重新猜需求，不要把 AI Core runtime 搬進來，也不要因 AI Core 的個人開發 priority 降低本產品的企業產品 priority。

正式原則：

> **共享研究，產品優先級獨立。**

> **先找先例；吸收成熟工程，不搬 donor authority。**

> **借工程、保架構。**

> **Knowledge Architecture != Agent Architecture。**

> **保留 Knowledge Domain Architecture；把 Agent、Model、Worker、Runtime 降回可替換 executor。**

## 不可翻案的六條 Truth Boundary

1. **證據不等於知識**：Outlook、Teams、Slack、Jira、文件、Agent 執行、人的操作先是 Evidence。
2. **候選不等於正式知識**：只有通過驗證與接受決策的 mutation 才能寫入 Canonical Knowledge。
3. **權限必須先於檢索**：未授權資料不能先進 Vector/Graph Search 再叫模型不要講。
4. **執行不等於驗證，驗證不等於接受**：`EXECUTED != VERIFIED != ACCEPTED`。
5. **修改提案不等於正式寫入**：Canonical Knowledge 採 Single Authoritative Writer。
6. **知識不等於流程/Skill**：Knowledge 是知道什麼；Procedure/Skill 是怎麼做。

## 目前最新 Rebaseline

2026-08-26 已加入 `文件/最小核心重基準.md`。

本輪不是把 Knowledge OS 砍成傳統 RAG，而是重新區分：

### 產品/domain 必須保留

- Source / Evidence
- Object / Work Context
- Knowledge Candidate / Canonical Knowledge
- ACL / Provenance / Conflict / Freshness
- Verification / Acceptance
- Canonical Single Writer
- Projection boundary
- Permission-before-Retrieval
- Context Budget / Bounded Retrieval
- Evaluation / Failure Routing

### 應降級成 executor / runtime detail

- Resident Agent
- Conflict Agent
- Review Agent
- Ingestion Agent
- Evaluation Agent
- Repair Agent
- default multi-agent topology
- per-agent registry / ledger / FSM

Agent 名稱不再自動構成 permanent subsystem。

## Executor Policy

預設採：

```text
DETERMINISTIC_LOCAL
→ BOUNDED_CHEAP_MODEL
→ STRONG_MODEL
→ HUMAN_AUTHORITY
```

hash、identity、schema validation、ACL propagation、source deletion、exact duplicate、version compare、parser、projection rebuild 等優先 deterministic。

語意分類、claim extraction、ambiguous linking、conflict interpretation、summarization、candidate generation、answer generation 才用模型。

## 核心 Spine

```text
來源
文件 / Outlook / Teams / Slack / Jira / Git / 人工 / Agent
  ↓
Raw Evidence
  ↓
Normalize / Atomic Extraction / Object Linking
  ↓
Work Record Projection / Knowledge Candidate
  ↓
Knowledge Staging
  ↓
Permission / Provenance / Conflict / Review / Verification
  ↓
Acceptance Decision
  ↓
Canonical Knowledge Writer
  ↓
Canonical Knowledge
  ↓
Rebuildable Projections
Vector / Search / Graph / Chunk
  ↓
Permission-before-Retrieval
  ↓
Authorized Retrieval Space
  ↓
Intent / Domain
  ↓
Context Budget
  ↓
Bounded Retrieval
  ↓
Answer / Action
  ↓
Usage / Feedback / Evaluation Evidence
  ↺ Re-evaluation / Revision Candidate
```

## MVP 不是完整產品

MVP 要證明一條垂直閉環：

```text
Jira / 文件為主，Outlook / Teams 作下一步來源
→ Evidence + Provenance
→ Object / Work Context / Candidate
→ Staging
→ Review / Conflict / Approval
→ Canonical Knowledge
→ Permission-before-Retrieval
→ Context Budget / Bounded Retrieval
→ PM/RD Answer + Citation
→ Machine/Human Evaluation
→ Failure → Correction → Re-evaluation
```

不阻塞 MVP：漂亮 Knowledge Graph、Achievement、Skill Marketplace、Shared Blackboard、Multi-Agent Runtime。

## 三個維度不能混

- **架構優先級**：P0 / P1 / P2 / Trigger-based
- **交付成熟度**：MVP / NEXT / NORTH STAR
- **商業能力等級**：L1 / L2 / L3 / L4

例如 Telemetry 可是 P0，但 MVP 只交付 L1；Repository Intelligence 可是 P1，但不一定進第一個 MVP。

## 下一步先做 Read-Only Reconciliation

不要直接施工。

先對 current component / backlog 標：

- `DOMAIN_CORE`
- `EXECUTOR_ONLY`
- `DETERMINISTIC_FIRST`
- `MODEL_ASSISTED`
- `RUNTIME_OWNED`
- `PROJECTION_ONLY`
- `ENTERPRISE_GOVERNANCE`
- `DEFER`
- `REJECT`

然後回傳：

1. 哪些 Agent 卡應改名為 Domain Capability
2. 哪些 Runtime 卡應降為 implementation note
3. 哪些 deterministic operation 不需要 Agent
4. 哪些 Graph / Vector / Chunk 卡應標 Projection Only
5. 哪些卡 amendment / merge / defer / cancel
6. 哪些企業治理能力必須保留

## 必讀文件

1. `文件/架構憲章.md`
2. `文件/最小核心重基準.md`
3. `文件/核心知識主幹.md`
4. `文件/MVP與優先級.md`
5. `文件/個人證據與工作紀錄.md`
6. `文件/權限政策與設定治理.md`
7. `文件/上下文預算與檢索.md`
8. `文件/驗證治理.md`
9. `文件/SaaS能力等級.md`
10. `文件/先例技術來源地圖.md`
11. `文件/待辦重整.md`
12. `文件/AI-Core邊界.md`
13. `文件/禁止重刻政策.md`
